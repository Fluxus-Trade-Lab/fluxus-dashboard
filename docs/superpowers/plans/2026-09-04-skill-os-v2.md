# 技能操作系统 v2 实施计划（按官方机制）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 skill 靠官方 description 机制被触发（触发率可量）、用完必须留痕（Stop hook）、按官方评估回路变好（带/不带对照）；现存 79 件四档处置；权限层补根；code-cartography 用文档 TDD 建起来。

**Architecture:** 不造路由表。触发＝`description`/`when_to_use`/`paths:` + skill-creator 描述优化器；留痕＝一个 Stop hook（读 `last_assistant_message`）；迭代＝skill-creator 评估回路（technique）或 writing-skills 压力场景（discipline）；周检读四个官方能量的数。

**Tech Stack:** Python 3.11+ 标准库 · pytest · Claude Code hooks（`.claude/settings.json` 入库）· skill-creator 脚本（`~/.claude/plugins/cache/anthropic-agent-skills/document-skills/b29e7cf65e5c/skills/skill-creator/`）· `claude -p`（描述优化器与评估都靠它）· Workflow fan-out（审计）

## Global Constraints

- Spec：`docs/superpowers/specs/2026-09-04-skill-os-v2-design.md`。
- **阶段三、四依赖 `claude -p` 可用**。09-04 实测报 `OAuth session expired`——**Andy 在交互终端跑 `claude login` 是前置**，未解则跳到阶段五再回来。
- 「退」永不删文件：第三方搬 `~/.claude/skills/_retired/`，自建 agent 搬 `.claude/agents/_retired/`。
- 每个脚本必须有「注入 bug 能红」的正面对照步。
- 定时任务书**不用 Write/Edit**，走 `update_scheduled_task`；云端 routine 走 `RemoteTrigger update`。
- 所有 commit 走「直推 main 标准动作」；尾行 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`。
- 描述优化器的问句必须是**真的**：从 Andy 的消息、Discord 语料、任务书里抄，含错别字与黑话；不许编「Format this data」这种。
- 每个 task 收工回复末行写 `skill-used:` / `skill-skipped:` / `skill-none:`——从计划执行的第一个 task 起就照 v2 的规矩做，别等 hook 上线。

## 文件版图

| 文件 | 职责 | 阶段 |
|---|---|---|
| `~/.claude/settings.json` `permissions.allow` | 定时任务也吃的允许规则 | 0 |
| `pipeline/tools/skill_inventory.py` + 测试 | 四个来源列全 | 1 |
| `data/research/ops/skill_audit_2026-09-04.md` | 79 件四档判定 + 冲突组 + 归档清单 | 1 |
| `docs/superpowers/verdicts.md` | 所有 skill 的裁决记录（第三方的不改原文件） | 1 |
| `.claude/skills/tearsheet/SKILL.md` | 从 commands/ 迁来 | 1 |
| `.claude/hooks/skill_stop_gate.py` + `pipeline/tests/test_skill_stop_gate.py` | hook 乙（Stop，留痕） | 2 |
| `.claude/hooks/task_test_gate.py` + `pipeline/tests/test_task_test_gate.py` | hook 丙（TaskCompleted，测试红不许标完成） | 2 |
| `.claude/settings.json` | 接线（入库） | 2 |
| `.claude/skills/fable-voice/SKILL.md` frontmatter · `.claude/skills/fable-voice/evals/trigger_eval.json` | 描述优化 | 3 |
| `.claude/skills/code-cartography/` (SKILL.md · evals/evals.json) + `code-cartography-workspace/` | 新 skill + 评估工作区（**workspace 一起入库，R25 推翻了原计划的 gitignore**：SKILL.md 的裁决记录引用 RED 报告路径，不入库＝悬空引用；with/without 六张答卷是「这个 skill 有用」的唯一证据） | 4 |
| `pipeline/tools/skill_health.py` + 测试 | 周检四个数 | 5 |

---

## 阶段〇 · 权限补根（先做，因为它在冻住班次）

### Task 0: 允许规则 + Andy 的两个手动步骤

**Files:**
- Modify: `~/.claude/settings.json`（用户级，不入库）

- [ ] **Step 1: 备份**

```bash
cp ~/.claude/settings.json ~/.claude/settings.json.bak-2026-09-04
```

- [ ] **Step 2: 用官方 skill 生成允许清单**

调 `Skill(skill="fewer-permission-prompts")`——它扫 transcript 里高频的只读 Bash / MCP 调用，产出一份优先级清单。它默认写进**项目级** `.claude/settings.json`；把产出的 `permissions.allow` 数组**同时**合并进用户级（官方原话：定时任务吃用户级 allow）。合并用脚本不手编：

```bash
python3 - <<'PY'
import json, pathlib
u = pathlib.Path.home()/".claude/settings.json"
p = pathlib.Path(".claude/settings.json")
ud = json.loads(u.read_text()); pd = json.loads(p.read_text()) if p.exists() else {}
allow = list(dict.fromkeys((ud.get("permissions") or {}).get("allow", []) + (pd.get("permissions") or {}).get("allow", [])))
ud.setdefault("permissions", {})["allow"] = allow
u.write_text(json.dumps(ud, ensure_ascii=False, indent=2))
print("用户级 allow 现在", len(allow), "条")
PY
```

- [ ] **Step 3: 校验 JSON**

Run: `jq -e '.permissions.allow | length' ~/.claude/settings.json`
Expected: 打出条数，exit 0。exit 5 = JSON 坏了，坏的 settings.json 会静默关掉整份设置——**先修再往下**。

- [ ] **Step 4: 两个只有 Andy 能做的动作（写进汇报，等他做）**

1. 桌面端 → 每个本机定时任务 → **Run now → 逐个 always allow**（官方唯一解法），或在编辑表单设权限模式。10 个任务。
2. 交互终端 `claude login`，解 `claude -p` 的 OAuth 过期。

- [ ] **Step 5: 验证**

Andy 做完后，选一个最容易卡的任务（joe-morning-check）Run now，看 History 面板是否出现 skipped/stalled。**没看到跑完不算通过。**

---

## 阶段一 · 审计

### Task 1: 清单脚本 `skill_inventory.py`

**Files:**
- Create: `pipeline/tools/skill_inventory.py`
- Test: `pipeline/tests/test_skill_inventory.py`

**Interfaces:**
- Produces: `inventory(roots: dict[str, Path]) -> list[dict]`，每项 `{"name","source","path","description","mtime"}`；`source ∈ {"project-skill","project-agent","project-command","user-skill","plugin-skill","scheduled-task"}`。CLI `python3 -m pipeline.tools.skill_inventory --json`。

- [ ] **Step 1: 写失败的测试**

```python
# pipeline/tests/test_skill_inventory.py
"""清单脚本：从四个来源把 skill/agent 列全。审计范围如果等于「我记得的地方」，
漏掉的就永远是孤儿。来源写死成参数，测试用临时目录造四种来源。"""
from pathlib import Path
from pipeline.tools.skill_inventory import inventory, read_description


def _mk(p: Path, body: str):
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(body)


def test_reads_description_from_frontmatter(tmp_path):
    f = tmp_path / "SKILL.md"
    _mk(f, "---\nname: x\ndescription: 做某件事\n---\n正文")
    assert read_description(f) == "做某件事"


def test_description_missing_is_empty_not_crash(tmp_path):
    f = tmp_path / "SKILL.md"
    _mk(f, "# 没有 frontmatter\n")
    assert read_description(f) == ""


def test_inventory_covers_all_sources(tmp_path):
    _mk(tmp_path / "proj/skills/a/SKILL.md", "---\ndescription: A\n---")
    _mk(tmp_path / "proj/agents/b.md", "---\ndescription: B\n---")
    _mk(tmp_path / "proj/commands/c.md", "# c")
    _mk(tmp_path / "user/skills/d/SKILL.md", "---\ndescription: D\n---")
    _mk(tmp_path / "sched/e/SKILL.md", "---\ndescription: E\n---")
    rows = inventory({"project": tmp_path / "proj", "user-skills": tmp_path / "user/skills",
                      "scheduled": tmp_path / "sched"})
    names = {(r["source"], r["name"]) for r in rows}
    for k in [("project-skill", "a"), ("project-agent", "b"), ("project-command", "c"),
              ("user-skill", "d"), ("scheduled-task", "e")]:
        assert k in names
    assert all("mtime" in r and "path" in r for r in rows)


def test_retired_dirs_are_excluded(tmp_path):
    _mk(tmp_path / "user/skills/_retired/old/SKILL.md", "---\ndescription: old\n---")
    _mk(tmp_path / "user/skills/_gstack-command/SKILL.md", "---\ndescription: router\n---")
    _mk(tmp_path / "user/skills/live/SKILL.md", "---\ndescription: live\n---")
    rows = inventory({"project": tmp_path / "nope", "user-skills": tmp_path / "user/skills",
                      "scheduled": tmp_path / "nope"})
    assert [r["name"] for r in rows] == ["live"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. python3 -m pytest pipeline/tests/test_skill_inventory.py -q`
Expected: FAIL `ModuleNotFoundError`

- [ ] **Step 3: 写最小实现**

```python
# pipeline/tools/skill_inventory.py
"""把四个来源的 skill / agent / command / 定时任务书列成一张表。
审计范围如果等于「我记得的地方」，漏掉的就永远是孤儿——所以来源写死成参数。"""
from __future__ import annotations

import argparse
import json
import os
import re
from pathlib import Path
from typing import Dict, List

_FM = re.compile(r"^---\s*\n(.*?)\n---", re.S)


def read_description(path: Path) -> str:
    try:
        m = _FM.match(path.read_text(errors="replace"))
    except OSError:
        return ""
    if not m:
        return ""
    for line in m.group(1).splitlines():
        if line.strip().startswith("description:"):
            return line.split(":", 1)[1].strip().strip('"').strip("'")
    return ""


def _row(name: str, source: str, path: Path) -> Dict:
    return {"name": name, "source": source, "path": str(path),
            "description": read_description(path),
            "mtime": int(path.stat().st_mtime) if path.exists() else 0}


def _skill_dirs(root: Path) -> List[Path]:
    if not root.exists():
        return []
    return [d for d in sorted(root.iterdir())
            if d.is_dir() and not d.name.startswith("_") and (d / "SKILL.md").exists()]


def inventory(roots: Dict[str, Path]) -> List[Dict]:
    rows: List[Dict] = []
    proj = roots.get("project")
    if proj:
        for d in _skill_dirs(proj / "skills"):
            rows.append(_row(d.name, "project-skill", d / "SKILL.md"))
        ag = proj / "agents"
        if ag.exists():
            rows += [_row(f.stem, "project-agent", f) for f in sorted(ag.glob("*.md"))]
        cm = proj / "commands"
        if cm.exists():
            rows += [_row(f.stem, "project-command", f) for f in sorted(cm.glob("*.md"))]
    for key, src in (("user-skills", "user-skill"), ("plugin-skills", "plugin-skill"),
                     ("scheduled", "scheduled-task")):
        root = roots.get(key)
        if root:
            rows += [_row(d.name, src, d / "SKILL.md") for d in _skill_dirs(root)]
    return rows


DEFAULT_ROOTS = {
    "project": Path(".claude"),
    "user-skills": Path(os.path.expanduser("~/.claude/skills")),
    "plugin-skills": Path(os.path.expanduser(
        "~/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills")),
    "scheduled": Path(os.path.expanduser("~/.claude/scheduled-tasks")),
}


def main(argv=None) -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--json", action="store_true")
    a = ap.parse_args(argv)
    rows = inventory(DEFAULT_ROOTS)
    if a.json:
        print(json.dumps(rows, ensure_ascii=False, indent=1))
    else:
        for r in rows:
            print(f"{r['source']:<16}{r['name']:<36}{r['description'][:60]}")
        print(f"\n共 {len(rows)} 项")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: 跑测试确认通过** — Expected: 4 passed
- [ ] **Step 5: 正面对照** — 删 `not d.name.startswith("_")`，`test_retired_dirs_are_excluded` 必须红；还原。
- [ ] **Step 6: 真跑记总数** — `PYTHONPATH=. python3 -m pipeline.tools.skill_inventory | tail -1`，N 应在 75–95。
- [ ] **Step 7: Commit** — `git add pipeline/tools/skill_inventory.py pipeline/tests/test_skill_inventory.py`

---

### Task 2: 审计 fan-out → `skill_audit_2026-09-04.md`

**Files:**
- Create: `data/research/ops/skill_audit_2026-09-04.md`

- [ ] **Step 1: 生成清单** — `PYTHONPATH=. python3 -m pipeline.tools.skill_inventory --json > /tmp/skill_inventory.json`

- [ ] **Step 2: 每个 agent 收到的 prompt 原文**

```
你在给一个 skill 做四档处置审计。判据是官方的，不是我们发明的。

这一件：{name}（来源 {source}，路径 {path}）
description 原文：{description}

我们的情境清单（只能从这里对）：
  周检 · 收工 · 设计/方案/plan · 写代码前 · 改 frontend/ 前 · 发布前 · 查 bug ·
  写中文给 Andy · 数据管线夜跑 · 内容产线 · 交接 · 安全审计 · 抓网页 · 做图表/artifact

读它的 SKILL.md 全文，回答（JSON）：
  how_it_works: 一句话
  situation: 清单里的一项，或 "无"
  duplicates: 与哪些 skill 做同一件事（精确名，可空）
  description_quality: 按官方两条打分——(a) 第三人称、含"做什么"+"什么时候用"；(b) 有没有把我们的中文说法/黑话写进去。写 0/1/2
  verdict: 用 | 修 | 退 | 建
  reason: 一句话——
    用 = 有情境 且 description_quality=2（触发率留给阶段三实测）
    修 = 有情境 但 description_quality<2
    退 = 零调用 + 对不上情境，或与另一件重复且另一件更好
  owner: TEAM.md 线名，或 "OPS"
  archive_action: 退时写具体动作（搬目录 / 关 enabledPlugins 键名），否则 ""
不许写「可能有用」。gstack 那 56 个：只要 situation="无" 就一律 退，动作统一写「随 gstack 整套搬 _retired」。
```

- [ ] **Step 3: 写 Workflow 脚本并跑** — `pipeline(items, item => agent(prompt, {schema}))`，items 来自 `/tmp/skill_inventory.json`（≈80 项，同构批量）；汇总 agent 产出 Step 4。

- [ ] **Step 4: 汇总成文件** — 表按 verdict 分组；末尾三节：**冲突组**（必查：`review`(gstack) / `code-review` / `superpowers:requesting-code-review`；`context-save/restore` vs `HANDOFF.md`；`design-review` / `impeccable` / `frontend-design`；`health`/`retro` vs 周检）、**体系判定**（superpowers = 开发主线；gstack = 整套退，个别件想要再搬回）、**归档清单**。

- [ ] **Step 5: 抽查 5 件「退」** — 自己读 SKILL.md 确认；站不住的改「修」并记「抽查改判」。

- [ ] **Step 6: Commit**（`data/research/**` 白名单）

---

### Task 3: 归档 + tearsheet 迁移 + verdicts.md

**Files:**
- Create: `docs/superpowers/verdicts.md`
- Create: `.claude/skills/tearsheet/SKILL.md`；Delete: `.claude/commands/tearsheet.md`
- Modify: `~/.claude/skills/`、`.claude/agents/`、`~/.claude/settings.json` enabledPlugins

- [ ] **Step 1: 搬 gstack 整套**

```bash
mkdir -p ~/.claude/skills/_retired
cd ~/.claude/skills
for d in */; do grep -qs "gstack" "$d/SKILL.md" && mv "$d" _retired/; done
ls | wc -l   # Expected: 6（lieflat-charts openmaic podcast-to-md shuorenhua video-subtitles youtube-clipper）
```

- [ ] **Step 2: 自建 agent 按审计结果搬** — `git mv .claude/agents/<name>.md .claude/agents/_retired/`

- [ ] **Step 3: enabledPlugins 该关的关**（用脚本改，不手编）

```bash
python3 - <<'PY'
import json, pathlib
p = pathlib.Path.home()/".claude/settings.json"; d = json.loads(p.read_text())
for k in ["ui-ux-pro-max@ui-ux-pro-max-skill", "impeccable@impeccable", "claude-security@claude-plugins-official", "vercel@claude-plugins-official"]:
    if k in d.get("enabledPlugins", {}): d["enabledPlugins"][k] = False
p.write_text(json.dumps(d, ensure_ascii=False, indent=2)); print("done")
PY
```
（这四个是审计判「退」时的默认候选；以 Task 2 结果为准，结果说留的就不关。）

- [ ] **Step 4: tearsheet 迁 skills/**

```bash
mkdir -p .claude/skills/tearsheet
git mv .claude/commands/tearsheet.md .claude/skills/tearsheet/SKILL.md
```
打开文件，frontmatter 补 `name: tearsheet` 与第三人称 description（含「tearsheet」「一页纸」「个股页 AI 段」三种说法）。调用名 `/tearsheet` 不变。

- [ ] **Step 5: 写 verdicts.md**

```markdown
# Skill 裁决记录

*所有 skill 的裁决集中记这里（第三方的不改原文件；自建的在各自 SKILL.md 末尾也有一份）。格式：`### [日期] <批/划/改/退> · skill · 一句话`。*

### [2026-09-04] 批 · superpowers:brainstorming · 第一次真用，两问定架构
### [2026-09-04] 批 · superpowers:writing-plans · 产出 v1 计划；v1 后被官方对账推翻，非它的错
### [2026-09-04] 退 · gstack 全套（56 件） · 零调用、与周检/HANDOFF/code-review 重复；目录在 ~/.claude/skills/_retired/
```
加上审计里全部「退」的行。

- [ ] **Step 6: 核实归档没误伤** — `PYTHONPATH=. python3 -m pipeline.tools.skill_inventory | tail -1`，M = N − 退的件数。
- [ ] **Step 7: Commit** — `git add docs/superpowers/verdicts.md .claude/skills/tearsheet/SKILL.md .claude/agents/ .claude/commands/`

---

## 阶段二 · hook 乙 + 接线

### Task 4: `skill_stop_gate.py`（读 `last_assistant_message`）

**Files:**
- Create: `.claude/hooks/skill_stop_gate.py`
- Test: `pipeline/tests/test_skill_stop_gate.py`

**Interfaces:**
- Produces: `verdict(payload: dict) -> dict`；空 dict＝放行；`{"decision":"block","reason":...}`＝打回。

- [ ] **Step 1: 写失败的测试**

```python
# pipeline/tests/test_skill_stop_gate.py
"""hook 乙只查留痕不查对错。失败模式一律放行——卡死会话比漏一行贵。"""
import importlib.util
from pathlib import Path
import pytest

ROOT = Path(__file__).resolve().parents[2]


@pytest.fixture(scope="module")
def gate():
    p = ROOT / ".claude" / "hooks" / "skill_stop_gate.py"
    spec = importlib.util.spec_from_file_location("skill_stop_gate", p)
    m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
    return m


def test_blocks_when_no_receipt(gate):
    v = gate.verdict({"last_assistant_message": "干完了，收工。", "stop_hook_active": False})
    assert v.get("decision") == "block" and "skill-used" in v["reason"]


@pytest.mark.parametrize("line", [
    "skill-used: superpowers:brainstorming · 批",
    "skill-skipped: fable-voice · 本轮是英文",
    "skill-none: 本轮无适用 skill",
])
def test_any_receipt_passes(gate, line):
    assert gate.verdict({"last_assistant_message": f"收工。\n{line}", "stop_hook_active": False}) == {}


def test_stop_hook_active_passes(gate):
    """已经在补写循环里，再拦就是死锁。官方另有 8 次上限兜底，但别靠它。"""
    assert gate.verdict({"last_assistant_message": "没写", "stop_hook_active": True}) == {}


def test_missing_field_passes(gate):
    assert gate.verdict({}) == {}


def test_receipt_must_be_a_line_start_not_inside_code(gate):
    """正文里引用了规矩不算留痕；留痕是那一行本身。"""
    v = gate.verdict({"last_assistant_message": "规矩是要写 `skill-used:` 那一行，我下次写。", "stop_hook_active": False})
    assert v.get("decision") == "block"
```

- [ ] **Step 2: 跑测试确认失败** — FAIL（文件不存在）

- [ ] **Step 3: 写最小实现**

```python
#!/usr/bin/env python3
# .claude/hooks/skill_stop_gate.py
"""hook 乙：收工前查最后一条正文有没有留痕行。没有就打回。

查三种行之一（行首，允许缩进与列表符）：
    skill-used: <名> · <裁决/无>
    skill-skipped: <名> · <原因>
    skill-none: 本轮无适用 skill

不查用得对不对——那是裁决的事。只保证「用没用」被写下来：
这个项目栽过三次「闸对、测试对、没人调用」，缺的从来不是判断，是留痕。

放行规则（失败模式必须是放行）：stop_hook_active=true；字段缺失；任何异常。
官方 Stop 输入直接带 last_assistant_message，不用解析 transcript。
"""
from __future__ import annotations

import json
import re
import sys
from typing import Dict

MARK = re.compile(r"^\s*(?:[-*>]\s*)?`?skill-(used|skipped|none):", re.M)
REASON = ("收工前补一行（行首）：`skill-used: <名> · <裁决/无>`，"
          "或 `skill-skipped: <名> · <原因>`，或 `skill-none: 本轮无适用 skill`。")


def verdict(payload: Dict) -> Dict:
    try:
        if payload.get("stop_hook_active"):
            return {}
        text = payload.get("last_assistant_message")
        if not isinstance(text, str):
            return {}
        if MARK.search(text):
            return {}
        return {"decision": "block", "reason": REASON}
    except Exception:  # noqa: BLE001 -- 卡死会话比漏一行贵
        return {}


def main() -> None:
    try:
        payload = json.load(sys.stdin)
    except ValueError:
        payload = {}
    print(json.dumps(verdict(payload), ensure_ascii=False))


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 跑测试确认通过** — Expected: 7 passed
- [ ] **Step 5: 正面对照** — 把 `MARK` 改成 `re.compile(r".")`：`test_blocks_when_no_receipt` 与 `test_receipt_must_be_a_line_start...` 必须红；还原。再删 `stop_hook_active` 那两行：`test_stop_hook_active_passes` 必须红；还原。
- [ ] **Step 6: 管道实测** — `echo '{"last_assistant_message":"收工","stop_hook_active":false}' | python3 .claude/hooks/skill_stop_gate.py` → 应打出 block；`echo '{"last_assistant_message":"skill-none: 本轮无适用 skill","stop_hook_active":false}' | python3 .claude/hooks/skill_stop_gate.py` → 应打出 `{}`。
- [ ] **Step 7: Commit**

---


### Task 4b: hook 丙 `task_test_gate.py`（TaskCompleted：点名的测试红了不许标完成）

**Files:**
- Create: `.claude/hooks/task_test_gate.py`
- Test: `pipeline/tests/test_task_test_gate.py`

**Interfaces:**
- Produces: `select_tests(text: str) -> list[str]`（从任务文本抓 `pipeline/tests/test_*.py`，去重排序）；`verdict(payload: dict, runner) -> tuple[int, str]`（返回 exit code 与 stderr 文本；`runner(tests) -> (returncode, output)` 可注入）。CLI：stdin 读 JSON，红则 exit 2。

- [ ] **Step 1: 写失败的测试**

```python
# pipeline/tests/test_task_test_gate.py
"""hook 丙：任务标完成前跑它点名的测试。守的是「绿」，不是「先红后绿」——
后者 hook 量不到，靠 writing-plans 的独立步骤 + SDD reviewer + TDD skill 预加载。"""
import importlib.util
from pathlib import Path
import pytest

ROOT = Path(__file__).resolve().parents[2]


@pytest.fixture(scope="module")
def gate():
    p = ROOT / ".claude" / "hooks" / "task_test_gate.py"
    spec = importlib.util.spec_from_file_location("task_test_gate", p)
    m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
    return m


def test_select_tests_finds_named_files_dedup_sorted(gate):
    text = "Test: pipeline/tests/test_b.py 和 pipeline/tests/test_a.py，再提一次 pipeline/tests/test_b.py"
    assert gate.select_tests(text) == ["pipeline/tests/test_a.py", "pipeline/tests/test_b.py"]


def test_no_named_tests_passes_without_running(gate):
    calls = []
    code, err = gate.verdict({"task_subject": "写事故档", "task_description": "只改 md"},
                             runner=lambda t: calls.append(t) or (0, ""))
    assert code == 0 and calls == []


def test_red_tests_block_with_exit_2_and_tail(gate):
    out = "\n".join(f"line{i}" for i in range(40)) + "\nFAILED x::y"
    code, err = gate.verdict({"task_subject": "T", "task_description": "Test: pipeline/tests/test_x.py"},
                             runner=lambda t: (1, out))
    assert code == 2 and "FAILED x::y" in err and "line0" not in err   # 只带尾巴


def test_green_tests_pass(gate):
    code, _ = gate.verdict({"task_subject": "T", "task_description": "Test: pipeline/tests/test_x.py"},
                           runner=lambda t: (0, "1 passed"))
    assert code == 0


def test_runner_crash_does_not_block(gate):
    """跑不起来（pytest 缺、超时）不等于测试红；失败模式是放行并在 stderr 留话。"""
    def boom(t): raise RuntimeError("no pytest")
    code, err = gate.verdict({"task_subject": "T", "task_description": "Test: pipeline/tests/test_x.py"}, runner=boom)
    assert code == 0 and "no pytest" in err
```

- [ ] **Step 2: 跑测试确认失败** — FAIL（文件不存在）

- [ ] **Step 3: 写最小实现**

```python
#!/usr/bin/env python3
# .claude/hooks/task_test_gate.py
"""hook 丙：任务标完成前，跑它点名的测试；红了就不许标完成。

官方契约（hooks 参考页）：TaskCompleted 在 TaskUpdate 标 completed 时触发，
输入带 task_subject / task_description；阻止靠 **exit 2 + stderr**，
stderr 原文回给模型。官方示例就是「跑测试，不过 exit 2」——这里照抄，
只加一条：只跑任务文本里点名的测试文件。全套光收集就超两分钟，每次
标完成都全跑会把执行拖死。

它守的是「绿」，不是「先红后绿」。先写会红的测试那一步 hook 看不见——
只能看完成那一刻。别把它说成 TDD 闸。

失败模式：跑不起来（没 pytest、超时、抛异常）＝放行并留话，不＝红。
"""
from __future__ import annotations

import json
import re
import subprocess
import sys
from typing import Callable, Dict, List, Tuple

_TEST = re.compile(r"pipeline/tests/test_[\w\-]+\.py")
TAIL = 15
TIMEOUT_S = 540


def select_tests(text: str) -> List[str]:
    return sorted(set(_TEST.findall(text or "")))


def _run(tests: List[str]) -> Tuple[int, str]:
    r = subprocess.run([sys.executable, "-m", "pytest", "-q", *tests],
                       capture_output=True, text=True, timeout=TIMEOUT_S)
    return r.returncode, (r.stdout or "") + (r.stderr or "")


def verdict(payload: Dict, runner: Callable[[List[str]], Tuple[int, str]] = _run) -> Tuple[int, str]:
    text = f"{payload.get('task_subject', '')}\n{payload.get('task_description', '')}"
    tests = select_tests(text)
    if not tests:
        return 0, ""
    try:
        code, out = runner(tests)
    except Exception as e:  # noqa: BLE001 -- 跑不起来不等于红
        return 0, f"task_test_gate: 测试没跑起来（{e}），放行但请人工看一眼"
    if code == 0:
        return 0, ""
    tail = "\n".join(out.splitlines()[-TAIL:])
    return 2, f"测试没过，不能标完成：{payload.get('task_subject', '')}\n{tail}"


def main() -> None:
    try:
        payload = json.load(sys.stdin)
    except ValueError:
        payload = {}
    code, err = verdict(payload)
    if err:
        print(err, file=sys.stderr)
    sys.exit(code)


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 跑测试确认通过** — Expected: 5 passed
- [ ] **Step 5: 正面对照** — 把 `return 2, ...` 改成 `return 0, ...`：`test_red_tests_block_with_exit_2_and_tail` 必须红；还原。把 `if not tests: return 0, ""` 删掉：`test_no_named_tests_passes_without_running` 必须红；还原。
- [ ] **Step 6: 管道实测（用真 payload 形状）**

```bash
echo '{"task_subject":"T","task_description":"Test: pipeline/tests/test_skill_stop_gate.py"}' | PYTHONPATH=. python3 .claude/hooks/task_test_gate.py; echo "exit=$?"
```
Expected: `exit=0`（那个测试文件是绿的）。再故意点一个不存在的文件：`pipeline/tests/test_nope.py` → pytest 报错、exit=2、stderr 有尾巴。**两个方向都要看到。**
- [ ] **Step 7: Commit**

### Task 5: `.claude/settings.json` 接线 + 现场证明

**Files:**
- Create: `.claude/settings.json`

- [ ] **Step 1: 确认仓库里没有**（09-04 核实过）；有则合并 `hooks` 键，不覆盖。
- [ ] **Step 2: 写接线**

```json
{
  "hooks": {
    "Stop": [
      { "hooks": [ { "type": "command", "command": "python3 .claude/hooks/skill_stop_gate.py", "timeout": 10 } ] }
    ],
    "TaskCompleted": [
      { "hooks": [ { "type": "command", "command": "PYTHONPATH=. python3 .claude/hooks/task_test_gate.py", "timeout": 600 } ] }
    ]
  }
}
```

- [ ] **Step 3: 校验** — `jq -e '.hooks.Stop[0].hooks[0].command, .hooks.TaskCompleted[0].hooks[0].command' .claude/settings.json` exit 0。
- [ ] **Step 4a: 证明 hook 丙拦** — 在新会话里 `TaskCreate` 一个任务，描述写 `Test: pipeline/tests/test_nope.py`，然后 `TaskUpdate` 标 completed → 必须被拒且看到 stderr 尾巴；改成真实存在且绿的测试文件再标 → 通过。
- [ ] **Step 4b: 证明 hook 乙拦** — watcher 只热加载会话开始时已存在的 settings 目录，**大概率要重开会话**。新会话里发「随便回一句，别写 skill 留痕」→ 必须被打回并看到 REASON；再发「skill-none: 本轮无适用 skill」→ 放行。**两个方向都要看到。** 没拦→让 Andy 在交互终端跑 `/hooks` 重载。
- [ ] **Step 5: Commit 直推 main**（云端生效的前提）

---

### Task 6: 任务书收工节 + 哨兵试跑一天

- [ ] **Step 1**: 13 份本机任务书（名单：fable-ceo-brief growth-weekly-ledger joe-morning-check ops-console-refresh personal-cortisol-bath personal-cortisol-breathing personal-fitness-daily-push personal-fitness-weekly-review remind-3d-effects-direction steve-content-daily-push steve-content-weekly-batch steve-night-campaign zac-night-study）用 `update_scheduled_task` 在「收工」节末尾追加：

```
**技能留痕（hook 乙会查，没有不许收工）**：最终回复末行固定写一行——
用了 skill：`skill-used: <名> · <裁决/无>`；该用没用：`skill-skipped: <名> · <原因>`；无适用：`skill-none: 本轮无适用 skill`。
```
个人类三份写死 `skill-none`。

- [ ] **Step 2**: 云端 3 个活 routine（`trig_01QCuAfFpqtYivKM5bbHwEws` 哨兵、`trig_01CTvty5VqdqPg1cFLQdmkmJ` 产线、`trig_01RTGvUGRfr9Uvj3mYPBP3UP` 每日页）`RemoteTrigger get` 取原 prompt → 追加同段 → `update`。
- [ ] **Step 3**: 试跑一天——哨兵每小时跑，`list_runs` → `get_run_log` 看接线后第一班末行有没有 `skill-*:`。看不到＝云端没吃到 settings.json，记进 spec §五风险表。
- [ ] **Step 4**: verdicts.md 追一行「hook 乙 云端实测 <通/不通>」。Commit。

---

## 阶段三 · 触发率（依赖 `claude login`）

### Task 7: fable-voice 描述优化

**Files:**
- Modify: `.claude/skills/fable-voice/SKILL.md` frontmatter
- Create: `.claude/skills/fable-voice/evals/trigger_eval.json`

- [ ] **Step 1: 重写 description + 加 when_to_use**（第三人称；做什么 + 什么时候用；把 Andy 的说法写进去）

```yaml
---
name: fable-voice
description: OPS Fable 写中文给 Andy 之前必读的文风账——治翻译腔、冠词式「一个」、粗体节奏、「不是X是Y」成瘾、零比喻、出错零表情。凡是要用中文回 Andy、写晨报、写每日页文案、写汇报、写给他看的任何段落，先读这本再动笔；哪怕只是回一句话。
when_to_use: 触发词：中文回复、给 Andy 看、晨报、汇报、每日页、文案、翻译腔、太 ai、说人话、改中文、你的中文。不触发：写英文、写代码注释、写契约行/任务书/事故档这类板着写的公文（那些有各自格式）。
---
```

- [ ] **Step 2: 写 20 条真问句**（从 Andy 的消息、Discord 语料抄；这是样例，执行者按语料替换其中至少 10 条）

```json
[
  {"query": "帮我把这段晨报改得像人话一点，现在太翻译体了", "should_trigger": true},
  {"query": "这个artifact文案太ai slop了 改", "should_trigger": true},
  {"query": "用中文给我总结一下今晚数据哨兵干了什么", "should_trigger": true},
  {"query": "回andy：数据已经修好了，dashboard翻到9/3了", "should_trigger": true},
  {"query": "写一段每日页的开头，讲昨晚断更的事", "should_trigger": true},
  {"query": "你的中文又开始一个一个的了，改掉", "should_trigger": true},
  {"query": "把这条汇报压成三句话，中文", "should_trigger": true},
  {"query": "给我一句话结论加action plan，中文", "should_trigger": true},
  {"query": "这段话有没有ai味 帮我看看", "should_trigger": true},
  {"query": "早报的回执节怎么写才不像机器", "should_trigger": true},
  {"query": "write the commit message for the yfinance pin", "should_trigger": false},
  {"query": "在 DATA_CONTRACTS §七 追一条契约行给 ALEX", "should_trigger": false},
  {"query": "写一份事故档，格式照 incidents/ 里已有的", "should_trigger": false},
  {"query": "把 #001 创刊号的英文稿清一遍语法，别加东西", "should_trigger": false},
  {"query": "帮我写 Substack 的英文 letter 开头", "should_trigger": false},
  {"query": "给 code-cartography 写 SKILL.md 的 description", "should_trigger": false},
  {"query": "翻译这段英文文档成中文，逐句对照", "should_trigger": false},
  {"query": "这个 python 函数的 docstring 用中文写一下", "should_trigger": false},
  {"query": "Andy 说的「说人话」skill 在哪个目录", "should_trigger": false},
  {"query": "把任务书的收工节加一行留痕规则", "should_trigger": false}
]
```
存到 `.claude/skills/fable-voice/evals/trigger_eval.json`。

- [ ] **Step 3: 跑优化器**（从 skill-creator 目录跑；模型 id 用当前会话的）

```bash
cd ~/.claude/plugins/cache/anthropic-agent-skills/document-skills/b29e7cf65e5c/skills/skill-creator
python -m scripts.run_loop \
  --eval-set /Users/taolezhu/Documents/AI-Trading-System/.claude/skills/fable-voice/evals/trigger_eval.json \
  --skill-path /Users/taolezhu/Documents/AI-Trading-System/.claude/skills/fable-voice \
  --model claude-fable-5-1 --max-iterations 5 --verbose
```
Expected：每轮打出 train/test 触发率，结束给 `best_description`。**`claude -p` 没登录会在第一轮就报 OAuth——那就停在这，汇报里写「阶段三阻塞于 claude login」。**

- [ ] **Step 4: 应用 best_description**，在 SKILL.md「裁决记录」追一行 `### [日期] 改 · 描述优化器 · 测试集触发率 <before>→<after>`。
- [ ] **Step 5: Commit**

### Task 8: tearsheet 同样跑一遍（Task 3 迁完之后）
同 Task 7 的五步；问句围绕「tearsheet / 一页纸 / 个股页 AI 段 / bull bear / 给 NVDA 做个 tearsheet」；不触发的近似题：「看一下 NVDA 的 GEX」「个股页 Signal History 怎么算的」。

---

## 阶段四 · code-cartography（文档的 TDD）

### Task 9: RED——先看没有 skill 时 agent 怎么做

**Files:**
- Create: `code-cartography-workspace/iteration-0/baseline/report.md`（**入库，不 gitignore —— R25 推翻本行原计划**：裁决记录引用它的路径，扔掉就是悬空引用）

- [ ] **Step 1: 派一个不带任何 skill 的 subagent**，prompt 原文：

```
frontend/src/components/watchlist/WatchlistPage.jsx 有 1195 行，越写越乱，加个功能不知道改哪。
帮我整理一下，让以后加功能不用到处改。直接动手，把方案和改动给我。
```
限制：只能读仓库、不许改文件（观察它**想**怎么做）。

- [ ] **Step 2: 逐字记它的行为**——有没有先盘消费者？grep 了几种拼法？量了什么数？拆分建议带不带 file:line？有没有直接开始重写？找了什么借口（「这个组件很明显该拆成三块」「先重构再说」）？写进 `report.md`。**这份就是 skill 要堵的东西。**

### Task 10: GREEN——写 skill 堵 RED 里的失败

**Files:**
- Create: `.claude/skills/code-cartography/SKILL.md`
- Create: `.claude/skills/code-cartography/evals/evals.json`

- [ ] **Step 1: frontmatter（含 paths）**

```yaml
---
name: code-cartography
description: 给越写越乱的文件或目录先画地图再动刀——盘清谁在读它、量出行数与消费者、给出带 file:line 的拆分建议、落地时带正面对照。Andy 说「这里乱了」「加功能不知道改哪」「拆一下」「重构」「组件太大」「整理代码」时用；周检扫到文件超 800 行（py）或 300 行（jsx）时用；改 frontend/src/components/ 里任何东西之前用。产出地图与建议，不产出重写。
when_to_use: 乱、拆、重构、整理、组件太大、加功能改哪、file 太长、周检、消费者、谁在用这个字段。不触发：修一个明确的 bug、加一个明确的字段、纯样式改动。
paths:
  - "frontend/src/components/**"
  - "pipeline/screeners/run_all.py"
  - "pipeline/adapters/yfinance_adapter.py"
---
```

- [ ] **Step 2: 正文——四步，每步针对 RED 里记到的具体失败写**（不是泛泛的「要盘消费者」，是「RED 里它只 grep 了一种拼法就下结论，所以这里写死至少三种」）。结构照官方 best-practices 的 checklist 模式：

```markdown
# code-cartography — 先画地图，再动刀

复制这张单子进回复，做一项勾一项：
- [ ] 1 盘消费者（每个导出 ≥3 种拼法 grep，覆盖 pipeline/ frontend/src/ data/reference/）
- [ ] 2 量（行数 · 导出数 · 直接 fetch/useState 数 · props 透传数）
- [ ] 3 拆分建议（每条：搬什么 file:line → 搬到哪 → 谁受影响 → 怎么验没坏；先说「拆了省什么」）
- [ ] 4 落地带正面对照（既有测试绿 + 新加守边界测试 + 注入真 bug 红一次）

## 为什么不许直接动手
（写 RED 记到的那几条：它没盘消费者就说「明显该拆三块」；它 grep 一种拼法；它没量就说乱……逐条对应）

## 常见借口 → 现实
| 借口 | 现实 |
|---|---|
| 「这个组件很明显该拆」 | 明显是感觉，不是消费者清单。先盘。 |
| 「先重构再说」 | 没地图的重构＝把乱搬个地方。 |
（其余从 RED report 抄）

## 裁决记录
### [日期] 建 · RED 基线见 code-cartography-workspace/iteration-0/baseline/report.md
```

- [ ] **Step 3: 写 evals/evals.json**（2–3 条真任务，含 WatchlistPage 那条）

```json
{
  "skill_name": "code-cartography",
  "evals": [
    {"id": 1, "prompt": "frontend/src/components/watchlist/WatchlistPage.jsx 有 1195 行，加功能不知道改哪，整理一下", "expected_output": "带 file:line 的拆分建议，先有消费者清单和三个数，不直接重写", "files": []},
    {"id": 2, "prompt": "pipeline/screeners/run_all.py 太长了，能不能拆", "expected_output": "同上，且先说拆了省什么", "files": []},
    {"id": 3, "prompt": "给 ScreenerPage 加一个新列，改哪", "expected_output": "先画那个组件的地图再指位置；不产出重写", "files": []}
  ]
}
```

- [ ] **Step 4: 带/不带对照跑**——每条 eval 同一轮起两个 subagent（with_skill / without_skill），输出各存 `code-cartography-workspace/iteration-1/eval-<id>/{with_skill,without_skill}/`；断言至少三条：「有 ≥3 种拼法的 grep 证据」「有三个数」「建议带 file:line」。
- [ ] **Step 5: 聚合 + 给 Andy 看**

```bash
cd ~/.claude/plugins/cache/anthropic-agent-skills/document-skills/b29e7cf65e5c/skills/skill-creator
python -m scripts.aggregate_benchmark /Users/taolezhu/Documents/AI-Trading-System/code-cartography-workspace/iteration-1 --skill-name code-cartography
python eval-viewer/generate_review.py /Users/taolezhu/Documents/AI-Trading-System/code-cartography-workspace/iteration-1 --skill-name code-cartography --benchmark /Users/taolezhu/Documents/AI-Trading-System/code-cartography-workspace/iteration-1/benchmark.json --static /tmp/cc_review.html
```
把 `/tmp/cc_review.html` 给 Andy 点（或走 Artifact）。**with 的通过率不高于 without＝skill 没写对，回 Task 10 Step 2。**
- [ ] **Step 6: Commit**（SKILL.md + evals + workspace —— R25：RED 报告 / benchmark.json / scoring.md / 六张答卷一起入库，约 700 行 markdown）

---

## 阶段五 · 仪表与收口

### Task 11: `skill_health.py` 周检四个数

**Files:**
- Create: `pipeline/tools/skill_health.py`
- Test: `pipeline/tests/test_skill_health.py`

- [ ] **Step 1: 测试**

```python
# pipeline/tests/test_skill_health.py
from pathlib import Path
from pipeline.tools.skill_health import summarize


def _skill(root: Path, name: str, evals: bool, bench_deltas=None):
    """root 是 skills 目录（真实布局：<repo>/.claude/skills）。
    workspace 与 .claude/ 平级，即 <repo>/{name}-workspace。
    bench_deltas: dict[iteration_suffix] = delta，例如 {"1": 0.4} 或 {"2": 0.1, "10": 0.9}。
    """
    d = root / name; d.mkdir(parents=True)
    (d / "SKILL.md").write_text("---\nname: x\ndescription: y\n---\n")
    if evals:
        (d / "evals").mkdir(); (d / "evals" / "evals.json").write_text("{}")
    if bench_deltas:
        repo_root = root.parent.parent
        for suffix, delta in bench_deltas.items():
            w = repo_root / f"{name}-workspace" / f"iteration-{suffix}"
            w.mkdir(parents=True)
            (w / "benchmark.json").write_text('{"delta": {"pass_rate": %s}}' % delta)


def test_counts(tmp_path):
    skills = tmp_path / ".claude" / "skills"
    _skill(skills, "a", True, {"1": 0.4})
    _skill(skills, "b", False)
    s = summarize(skills)
    assert s["skills"] == 2 and s["with_evals"] == 1
    assert s["last_delta"] == {"a": 0.4}


def test_last_delta_picks_newest_iteration(tmp_path):
    skills = tmp_path / ".claude" / "skills"
    _skill(skills, "c", True, {"2": 0.1, "10": 0.9})
    s = summarize(skills)
    assert s["last_delta"].get("c") == 0.9
```

- [ ] **Step 2: 实现**

```python
# pipeline/tools/skill_health.py
"""周检的四个数里能在仓库里量的两个：有评估集的 skill 数、最近 benchmark 的 delta。
另两个从仓库外取（`skill-used:`/`skill-skipped:` 行数比、/plugin Not-used-recently），写进周检。
—— 原写「描述优化器触发率」，R24 作废：仪器有确定性缺陷，换成 hook 乙留痕的真读数。"""
from __future__ import annotations
import argparse, json
from pathlib import Path


def summarize(skills_root: Path) -> dict:
    # 约定：workspace 在仓库根，与 .claude/ 平级（不是 skills_root 的兄弟）。
    # skills_root 通常是 <repo>/.claude/skills，所以仓库根是 skills_root.parent.parent。
    repo_root = skills_root.parent.parent
    out = {"skills": 0, "with_evals": 0, "last_delta": {}}
    for d in sorted(p for p in skills_root.iterdir() if p.is_dir() and not p.name.startswith("_")):
        if not (d / "SKILL.md").exists():
            continue
        out["skills"] += 1
        if (d / "evals" / "evals.json").exists():
            out["with_evals"] += 1
        ws = repo_root / f"{d.name}-workspace"
        if ws.exists():
            its = sorted(
                ws.glob("iteration-*/benchmark.json"),
                key=lambda p: int(p.parent.name.removeprefix("iteration-")),
            )
            if its:
                try:
                    out["last_delta"][d.name] = json.loads(its[-1].read_text())["delta"]["pass_rate"]
                except (KeyError, ValueError):
                    pass
    return out


def main(argv=None) -> int:
    ap = argparse.ArgumentParser(); ap.add_argument("--root", default=".claude/skills"); ap.add_argument("--json", action="store_true")
    a = ap.parse_args(argv); s = summarize(Path(a.root))
    print(json.dumps(s, ensure_ascii=False) if a.json else
          f"自建 skill {s['skills']} · 有评估集 {s['with_evals']} · 最近 delta {s['last_delta']}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 3: 跑测试 → 通过；正面对照：把 `with_evals += 1` 删掉，测试必须红；还原。**
- [ ] **Step 4: 挂进周检任务书**（`update_scheduled_task`）：跑 `python3 -m pipeline.tools.skill_health`，加两句人填的：**过去 7 天各任务书回复里 `skill-used:` 与 `skill-skipped:` 的行数比**（R24 定的口径，替掉已作废的「描述优化器触发率」——那个仪器有确定性缺陷，Task 7/8 的 4/8 永久标「不可用作评分」）、`/plugin` Not-used-recently 条数。**有评估集的数连续两周不涨 → INBOX 拉 🔴。**
- [ ] **Step 5: Commit**

### Task 12: 收口
- [ ] HANDOFF.md 第四节加一行指向 `docs/superpowers/verdicts.md`；「最后核对」改当天。
- [ ] KNOWLEDGE.md SOP 表加两行：「技能操作系统 v2（官方机制）」→ spec v2；「先画地图再动刀」→ code-cartography。
- [ ] §七 契约行：上线通告（hook 乙拦留痕、任务书已加行、gstack 已退、优化器/评估回路怎么跑）。
- [ ] 零删除自检 + Commit。
- [ ] 请 Andy 首裁：一屏——四个数现状、归档了几件、code-cartography 带/不带对照的 benchmark、fable-voice 触发率前后。他每裁一条当场追进 verdicts.md——**这就是系统第一次真实迭代。**

---

## 自审（对照 spec v2）

| spec 条 | 任务 |
|---|---|
| §3.1 触发靠 description/when_to_use/paths + 优化器量触发率 | Task 7、8、10 Step 1 |
| §3.2 technique 走评估回路 / discipline 走压力场景 | Task 9–10（technique 全程）；fable-voice 的压力场景作为 Task 7 的后续（本轮先做触发率） |
| §3.3 hook 乙读 last_assistant_message | Task 4–6 |
| §3.3b hook 丙 TaskCompleted 测试闸（守绿不守红-先） | Task 4b、5 |
| §3.4 权限补根 | Task 0 |
| §3.5 四档判据（触发率/description_quality） | Task 2 |
| §3.6 首批住户 | Task 7（fable-voice）、8（tearsheet）、10（code-cartography） |
| §四 四个数 | Task 11 |
| §五 claude login 阻塞 | Global Constraints + Task 7 Step 3 |

占位符扫描：无。类型一致：`verdict()` 在 Task 4 定义、Task 5 Step 4 验证；`summarize()` 在 Task 11 一处定义。
