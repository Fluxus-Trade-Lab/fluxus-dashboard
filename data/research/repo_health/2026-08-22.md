# 仓库体检 2026-08-22（OPS Fable）

## 摘要
- 远端分支 32 条（不含 main）：**0 条**已合并进 main 可删；**11 条**超 14 天未动且未合并；其余 21 条活跃。
- wip(archive) 封存分支 8 条、`archive/*` 分支 3 条，均未合并——是候选清理对象，本次只报不删。
- 契约抽查 4 条未打勾行：§七 431/437 已在代码落实但文档未勾；§七 447（今日行，六条封存分支去留）仍未落实；§七 430 引用的 `Code.gs` 全仓搜不到，事实无法核实。
- 详见下两节，未删除/修改分支或数据文件之外的任何东西。

## 一、远端分支
已合并可删：无。

超 14 天未动且未合并（11 条）：
- `archive/capital-deployment-6b92509e` 23天 — capital deployment chart
- `archive/feature-dashboard-local` 123天 — 两轮代码审查修复
- `archive/feature-model-book-local` 143天 — 左表格加宽
- `claude/friendly-chaplygin-b46c13` 42天 — GEX 引擎实施计划
- `claude/hungry-kalam-6a4e27` 67天 — 权益曲线拆股修复 #3
- `feature/dashboard` 123天（同 archive/feature-dashboard-local）
- `feature/model-book` 143天（同 archive/feature-model-book-local）
- `marketing` 144天 — chore
- `pine-indicators` 144天 — chore
- `follow-traders` 15天 — trader-feed 骨架（never run）
- `gex-integrate` 15天 — 合并 main，22 处冲突已解

特别标出：wip(archive) 封存分支 **8 条**（均 08-22 生成）；`archive/*` 前缀 **3 条**（已含在上表）。
活跃分支（≤14天）**21 条**，多为今日/近日 `auto/night-*`、`claude/*`、`feat/*` 在制分支，略。

## 二、契约 §七/§八 盘点
| 行 | 状态 | 证据 |
|---|---|---|
| §七 431（✗/★不骑 `sync_all`） | 已落实（未勾） | `frontend/src/components/watchlist/shortlist/sync.js:65` 发独立 `shortlist_upsert`；`sync.test.js:53` 断言不含 `sync_all` |
| §七 437（sidecar 改 blocks+charts） | 已落实（未勾） | `data/output/library/offense_ep_mrna.json` 含 `blocks`/`charts` 键，非旧版裸图表映射 |
| §七 430（GAS 半已在仓库 `Code.gs`） | 无法核实 | 全仓 `find -iname "*.gs"` 零结果；GAS 项目可能本就不在本仓库,该引用需数据端自证 |
| §七 447（08-22 六条封存分支去留） | 未落实仍有效 | 对应实现（`staleness.py`/`_upsert_entry`/`test_no_offsession_rows.py`/`hookOrder.test.jsx`）均不在 main；6 条分支合并状态仍为「no」 |
