# 仓库体检 2026-08-24（OPS Fable）

## 摘要
- 发布计分：过去 7 天发帖 **0** 条，`posts.csv` 最后一行日期 **2026-08-06**（断更 18 天）；对照 NOW.md 本周主线「发布 5 件过关」——**关卡挂零，与主线严重不符**。
- ⚠️ 本机心跳：Zac（night_reports）08-24 新鲜 / Joe（audit_unpushed 相关 commit）08-23 新鲜 / **Steve（posts.csv 回填）自 08-06 无产出,18 天无痕**——TEAM.md 08-23 已记「posts.csv 断更无人负责」,本次核实缺口仍在。
- 远端分支 36 条（不含 main,较上周 32 条 +4,新增均为 `auto/night-*`）：0 条已合并可删；**13 条**超 14 天未动且未合并（较上周 11 条 +2）；其余 23 条活跃。wip(archive) 8 条、`archive/*` 3 条,与上周一致。
- 契约盘点：**本周无删行事故**,上周 4 条追踪项全部有迹可循；新增 2 条待办（两盏灯裁决、`rs_accel` 请求）仍生效。
- 只报告不删,未修改分支或本报告之外的任何数据文件。

## 一、发布计分
`data/content/posts.csv` 共 6 行数据,最新一行 2026-08-06（VOICE,Market God 段）。过去 7 天（08-17~08-24）**零发帖**。NOW.md 本周主线是「宣传：把已建好的东西说出去」,周关卡目标发布 5 件——当前进度 0/5。缺数据本身就是最响的警报：素材台/日推稿早已备好,回填单点断更是唯一阻塞。

## 二、本机心跳
| 心跳 | 依据 | 最后产出 | 距今 | 状态 |
|---|---|---|---|---|
| Zac（夜间组） | `data/research/night_reports/2026-08-24.md`(+`origin/auto/night-20260824-*`) | 08-24 | 0 天 | 正常 |
| Joe（可靠性巡检） | `pipeline/tools/audit_unpushed*` 最后一次改动 `a35eea81` | 08-23 | 1 天 | 正常 |
| Steve（发布回填） | `data/content/posts.csv` 最后行 | 08-06 | 18 天 | **⚠️ 超 3 天无痕** |

Steve 心跳缺失与发布计分是同一信号,不是两个问题；TEAM.md 已把 `data/content/` 判给 Marketing Steve（08-23 补,此前无主）,本条只做二次核实,不重开归属讨论。

## 三、远端分支堆积体检
已合并可删：**0 条**。

超 14 天未动且未合并（**13 条**,较上周 +2：`feat/gex-engine`、`claude/trusting-burnell-bde353` 本周新触线）：
- `marketing` 145d、`pine-indicators` 145d — chore
- `feature/model-book` 144d（= `archive/feature-model-book-local` 144d）— 左表格加宽
- `feature/dashboard` 124d（= `archive/feature-dashboard-local` 124d）— 两轮代码审查修复
- `claude/hungry-kalam-6a4e27` 68d — 权益曲线拆股修复
- `claude/friendly-chaplygin-b46c13` 43d — GEX 引擎实施计划
- `archive/capital-deployment-6b92509e` 24d — capital deployment chart
- `gex-integrate` 16d、`follow-traders` 16d(never run)
- `feat/gex-engine` 15d、`claude/trusting-burnell-bde353` 15d（新触线，SQN 样本修复）

`wip(archive)` 封存分支 **8 条**（均 08-22 生成）；`archive/*` 前缀 **3 条**（已含上表）。均与上周一致,无新增封存。
活跃分支（≤14 天）**23 条**,多为近日 `auto/night-*`、`claude/*`、`feat/*` 在制分支,略。

## 四、契约 §七/§八 盘点
一句话结论：本周无删行事故,上周 4 条追踪项全部有迹可循,「封存分支」项已从「未落实」推进为「五条裁决完毕」。

| 行摘要 | 分类 | 证据 |
|---|---|---|
| `✗/★` 不骑 `sync_all` | 已落实（未勾） | `frontend/src/components/watchlist/shortlist/sync.js:65` 发独立 `shortlist_upsert`；`sync.test.js:53` 断言不含 `sync_all` |
| sidecar 改 blocks+charts（offense_ep_mrna） | 已落实（未勾） | `data/output/library/offense_ep_mrna.json` 含 `blocks`/`charts`/`title` 键 |
| GAS 半已在仓库 `Code.gs`（已标 ✅） | 无法核实（同上周） | 全仓无 `.gs` 文件；`Fluxus_Brand/visual/2026-08-10_TSF_PAGE_MAP.md:154` 指向仓外路径,部署真伪本仓不可验 |
| 08-22 六条封存分支去留 → 本周「五条裁决完毕」 | 已落实（✅）+1 条仍未落 | `test_ohlc_store.py`/`test_stockbee_ratio.py` 已入 main；第 6 条前端 hookOrder 测试仍未入 main,行内已明注「仍待前端」,6=5+1 非漏行 |
| 两盏灯「变体幸存者」请风险线裁决 | 未落实仍有效 | waiver 到 08-30 未到期,`claims.jsonl` 记账中 |
| 08-24 请补 `groups_history.json` 的 `rs_accel` 数组 | 未落实仍有效 | 当前文件无 `rs_accel` 键,同日提出未及处理 |
| §八 Today's List 按步骤用 | 已落实 | `frontend/src/components/watchlist/WatchlistPage.jsx:80-115` 含步骤条+过滤逻辑 |

**删行事故核查**：无。上周 4 条（sync_all/sidecar/GAS/封存分支）本周均可查,GAS 状态维持同判,封存分支项数目吻合、非漏记。
