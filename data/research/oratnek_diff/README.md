# oratnek 同日扫描对照 —— 三天拟合(2026-08-11 / 08-13 / 08-14 收盘)

工具:`python -m pipeline.tools.oratnek_diff --asof <session> --his his_<session>.json`
(git 捞那天的 universe 快照 + yfinance bar 切到那天 → 完整富集/打分/分格;`universe_asof_*.json` 是缓存,`diff_*.txt` 是逐格报告)。
**日期规则**:他页面标的日期 = 盘前那天,bar 是前一交易日收盘。Aug 17 页 = 08-14 bar,Aug 14 页 = 08-13 bar,Aug 12 页 = 08-11 bar(用 RS 数字当探测器验证:按 08-11 算 55 点只错 2 处,按 08-12 算错 37 处)。

## 一、票旁数字:锁定
`rs_line_pctl_21` = RS 线(close/SPY)在自己最近 21 日的百分位。三天 162 个点 **159 中**;3 处差一档(OKTA 86/90、TENB 71/76、BB 57/62)= 差一根 bar 的量级(他盘前抓的 SPY 可能未定收盘)。

## 二、检测结果:三天汇总(闸 $1B + $20M 成交额)
| 格 | 他(3 天合计) | 我们 | 他的在我们里 |
|---|---|---|---|
| LL-HL 1st / 2nd / trend | 4 / 15 / 4 | 177 / 219 / 59 | 4 / 13 / 2 |
| PP today / PP 2+ | 1 / 34 | 67 / 323 | 1 / 23 |
| VCS | 7 | 36 | 2 |
| Momentum 97 | 63(其中 08-13 只显示 29/42) | 39 | 9 |
| 4% Bullish | 33 | 94 | 28 |
| Weekly 20%+ | 32 | 122 | 30 |
| **合计** | **193** | **1,136(5.9×)** | **112(58%)** |

事件层面基本一致(他有的我们 58% 有;差的多为闸/语境门/配方不同),差在**宽度**。

## 三、宽度差从哪来 —— 一条统一假设解释掉 60%
逐格贪心拟合,第一条规则几乎都是同一个量:**3 个月强势**(`perf_3m_pctile ≥ 0.85` ≈ `rs_3m ≥ 85`):
- LL-HL 2nd:一条 `perf_3m_pctile ≥ 0.88` 把我们多出的 206 只剔到 5 只,他 13 只全留
- PP 2+:剔 489/683;VCS:`perf_3m ≥ 38%` 剔 561/635;LL-HL 1st:`perf_1w_pctile ≥ .89` 剔 167/173(样本 4,过拟合风险)

把「他的池子 = 3M 强势(top 15%)」当**统一前提**套到所有格三天:我们从 1,136 → **476(2.5×)**,他的 112 只只丢 4 只。**他扫的不是全市场,是一张 3M 领先名单。**

剩下没解释的(2.5× 里的大头):
- **Momentum 97**:配方不同。他的名字**全部** RS 线 = 100(月内新高)且当天上涨,`perf_1w` 分位 0.69–0.99(不是 ≥.97);贪心第一条是 `rs_ibd ≥ 61`、再 `adr ≥ 3.7`、非医疗——但 RS=100 的候选 403 只,他 34 只,拟到 43 只多余为止。"97"更像"RS 1M ≥ 97"(即 =100)。
- **PP 2+**:3M 门后仍 55–70 vs 他 11–12;他的 FRPT 不满足我们的 trend_base(周线 WMA),所以他不用那道门;剩余差异未解。
- **VCS**:他的 5 个名字在我们尺上 55–64("developing"),不是最高分;共同点 `rs_3m ≥ 88`;BB 在 SMA50 下 → 他没有 SMA50 门。候选 26–31 只对他 2–3 只,反推不出。
- **Weekly 20%+ / 4% Bullish**:3M 门后 28–39 vs 他 17–27,他的几乎全在我们里(30/32、28/33),多余的是非医疗/量能类差异,已接近。

## 四、建议
1. Watchlist 加一个**池子开关**:「全池」vs「3M 领先(perf_3m_pctile ≥ 0.85)」——后者就是他的视图,数据端可以在 watchlist.json 里给每只票带 `top_3m` 布尔(同 rs_high 的做法,只探不筛)。
2. Momentum 97 / VCS / PP 2+ 他有我们不知道的第二道门,再攒几天;新截图直接 `oratnek_diff` 跑,五分钟一天。
