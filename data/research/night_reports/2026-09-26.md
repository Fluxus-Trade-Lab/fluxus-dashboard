# 夜间研究班晨报 · 2026-09-26（JST）· linda

ET now 2026-09-25 15:31（周五，盘中）| last completed session **2026-09-24**（周四）| 今天是交易日
任务 `T-0926-21` · 窗口 04:31 JST（在 04:00–10:00 内）· 时间盒 150 分钟

> 昨夜（`T-0925-11`）把 `audit_archives` 57%→97%、`audit_ledger` 66%→98%，
> 收尾写明下轮第一件事：**量 `audit_ci_test_coverage`（162 个变异点，全库最大，从没量过）**，
> 三段切片 `0:55` / `55:110` / `110:162`，并留了一句叮嘱——
> **「全库最低」在它量出来之前只是「量过的里面最低」**。今晚照办。

## 一、回执

| 来源 | 内容 | 状态 |
|---|---|---|
| 任务板 `list --owner linda` | 名下只有本单 `T-0926-21`（claimed），无其他 open | 已核 |
| INBOX 🔗 收藏夹 | 最后一条 09-12 已 ✅，**无新条目** | 不适用，跳过 1.5 节 |
| INBOX 📌 给 Andy 的待办 | 本线名下无未核销条目（09-17 GEX 退役那条已 ✅ 核销） | 已核 |
| 09-25 晨报「下轮第一件事」 | 量 `audit_ci_test_coverage`，三段切片 | 本班执行中 |
| 上一轮 `needs-andy` / `handoff` | 无 needs-andy；`T-0925-12` → ops（三棵未推树）已转出 | 已转交 |

**已认领**：无新挂单指名本线。

## 二、进行中

- **今晚的题**：`audit_ci_test_coverage` 的变异杀死率——全库最大的一道闸（720 行源码 / 641 行测试），从没量过。
- ⚠️ 第一步同样不是补测试，是**量**：162 这个数也是转抄来的（09-25 晨报「没动的大件」栏），
  按昨夜两次复算的教训，先现场 `--list-sites` 确认站点数，再切片跑。
- ✅ 做完了，但第一步撞上的不是那道闸，是量它的那把尺子。详见第三节。

## 三、今晚做了什么

**一句话**：全库最大那道闸之所以「从没量过」，不是没人排到它——
**是尺子自己搭的沙箱少拷了它要读的那个目录，于是每次都报「这道闸的测试坏了」**；
补上之后第一次读数 126/174 = 72%，收尾 154/165 = 93%。

研究档（可复跑命令、逐个存活点的判词）：[`data/research/audit_mutation_2026-09-26.md`](../audit_mutation_2026-09-26.md)

### ① 尺子的盲区：`audit_mutation_sweep` 的沙箱没有 `tests/`

`--module audit_ci_test_coverage` 一直返回 `baseline is already red; refusing to sweep`。
真树上这道闸的测试是 **73 passed**。红的是沙箱——它拷 `pipeline/`、软链 `data/`、拷 `.github/`，
**独独没有 `tests/`**，而这道闸的全部主题正是「仓库有第二个测试根、CI 不跑它」，
它的 `DECLARED` 表里就声明着 `tests` 这个路径。于是它自己的 T3（「声明的路径已不存在」）当场亮红。

**闸是对的，沙箱是缺的。** 同形第二次——09-21 那次缺的是 `.github/`，受害者是
`audit_schedule_windows`（整个工作就是读 workflow 的 cron）。两次之后把规矩写进注释：
**凡是一道闸要读的、长在 `pipeline/` 外面的东西，沙箱必须有。**

阳性对照做过：撤回修复，新测试报的是**同一句**错；加回，绿。
**波及面量过：只这一道。** 20 道有测试的 `audit_*` 用新旧沙箱各跑一次 baseline，其余 19 道两边都绿。

### ② 第一次读数 126/174 = 72%

站点数不是昨夜转抄的 162，是 **174**（09-23 扩 `DECLARED_TRIGGERS` 之后那个数就过期了）。
48 个存活点挤在三处：`-m` 标记解析 16 个（**这道闸的核心输入**）、`render()` 13 个
（这个工具唯一的产物，只有两条测试）、`main()` 退出码 2 个。

⚠️ **`return 0 if 干净` 那一个是同形第三次**——它在 `audit_archives` 和 `audit_ledger` 里各活了三周，
09-25 刚一起补掉。它坏的是**安静那条路**：每个干净的夜晚 CI 都 exit 1，仓库没毛病，
而人学会的修法是不再看这个检查。按三次律，判据已写进新测试的正文，下一道闸照抄。

### ③ 补 26 条测试 → 88%，再删一个跑不到的函数 → 93%

剩下的 20 个存活点里，**9 个长在 `_marker_expr()` 上——全仓零调用**，
`parse_pytest_args` 后来在自己肚子里长了一份同样的实现。

> **变异存活清单分不出「没测到」和「跑不到」。** 存活点密集挤在一个小函数上，
> 先当成第二种——**把死代码钉住，正是让它看起来不像死代码的办法。**

删掉（不补测试），站点数 174 → 165，正好少 9 个。四段收尾读数 95% / **100%** / 90% / 87%。

剩 11 个逐个判过：6 个实跑验为等价或只改帮助文本，2 个造不出能分家的真实 YAML，
**3 个是真洞没补**（`pytestmark` / `skipif` 的 ast 读取），时间盒到了，留给下一班。

## 四、收尾

**测试**：`pipeline/tests` + `tests` 两根一起跑，**3642 passed / 1 skipped / 12 deselected**，全绿（6 分 35 秒）。

**已合的 commit**：

| commit | 内容 | gate |
|---|---|---|
| `3141184c` | 本晨报开工 | none（已在 main） |

**待合分支**：`agent/linda/T-0926-21`（`8999d3f1` 沙箱修复 + `a81cab42` 测试与死代码删除 + 收尾档）。
gate 判 **reviewer**——改的是 `pipeline/tools/audit_*`，T-0921-115 把改闸从自合白名单里收走了，
且这轮**删了 main 上的可执行行**（死函数 `_marker_expr` 六行），按宪法 safe-merge 判据必须复核。
删的是什么、为什么，写在删除处的注释里和研究档第四节。

**收藏夹判定清单**：INBOX 🔗 节最后一条是 09-12，已 ✅，**本轮无新条目**，不适用。

**预览稿分数表**：本夜未做 UI 预览稿（2.5 节）。时间给了上面这件——
尺子的盲区是先决条件，不修它今晚的题根本量不出数，且它一修就把全库的可测范围补齐了。

**素材箱**：已追一行。

**只差 Andy 一步的交付**：**无。**（故 `needs-andy` 与 INBOX『📌 给 Andy 的待办』两处均不适用。）

**没动的大件**：`audit_calendar_gaps` 61% / `audit_universe_shape` 63%（09-02 读数，**先复算再动手**）·
`DATA_RELIABILITY.md` §六第 6 条那四条欠账（都要动 `.github/workflows/tests.yml`，不在本线边界）。

**下轮第一件事**：补 `audit_ci_test_coverage` 剩的 3 个真洞（`pytestmark` / `skipif` 的 ast 读取，
现行行号 L501 / L506 两处），外加 `render()` 那个「恰好 4 条」的边界（L699）。
然后复算 `audit_calendar_gaps` 与 `audit_universe_shape`——
⚠️ 那两个数同样是 09-02 的读数，**而今晚第三次证明了转抄的读数会过期**：
昨夜是数字错（53% 实为 66%/57%），今晚是站点数错（162 实为 174）。
量一次的成本是 5 分钟一段，比选错一晚的题便宜。
