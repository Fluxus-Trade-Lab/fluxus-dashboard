# 夜间自学 2026-08-21（JST）

ET now 2026-08-20 15:36 | last completed session **2026-08-19** | today is trading day True
分支 `auto/night-20260821-96c7df`（工作树开在本会话 scratchpad，未碰共享树，未触发 cron）

---

## 一、做了什么

### 1. run_all 端到端 smoke —— `DATA_RELIABILITY` §六.0，最大盲区已封
commit `c6fbe19c` · [pipeline/tests/test_run_all_smoke.py](pipeline/tests/test_run_all_smoke.py)

编排器 `main()` 650 行、零测试。它每一段都套自己的 try/except（对的设计——一个 vendor 坏掉不该赔掉整夜），代价是**一段可以静默失败**：08-19 一个 `else:` 被重绑到插进来的 `if`，breadth.json 整夜没有 regime/state_board/verdict/conditions，没有任何东西报错。

所以这个 smoke 不断言「跑不崩」，断言两件事：

| 断言 | 抓什么 |
|---|---|
| `REQUIRED_BLOCKS` 里每个文件都写出来**且块齐全** | 08-19 那类：文件在，块没了 |
| **没有任何一段把异常吞进 log** | try/except 把 traceback 变成一行 log，所以 log 就是失败通道 |

**两个变异都验过会红**（这是它值不值钱的唯一证据）：
- 删掉 `breadth_result['state_board'] = board` → `AssertionError: {'breadth.json': ['state_board']}`，正是 08-19 的签名
- 让 rotation 段抛异常 → `stage(s) failed silently inside their try/except: Rotation layer failed`

规格：152 行夹具宇宙、**全部网络调用打桩**、三条测试共跑一次 `main()`、**25 s**。价格序列是确定性合成走势，锚在夹具自己的收盘价上——**没有任何断言读市场数字**，主语是控制流和写出来的形状。

夹具 [pipeline/tools/make_smoke_fixture.py](pipeline/tools/make_smoke_fixture.py) 从 universe.json 切，**剥掉 `compute_universe_scores` 会算的列**，否则测试断言的是它根本没算过的数。

> 过程里的一个坑，值得记：我第一版把 `vcs` 也当成「算出来的」剥掉了——因为 grep 到了 `df['vcs'] =`。那行是**强制 dtype**，不是计算；`vcs` 是 adapter 供的。结果质量闸对着一个 100% 缺失的列判 severe、`SystemExit`。**是夹具错了，不是管线错了。** 按名字 grep 认不出「赋值」和「转换类型」的区别。

**仍没覆盖**（写进 `DATA_RELIABILITY` 了）：各段自己的算法正确性、真实 vendor 返回形状漂移（打桩挡住了）、`quality` 基线随夜变动导致的 severe 判定。

### 2. `data/output/library/index.json` —— 契约 §七 那条
commit `1a0bf3d2` · [pipeline/content/library_index.py](pipeline/content/library_index.py) · 8+1 条单测

浏览器没法列目录，所以没有它前端只能读编译进 bundle 的名单：**每加一篇文章都要发一版才看得见**，而且发版前页面说「1 篇」而实际有两篇。接在 run_all 的 site quality 之前，自己一个失败域，每晚重生成——所以它不会落在新增文章后面。

**两点和前端原文不同，请他核**（已写进契约同一行）：
1. 值是 **`.json` 文件名**，不是 `.md`。`useLibrary.js` 是 `fetch` 完 `r.json()`，`.md` 没人渲染——**以跑着的代码为准**。
2. 分组读**文章自己的 `page` 字段**，不是文件名前缀。`portfolio-management` 带连字符，按下划线拆前缀分不出它和一个叫 `portfolio` 的页。`page` 缺失或不认识的文件**只报不猜**，不塞进任何一桶。五个页 key 永远都在，空的是 `[]`。

> **新 smoke 上线十分钟就回本了**：把这一段接进 run_all，`test_no_stage_swallowed_an_exception` 立刻变红——`write()` 在没有 library 目录的检出上抛异常，而编排器的 try/except 会把它咽成一行 log。`mkdir(parents=True)` 修掉 + 补测。这正是它该抓的东西。

**验证**：`745 passed, 12 deselected`（`--deselect test_content_processor.py`），且跑完**工作树干净**（见下条）。

### 3. 一个测试在往生产的质量基线里写假数据
commit 见下 · [pipeline/tests/test_quality.py:305](pipeline/tests/test_quality.py:305)

跑完全套测试后工作树是脏的，`M data/history/quality/breadth_last.csv`。二分下去是 `test_quality.py` 的 `TestRequiredBlocks::test_missing_block_grades_severe`：

```
rep = check_site(tmp_path, "2026-08-19")        # 只传了两个参数
```

`check_site(output_dir, date, history_dir=QUALITY_DIR)` 的第三个参数**默认是真实的 `data/history/quality`**。所以这个测试每跑一次，就拿它那个只有两个字段的假 breadth.json，往**决定 severe、决定要不要中止夜跑**的那条基线里追一行 2026-08-19。补上 `tmp_path / "hist"` 就干净了。

会话开始时共享树上那个 `M data/history/quality/breadth_last.csv` 大概率就是这么来的——**谁在本地跑过一次测试，谁就污染一次基线**。修完再跑全套，工作树干净。

> 顺带：`tmp_path` 用得越顺手，越容易漏掉这种「第三个参数有默认值」的。前两个参数都指向 tmp 会让人以为整个调用都是隔离的。

---

## 二、请 Andy 决定

1. **smoke 要不要进 CI / nightly 前置闸？** 依据：25 s，只读夹具不碰网络；08-19 那类事故它能抓，而现在没有任何东西抓。我没有自作主张改 workflow。
2. **`shortlist.json` 的 `seat.empty_reason` 我停在闸前了。** 契约 §七 里前端要 `not_measured|none_found|all_excluded` + `excluded_n`，理由充分（三种空现在长成同一个灰框，和「`regime.score` 为 null 显示不可测」是同一条规矩）。但任务书写着 **Short List schema 冻结**，加字段是改结构 —— **要不要解冻这一条**，你说了算。
3. **`data/output/library/index.json` 今晚不在提交里**（`data/output/` 在我的禁改清单）。它由下一次 cron 自己写出来。如果你想立刻看到，本地跑 `python3 -m pipeline.content.library_index`。

---

## 三、网页优化（§2.5）：**这一轮没做，如实报**

没出预览稿，也没有评分表。原因两条，都不是「没空位」：

1. **时间盒不够做一轮真的。** 要求是 3 个**方向不同**的变体 + 逐条 0–2 分、注明每一分扣在哪 + 至少 2 轮迭代。剩下的时间只够产出一张看起来像评分表的东西，而评分表最怕的就是分数落不到具体的扣分处。**填一张凑数的比不做更糟。**
2. **我原本瞄准的那个空位已经不存在了。** 记忆里 [todo_watchlist_ui_fields](todo_watchlist_ui_fields.md) 记着「ATR 徽章负值会全绿（配色缺负分支）」。去读代码，`atrFill` 现在第一行就是 `if (v == null || !Number.isFinite(v) || v < 0) return null` —— 负值根本不填色，`atrTitle` 也有专门的「在 50 日线下方，不在扩张刻度上」分支。**这条记忆过期了，别再照它派活。**

下一轮想接着做，已经踩好的点：Library 那五个空页是**从零建**不是改版（而且你还卖着关子），`screener` / `watchlist` / `review` 是安全的练手场。

---

## 四、积压里仍没动的大件

- `DATA_RELIABILITY` §六.1/2/3：I4 阈值（0.3×/3×）还是拍的、universe 行数 vs Finviz 宣称总数、run_ledger 月报 —— 三条都在**等样本攒够**，不是等人做。
- 契约 §七 未动：`shortlist_log.csv` 缺分母（每席每天一行）、`✗` 三义合一、GAS `shortlist_upsert` 那一半、`readings.hi20` 六卡全 null（是没接上还是故意留空，**这个只有你能答**）、`offense_ep_mrna.json` 的两张配图。**前四条都撞 Short List 冻结**。
- 研究复盘：Delayed EP `--review`（今天起够 3 天了）、TML/leaders_log 前瞻、oratnek Momentum 97 shadow 对比、regime shock_days 台账补行 —— 今晚一条没碰，两件可靠性活吃满了时间盒。
