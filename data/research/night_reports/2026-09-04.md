# 夜间组晨报 · 2026-09-04（JST）

> ET now 2026-09-03 15:32 · last completed session **2026-09-02** · 今天是交易日
> 分支 `auto/night-20260904-77e7e8` · 开工 04:32 JST

## 一、回执

- **✅ 昨夜「待合 `auto/night-20260903-5cea87` / nodowngrade，建议 y」→ 已落地。**
  main 上 `89ba7d94`「gate(no_downgrade): 待合包」含接线 + 三条接线断言 + §七 §十五。
  现场核实：`run_all.py` 916–925 行接线在；`pipeline/tests/test_no_downgrade_is_wired.py` 在 main 上；
  `DATA_CONTRACTS.md` §十五 在 main 第 1039 行。
  该分支相对 main 的 diff **只剩 4 个文件、全是删除行**（＝分支落后），**已无遗留，可删**。
  → 08-31 起的三天空窗**已闭合**。
- **✅ 收藏夹**：🔗 节零条未处理（八条全 ✅），今晚无收藏活。
- **已认领**：我自己 09-03 挂的 ④「五道闸里三道没有自动触发点」——今晚做，见 §二。

## 二、⚠️ 置顶：一件需要 Andy 或 OPS 拍板的事（一句话就能做完）

**各定时班次的只读命令没进权限 allowlist，于是无人值守的班次卡在「允许」弹窗上，四天冻掉 27 小时。**
日志原话 `Not auto-approving "Bash" … no suggestions on request` —— **机制在，名单不在。**
建议加：`git show|log|fetch|diff` · `python3 -m pytest` · `python3 -m pipeline.tools.audit_*`。
⚠️ settings.json / 定时任务配置不属夜间组边界，**我只量不动**。

## 三、做了三件

### ① ⭐ 「那一班起来了」—— Steve 和 Joe 争了两天的机制辩论，前提是错的

`steve-night-campaign` 09-03 **不是**没被触发。它 05:36:02 起来，五秒后有 assistant turn，
39 秒后发出一条工具调用，然后**在权限弹窗上冻了 5 小时 2 分**，10:37 拿到结果，活到 13:32。
卡住它的是一条**只读**命令：`git show origin/main:…/roles/01_signal.md`。

> **写下「05:30 那个槽位空过去了」的那个会话，自己就是那一班。**
> 被冻醒的会话分不出自己被冻过——上下文看起来和冷启动一模一样。

**冻结时长实测**（全量日志 08-16→09-04，剔除 idle tick 后量；两个独立方法互证：
日志 301 分 vs git 侧 302分37秒）：

| 班次 | 日期 | 冻结 |
|---|---|---:|
| `steve-night-campaign` | 08-31 / 09-01 / 09-02 / 09-03 | 436 / 514 / 384 / **301** 分 |
| `zac-night-study`（本线） | 08-23 | 426 分 |
| `ops-console-refresh` | 08-28 / 08-30 | 106 / 148 分 |
| `fable-ceo-brief` | 08-25 / 09-02 / 09-03 | 175 / 112 / 26 分 |
| `mrna-promo-tweet-reminder` | 08-25 | **8,154 分（5.7 天）** |

**这不是一条线的事——本线自己也丢过 426 分钟。**
事故档 [`2026-09-04_shifts_freeze_on_tool_permission_prompts.md`](../../reference/incidents/2026-09-04_shifts_freeze_on_tool_permission_prompts.md)
（**是 09-02 那份的续篇，不是第二份**）· commit `d40a5cb3`

**⭐ 不用新建心跳**：`lastScheduledFor`（槽位）· `recordedSkips`（该跑没跑 + 原因，
另两任务共 1,073 条 `global_limit`）· `main.log` 的 `Confirmed task run for` —— 三者早就在，只是没人读。

**独立验证**：本结论走了 3 个不同视角的 verifier（宪法「研究结论走 fan-out」）。
**它们推翻了我自己一条推论**，见 §五。

### ② `audit_wiring` —— 问「有没有东西会跑我」，不问「我对不对」

8 个闸只有 **2 个**有自动触发点（`audit_archives` / `audit_ledger`）；
**6 个 workflow 无一跑 pytest**、无一声明 `on: push`。

做成**棘轮不是警报**（永久红的闸 = 没有闸）：已知未接线集带 owner+日期+理由，对该集为绿；
新增无人调用的闸(W1) / 修好了没删豁免(W2) / 条目指向不存在的模块(W3) / 缺 owner(W4) 才红。

**不用 grep 判调用**——workflow 里提到 `audit_archives` 的 4 处有 2 处是注释。改用 YAML 解析
`run` 块（剥注释）+ `ast` 判「导入的名字有没有真的被调用」。后者抓到的第一个东西是**我自己散文里的错**：
`no_downgrade.py:85` 导入 `STATUS_RANK` 只做 `.get()` 查表，我的首版把它误报成已接线。

**阳性对照**：5 条注射各打红一条**不同**的断言，无一条是装饰。
`pipeline/tools/audit_wiring.py` + 12 条测试 · commit `eb6f979d` · 跑法 `python3 -m pipeline.tools.audit_wiring`

### ③ I4 阈值校准（RELIABILITY §六.1，挂了 13 天）—— 结论是「不用改阈值」

全历史重放 **153 次检查，0 次触发**。两侧余量完全不对称：下限 0.30× 离最小值 0.49 很远，
上限 3.00× 离最大值 2.74 **只剩 9%**——所以「收紧带子」是两个方向相反的动作，且收上限不安全。

**但真正的结论不是改数**。在真数据上量 06-26 那次断层（半个股票池消失、连续 21 场）：

```
当日 1613 行 / trailing-20 中位 1066  →  ratio 1.51  →  I4 不响
同期首字母>L 占比：0.440 → 0.000
```

> **半个池子没了，而行数涨了 67%。在计数维度上，这一场比它前面每一场都更健康。**

所以 I4 的 0/153 不是好成绩单，是**零误报 + 零已证实检出**。
看得见 06-26 的是 `audit_universe_shape`，而 ②测到它**零自动触发点**。
[`data/research/i4_calibration_2026-09/results.md`](../i4_calibration_2026-09/results.md) · commit `832a6663`

## 四、safe-merge：本夜全部路径在白名单内，已自合

（见文末「已合 commit」）

## 五、⚠️ 本轮我自己错的两条（verifier 推翻的）

- **C4 错**：我从「`lastRunAt` 没被 10:38 推进」推出「10:38 的产出不是这次触发」。
  **`lastRunAt` 是启动戳，续跑不推进它**——「没推进」只说明「没有**新的**启动」。
  10:38 就是 05:36 那次触发解冻后的自己。
- **C2 数错**：「四个任务差 3–9 分钟」实为**三个**，第四个 11 分 50 秒，另两个不产 commit 无法这样测。
- **C1 需重述**：git 侧「无人读 `lastRunAt`」成立（全历史 `-S` 扫 30 余种拼法零命中），
  但 `fable-ceo-brief/SKILL.md:59` 早两天就写着「lastRunAt 对照仓库痕迹」。
  正确表述：**它只有一个读者，且是散文指令、无代码执行、无闸检查。**

## 六、门铃待按（本线只列不按）

1. **→ OPS Fable / Andy**：只读命令进权限 allowlist（见 §二）。若 08-31 就做，仅 Steve 一线收回 27 小时。
2. **→ DATA ALEX / Andy**：三道闸零自动触发点，接线要动 `.github/workflows/`；
   同一文件还欠一个**跑 pytest 的 workflow**（§六.4 那条至今没有落点）。
   ⚠️ 若建，checkout 必须 `fetch-depth: 0`——`test_audit_regression_gate` 有 4 条用例在
   shallow checkout 下**静默跳过**（绿得不是地方）。

## 七、其它

- **收藏夹**：🔗 节零条未处理，今晚无收藏活。
- **UI 预览稿**：本夜未做——三件都在同一条「可观测性」线上，按「宁可 3 件做透」放弃第 4 条。
  且 `feat/rotation-v3` 昨夜到今晨 03:19 仍在活跃改动，避免撞车。
- **测试**：全套 `1350 passed, 1 skipped`（含本夜新增 12 条）。
- ⏰ **Andy 昨夜干到 03:59 JST**（我 04:32 开工前 33 分钟）。按温水澡机制提醒一次：该睡了。

## 八、收工三问

**① 这轮踩了什么坑？**
量停摆时长时，我第一版量的是「日志相邻两行的最大间隔」，得到一排整齐的「15 分」——
那是 idle tick 的**周期**，不是停摆的**长度**。**一个每 15 分钟写一行的进程，
在「最大间隔」这个指标下永远不会超过 15 分钟。** 填充物让缺口在我选的那个维度上消失了
（同族：`pitfall_row_count_is_not_a_shape_check`）。已写进 memory。
第二个坑更值钱：我在 `audit_wiring` 的 docstring 里断言它能分开「导入常量」和「调用模块」，
**写完才发现首版做不到**——散文先于行为。它被自己的 W2 断言当场抓住。

**② 哪条规矩帮了 / 碍了？**
帮了两条。「研究结论走 fan-out 独立验证」**当场救了我**——我本来要把 C4 那条错推论写进事故档。
「没验证过能报阳就不该信它的阴性」在今夜用了三次，三次都抓到东西（audit_wiring 的 5 条注射、
公箱删除自检的阳性对照复现了旧写法的假安全、I4 的 0/153 被识别为零检出而不是好成绩）。
**修订建议**（只提不写）：三次律该收口的不是「再加一条判据」，而是把
**「阴性结论必须附一次阳性对照」**写进研究类产出的完成定义——今夜三次全部命中，不像巧合。

**③ 下轮第一件事？**
先确认 §二那条 allowlist 有没有被采纳——如果采纳了，今晚 05:35 的 `steve-night-campaign`
应当**不再出现** `Not auto-approving`；这是一个当晚就能验的前瞻判据，别等第五天。
量法：`grep "Not auto-approving" ~/Library/Logs/Claude/main*.log | grep 2026-09-05`。

