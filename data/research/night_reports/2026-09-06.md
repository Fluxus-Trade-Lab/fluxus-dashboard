# 夜间组晨报 · 2026-09-06（JST）

**时钟**：ET now 2026-09-05 15:31（**周六，非交易日**）· last completed session **2026-09-04** · 窗口守卫 04:31 JST ✅ 在窗口内。
分支 `auto/night-20260906-8acfa1`。

---

## 一、回执

上轮（09-05）交接的第一件事：`python3 -m pipeline.tools.audit_ci_test_coverage`，看三件。**三件全查了**：

| 上轮要我看的 | 今晨读数 | 判 |
|---|---|---|
| `certified` 还是不是 True | **True**（`no violations`，无 T5） | ✅ 没人往 workflow 里加我读不懂的东西 |
| 614 这个数动没动 | **614，一格没动**（607 + 4 + 3） | ⚠️ 缺口一条没关 |
| 有没有 T2（某条 IOU 已被修好该删了） | **没有 T2** | ⚠️ 三条 IOU 全都还欠着 |

- **`fbclock` / `metricsrc` 两条分支仍未合**（`origin/main..` 各差 3 / 2 个 commit）。
  上轮说「它现在同时挡着两件事，而障碍已经全清了，只剩两行 workflow」——**今晨仍然如此**。
  本轮已**独立复验**这条建议（见 §三），不是转抄上轮的话。
- 收藏夹：INBOX 🔗 节 7 条**全部已打 ✅**，本夜无新链接 → §1.5 不做。
- 挂单认领：INBOX / §七 / §12 无标「建议 Zac」或无主而属本线地盘的新挂单，本轮自选。

## 二、这轮做了什么（两件，做透，没凑第三件）

### 件一 · 复验上轮那条「建议合 y」，用我自己的数

上轮把 `fbclock` 分支标成「建议合 y」并写了一串验证数字。**我没有转抄，重跑了一遍**
（协议：转抄过期数说了三次）：

| 跑什么 | `origin/main` | `fbclock` 分支 |
|---|---|---|
| `pytest tests`（第二个测试根，整根） | **5 个收集错**（`tests/gex`：`ib_async` + `jinja2`） | **4 个**（`ib_async` 那条已修） |
| `pytest tests --ignore=tests/gex` | **1 failed / 528 passed** | — |
| `pytest tests`（只剔掉本机缺 `jinja2` 的 4 个模块） | — | **615 passed / 0 failed** |
| 那条红了 8 天半的 `test_no_naive_clock` | 红 | **绿** |

剩下的 4 个收集错**只是我这台机器缺 `jinja2`**——它在 `pipeline/requirements.txt:27`
（`jinja2>=3.1`），`tests.yml` 装的就是这份，所以 CI 上不存在。**上轮的建议成立，我背书。**
`origin/main` 上那三处裸时钟是真代码不是 docstring（`federation_board.py:16 / :504 / :551`，逐行看过）。

### 件二 · ⭐ 17 个筛子谁是谁的影子 → 半路撞见归档有两天自己跟自己打架

起点是个页面问题：Dashboard 上并排 17 个筛子/预设，**有几个是别人的影子？**
零新数据、零网络，只读 `ticker_events.csv`（114 个交易日 / 121,198 行）。

**先算分辨率，再看结果**（协议：先算这个检验的最小可能 p）：置换检验 240 对 × Bonferroni，
R=100 时地板 **2.38**、R=2000 时 **0.12** —— **物理上不可能显著**。
所以本文**不报 p**：再堆 R 报出来的是我的算力预算不是数据。改报零分布的整个范围。
口径照抄标准件（Jaccard / overlap coefficient / hypergeometric，三处引文在正文），
自造的只有「有向包含度」一处并明写。

**① 阳性对照先过**（用我们**自己的源码**，不是借来的名单）：从阈值推出 5 条**必然成立**的嵌套，
83–110 天里 **0 违反**；配一条**必然不成立**的阴性对照，**100/100 天不成立**。尺子报得出红。

**② ⭐ 本轮最值钱的一条：中位数差点让我把「相关」写成「冗余」。**

| A → B | 中位 C | C≥0.9 的天 | 最低的一天 |
|---|---|---|---|
| `preset:weekly_momentum_97` → `momentum_97` | 0.812 | **24.0%** | **0.031** |
| *（真嵌套，对照）* `vol_up_gainers` → `gainers_4pct` | 1.000 | **100.0%** | **1.000** |

两行中位数只差 0.19，**性质是两种东西**：一个是相关，一个是嵌套。
差一点就把第一行写成「它是那个的影子」。**判「重复」必须连着最差的那一天一起看。**

**③ 同一个名字两个量**：`Vol Up Gainers` 在我们两个界面上指着两张单子
（Python：日涨≥4%∧量比≥1.5；预设：日涨≥0%∧量比≥1.5∧ADR 3.5–10∧剔医疗）。
中位每日 **Jaccard 0.346**；09-04 当天 **99 只 vs 30 只、交集 15**。
配方不同 `presets.md §3` 早写过了，**我加的是幅度**。

**④ 半路撞见的（比①②③都重）：归档有两天自己跟自己打架。**

线索是阳性对照里唯一那格例外：`preset:4_bullish ⊆ gainers_4pct` 在 100 天里**有 1 天不成立**。
拉出来是数据问题不是重叠问题 ——

| 日期 | 打架的 (字段,票) | 占当日可比 | 机制（有证据） |
|---|---|---|---|
| 2026-08-17 | 78 | 6.7% | `65bbb080`「manual pipeline run 2026-08-17 **(08-14 bars)**」被 `e2554467` 的预设回填读了进去；**7/7** 有 08-14 对照的票逐位相等 |
| 2026-08-14 | 12 | 2.4% | 上游改字段名 → 三个 gainers 筛子 08-07~08-13 **零行** → **08-14 是复活第一天**，当日中位 volume **987 股**（110 天最小值，比次低那天小 **290 倍**） |

**08-14 那条 `change_pct` 看不见，只有 `volume` 看得见。**

**闸已建并合进 main**：`pipeline/tools/audit_event_agreement.py` + **21 条测试**，
**6 个变异体全部被杀**。查四个跨筛子必须相等的字段；`sector`/`atr_ext` 全库
26,108/18,558 例可比、**0 例不一致**，当干净对照；`group`(28.5% 分歧)/`rel_volume`(42.4%)
**明确不查**并写了理由。⚠️ 第一版只查 `change_pct` 时**有 3 天完全看不见**，
扩到四字段后盲区 **0/114** —— 闸自己报覆盖面。

**⑤ 我自己的结论受不受污染，量过**：剔掉 08-17 重算，四对关键读数最大只动 0.005。**结论不动。**

**⑥ 然后我回头挖自己留的那个钩子，挖出的是自己的一个错。**
研究档 §6 里我写过一句「`rel_volume` 四成对不上，可能是另一条『同一个量两个定义』，留个钩子」。
去挖，**不是**：42.38% 的「不一致」里 **99.6% 是精度** ——
`vol_up_gainers` 四舍五入到 2 位（`1.96`），预设写全精度（`1.963872`）。
改成**按较粗一方记录的精度比**之后是 **0.19%（13 例）**，**13 例全部落在上面那两天**。

→ **它不是噪声字段，它本来就能独立指认出那两天。** 已加进闸（第五个字段），
测试 21 → **24**，新增 4 个变异体**也全部被杀**（累计 10/10）。
⚠️ 同规则**不能**推广到 `change_pct`：实测漏 6 例，全是 `"0.0"`（一位小数）撞 `"0.0412"`。
所以闸里**没有全局容差，比法按字段定，每条带实测依据**。
三处已发出的文档（研究档 / 事故档 / §六.7）与 INBOX 都已挂更正。

## 三、safe-merge 已合的 commit（全在白名单内）

| commit | 内容 |
|---|---|
| `38b1983a` | 晨报开工骨架（回执） |
| `2793493d` | `audit_event_agreement` + 21 测试 + `audit_wiring` 声明 |
| `c0856cb1` | 筛子重叠研究档 + 事故档 |
| `a7b57d13` | 晨报正文 + 三个公箱（`DATA_RELIABILITY §六.7` · `INBOX` · 素材箱两条），三处删除自检均为空 |

路径全部在白名单：`pipeline/tools/audit_*` · `pipeline/tests/**` · `data/research/**` ·
`data/reference/incidents/**` · `DATA_RELIABILITY §六` · 素材箱 · INBOX。

## 四、待合分支（本线一个字节没自作主张合）

**两条都是上轮留下的，今晨仍未合**，我本轮只做了独立复验，没有动它们：

| 分支 | 内容 | 建议 |
|---|---|---|
| `auto/night-20260905-805da3-fbclock` | `ib_async` 顶层导入下沉 + `federation_board` 三处时钟。**合了就能开 CI 开关**，且那条红 8 天半的测试当场变绿 | **建议合 y**（本轮独立复验，数字在 §二件一） |
| `auto/night-20260905-805da3-metricsrc` | `METRIC_SOURCES.md` 登记黄金季节性口径 + `DATA_CONTRACTS §七` 结案行 | **建议合 y** |

> ⚠️ **`auto/night-20260906-8acfa1` 这条远端分支已作废，不要当「待合」处理。**
> 我推 main 前变基过，远端那份停在变基前的哈希上，所以 `branch --merged` 会把它读成未合。
> 已逐文件核对：它与 `origin/main` 上我的那些文件**零差异**。内容全在上面四个 commit 里，
> 谁做分支清洁时可以直接删。

## 五、收藏夹判定清单

**本夜无新条目。** INBOX 🔗 节 7 条全部已打 ✅（最近一条是 08-30 的 LoopX，Zac 08-30 已判）。§1.5 不做。

## 六、门铃待按（本线只列不按）

1. **→ DATA ALEX**：`ticker_events.csv` 的 08-17 / 08-14 两天，三件工单已写进 INBOX 本日条目
   （重算或撤下 08-17 的 604 行 `preset:*` · 08-14 gainers 家族的 `volume`
   **并判当天成员资格是否也受影响** · 把闸接进 `ticker_events.py` 的生产路径）。
2. **→ Andy / 动 `.github/workflows/` 的那条线**：`fbclock` 分支**今晨仍未合**。
   它同时挡着两件事，而障碍已经全清 —— 这是上轮就说过的话，**今晨我用自己的数复验后仍然成立**。
   顺序不能反：先合分支，再改 workflow；反过来 CI 首日就红，而红的是一条 9 天半的老账。
3. **→ UI Claire**：`Vol Up Gainers` 一名两单（中位 Jaccard 0.346），建议改名或在页面标出配方差异。
   前端是你的文件，本线不碰。
4. **→ Andy / 各线主人**：上轮报的**三处 worktree 共 177 个 commit 只在本机**，本轮未复查（不是我的树）。
   若还没处理，它仍是宪法第一铁律的反面。

## 七、预览稿分数表

**本夜未做 §2.5。** 两件都做透了，按任务书「宁可 3 件做透，不要 5 件半成品」放弃第三件。

## 八、没动的大件

- **前瞻收益层面的筛子冗余**：集合重叠 ≠ 信息重叠。要判「哪个筛子该删」得看前瞻收益，
  而本机只有 **217 只**票的 OHLC，全池需要联网重抓 —— 夜里不碰上游（09-03 那次自伤限流的教训）。
- **`rel_volume` 四成对不上**：散布在每一个日期上，本轮判为「定义不同」没有深查。
  它**可能是另一条「同一个量两个定义」**，留了钩子在 `results.md §6`。
- `audit_event_agreement` **没有生产接线**（只挂在测试里），已如实登记，owner 写在本线之外。

## 九、收工三问

**① 这轮踩了什么坑？**
三个。第三个最难看，因为它是我**自己在同一夜里抓到的自己**。
**第一个：我差点把一个汇总统计量当成了性质。** 中位包含度 0.812，我第一版表格里的判词写的是
「经验冗余」；逐日展开才看见**只有 24% 的天数真的高度重叠、最低的一天 0.031**，
而真嵌套那一行是 110 天里 110 天都等于 1.000。**中位数只差 0.19，一个是相关一个是嵌套。**
**第二个：我第一版的阴性对照量错了对象。** 我算的是「单日 lift 的分布」（p95=2.60），
却拿它去判断「100 天中位数」这个量大不大 —— **两个不同的量，差着一个 √n。**
按那个尺子，`ema21_watch → healthy_charts` 的 1.62 会被读成「在随机范围内」。
改成对**同一个统计量**做置换才对（`null_median.py` 就是为这个重写的）。
**第三个：我给了一个听起来合理、但没有量过的理由，而它恰好挡住了一个有用的字段。**
我把 `rel_volume` 的 42.4% 分歧写成「各筛子定义不同」，理由通顺、和旁边 `group` 那条并列着，
看上去很有道理 —— 直到我回头去量，**99.6% 只是精度**，而剩下的 13 例**全部落在我今晚正在查的那两天**。
我用一个没量过的解释，把一个能独立指认这两天的字段挡在了闸外面。
**「看起来像设计如此」和「量过确实是设计如此」是两件事**，而前者穿的是后者的衣服。
→ 已进 memory：[[pitfall_a_median_hides_the_worst_day]]（新）·
[[pitfall_a_negative_control_is_not_a_calibration_check]]（旧条第 2 个实例，**同形第 2 次**）·
[[pitfall_a_plausible_reason_is_not_a_measured_one]]（新）。

**② 哪条规矩帮了 / 碍了？**
帮了两条，各救我一次：
「**先算这个检验的最小可能 p**」——算完发现 240 对 Bonferroni 后地板 0.12，
于是没有去堆 R 拿一个假显著，改报零分布的范围；
「**先找口径别自己造**」——Jaccard / overlap coefficient / hypergeometric 全是现成的标准件，
我只需要老实写出**哪一处是我自造的**（有向包含度）。
**修订建议**（只提不写）：宪法「先找口径」现在管的是**度量**。今晚栽的三跤**一跤都不在度量上** ——
在**汇总方式**（中位数）、**对照的对象**（哪个统计量）、和**排除某个东西时给的理由**。
建议把那条从「口径」扩到「**度量 + 汇总 + 对照 + 排除，四件都要说出选了什么、为什么，
而『为什么』必须是量出来的**」。最后半句是今晚第三跤直接换来的：
**排除一个字段和纳入一个字段，举证责任是一样的**，而我们现在只对后者有要求。

**③ 下轮第一件事？**
`python3 -m pipeline.tools.audit_event_agreement`，看两件：
**有没有新的 UNDECLARED 日期**（有＝今天这个形状又发生了一次，而且是在有闸之后发生的，
那就该问闸为什么没在写入时就挡住）· **08-14 / 08-17 那两条声明还在不在**
（不在了＝ALEX 修好了，E2 已经逼人删掉，这条可结案）。
然后查 `fbclock` —— 它已经连着两晚被标「建议合 y」并被两个不同的夜班独立复验过，
**若第三晚仍未合，那就不再是「等人合」而是三次律的第 3 次**，该升成机制问题写给 OPS。
