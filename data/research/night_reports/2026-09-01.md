# Nighty Zac 晨报 · 2026-09-01（JST）

**时钟**：开工时 ET 2026-08-31 15:32（**盘中**）| last completed session **2026-08-28** | 收工时 ET 已过 08-31 收盘
**分支**：`auto/night-20260901-2957fa` · **窗口**：04:32 JST 开工（窗口内）

> ⚠️ **给 Andy 的一句话**：我本来只是去跑一个从没跑过的复盘，结果撞出**两个源头级的数据完整性问题**，
> 其中一个是**我们的归档有六周只扫了半个字母表**（21 个 session，15 个筛子一天不落，占全档 17.9%），
> 而**所有的闸都是绿的**——因为行数没掉，掉的是宇宙的一半。
> **今晚的产出不是一个读数，是两个新闸 + 两份事故档 + 一条提案。**
> 好消息：**字母截断的根因我查到底了，而且代码 08-09 就已经修好**——
> 剩下的是历史数据怎么办，以及**一个更一般的洞：我们修静默失效时，从不回头量它已经吃掉了多少历史**。
> 需要你或数据端动手的三件在 §⑥。

---

## ① 回执

- **[2026-08-30 Andy 立项] 喜剧与说唱的语言技法研究** → **仍卡在你的 20 个字母。**
  盲选卷 08-31 已在 main（[BLIND_A](../comedy_rap_2026-08/BLIND_A_compression.md) / [BLIND_B](../comedy_rap_2026-08/BLIND_B_closings.md)）。
  今晨复查 INBOX / NOW.md / §七 **均无答复**，按我 08-31 收工三问 ③ 走了备选路径。
  ⚠️ **我没有再造第二份卷子**——第一份 20 组还没回，再加 12 组只会让最稀缺的输入更堵。
- **INBOX 🔗 收藏夹**：**零条未处理**。
- **INBOX / §七 / §12**：无新的标「建议 Zac」或无主而属我地盘的挂单。
- **[2026-08-29 Joe → Zac] 回归闸** → 08-30 已交付自合，闭环。
- **[2026-08-29 Joe] 变异测试基线** → 他自己认领了，我没抢；今晚顺手给两个新闸各量了一次杀死率（见 ②）。

---

## ② 本夜做了什么（3 件，全部已合进 main）

### 1【已合】Delayed-EP 账本首次复盘 —— 跑之前先修了两件会让结论作废的事

[`data/research/delayed_ep_review_2026-09/`](../delayed_ep_review_2026-09/results.md) · commit `1e9090e8`

`--review` 自 08-13 建账本以来**从没跑过**。它印六个数，**没有一个能读**：

| 它的问题 | 为什么致命 |
|---|---|
| 没有基准 | 「basing +5d 中位 −1.4%」——同期市场自己走了多少？ |
| `n=31` | 那 31 行来自 **2 个 as-of 日**。一次市场波动装在全部 31 个窗口里 |
| 会话网格取自「源返回了哪些行」 | **源少给了一天（08-28）**，于是 `+5d` 实际测的是 6 个交易日 |
| 拿盘中实时价当收盘价 | 跑的时候 ET 还没收盘，yfinance 已经给出标着 08-31 的 K 线 |

改成从 `marketcal` 取权威网格、要求目标日期精确存在、拒绝晚于 `last_completed_session()` 的端点。

**✅ 账本本身是干净的**：617 行的存储收盘价 vs 今天重拉，**100% 在 0.5% 以内，最大偏差 0.1%**。

**结论**：**这个账本现在还回答不了任何问题，包括它自己被造出来要回答的那个。**
`breaking`（第二次突破 = Pradeep 主张的全部内容）在 **+10 日上 n=0**，+3/+5 日只有 3–5 个不同 as-of 日。
按当前速率**到 30 个不同名字还要约 8 个 session；但紧约束是不重叠的 as-of 日数，那需要约 60 个交易日。**

### 2【已合】新闸 `audit_calendar_gaps` —— 源少给一天，我们所有的闸都是绿的

commit `87aadff6` / `ac595362` · 事故档 [`2026-09-01_vendor_dropped_a_completed_session.md`](../../reference/incidents/2026-09-01_vendor_dropped_a_completed_session.md)

`audit_archives` 查归档跟自己一致 · `audit_ledger` 查运行跟自己的证据一致 · `audit_regression_gate` 查新旧哪份更差。
**三个的会话网格都是从「源返回了哪些行」推出来的**，所以源少给一天，剩下的行依然完好、按序、无重复。
**坏掉的不是数值是索引**：`iloc[-N]` 从此指向另一天。

C0 空样本 = 违规不是通过（写测试时发现原实现 `check({})` 返回 `ok=True`）· C1 缺 session · C2 盘中实时价 ·
C3 休市日有 K 线 · **C4 判据：多大比例的票丢了同一天**（接近全部 = 源丢了 session；只有几只 = 那几只没交易）·
C5 那格里有东西 ≠ 那格里是真数据 · **D1/D2/D3 三方对账**（日历 / 源 / 我们自己的归档）。

**三方对账是今晚最有用的一件**，因为下面这两个**在日历和源上长得一模一样**，只有归档分得开：

| 日历 | 源 | 归档 | 判定 | 今晚的真实样本 |
|---|---|---|---|---|
| 交易日 | 有 K 线 | **无行** | **D1 归档洞** | `ticker_events` 缺 07-14..07-24 等 12 天 |
| 交易日 | **无 K 线** | 无行 | **D2 日历错** | **2025-01-09**——5 只票 + 我们的归档都说休市，是 `marketcal` 不建模临时休市 |
| 交易日 | **无 K 线** | **有行** | **D3 源退化** | **2026-08-28** |

34 个测试 · 变异普查 34% → **51%**。

### 3【已合】⭐ 新闸 `audit_universe_shape` —— 归档有六周只扫了半个字母表

commit `7db2ada8`（事故档）/ `95c82aff`（闸） · [`2026-09-01_half_the_alphabet_missing_for_six_weeks.md`](../../reference/incidents/2026-09-01_half_the_alphabet_missing_for_six_weeks.md)

`ticker_events.csv` 在 **2026-06-26 .. 08-07 的 21 个 session** 里，**没有任何一只票的首字母在 `L` 之后**。

| | |
|---|---|
| 覆盖 | **15 个 screener，一天不落，全部 0.0%**；各自窗口外基线 43–52% |
| 行数 | **19,850 行 = 全档的 17.9%** |
| 边界 | 06-25 = 44.0% → **06-26 = 0.0%** → 08-11 = 50.8%，**无过渡带** |
| `episodic_pivot` 单看 | 基线 51.7%，窗口内 93 行零命中 → **概率 4.25e-30** |

**⭐ 根因（收工前查到了，而且修复早就在 main 里）**：`finviz_adapter.py` 的 `max_pages = 150`
（= 3,000 只，`3fa5287d` **2026-03-01** 就有）。**Finviz 按字母序返回行**，所以一个会咬的分页帽
**不是对市场抽样，是把它从字母中间切断**。
它 **06-26 才开始咬，因为那天宇宙涨过了 3,000**——这就是为什么 06-20..07-02 之间 `pipeline/` 零 commit
却出现硬边界：**代码没变，是帽子开始咬了**。
`2f782b53`（**2026-08-09**）已抬到 600，其注释原话：
> *At 150 pages the universe stopped at **LNTH**: every ticker from M to Z was missing,
> including NVDA, MSFT, TSLA and PLTR.*

`LNTH` 在我们数据里就是 07-02 那天的 max。归档 08-11 恢复，时间线完全吻合。**代码侧不用修。**

⚠️ **而这才是这件事真正的教训**：`2f782b53` 08-09 修好了代码，**却没有人回头问「那之前那些天呢」**。
缺的不是修复，是**修复之后的回溯**——一个「从今天起不再发生」的补丁，
**不会自己告诉你它已经发生了 21 天。**

**为什么没有闸响**：`audit_archives` I4 查行数落在滚动中位数的 [floor, 3x] 内——而 **06-26 有 1,613 行，比前一天的 965 还多**。
少的不是行，是宇宙的一半，剩下那一半碰巧更活跃。

> **一个按内容维度切一半的源，在所有计数维度的检查下都是完美的。**

所以新闸量的是**一个行数完全不动也会动的量**：首字母在 `L` 之后的占比，对自己的滚动中位数。
**真实历史上阴阳对照都拿到了**：在 **06-26 这个首个受影响 session 就报**，且 **03-09..06-25 的 74 个健康 session 零违规**。
15 个测试（含一个直接钉死本意：两个 session 行数完全相同、只有覆盖度变了，它必须报）· 变异普查 43%。

⚠️ **这条坑（`pitfall_the_universe_chose_the_answer`）今晚是第 4 次**，前 3 次都是便利样本。
按宪法三次律不再记 memory，已升级为闸。

---

## ③ 对抗性验证（按任务书 §2 «研究结论走 fan-out»）

**两个验证者，两个视角，都被派去反驳我。结果是我今晚最值的一笔投入——它们改写了我两个结论。**

| 视角 | 判定 | 它改了什么 |
|---|---|---|
| 数据完整性 | **PARTIALLY** | 我的**机制说错了**：不是「yfinance 不再返回这一天」，而是**槽位在、OHLC 全 null**，宽窗口被 `dropna` 删掉、**截断窗口返回一根贴着 08-28 标签的 08-31**。它还推翻了我的「四种写法一致所以是上游」（**四种都用了宽窗口，是同一条件下的四次同形观测，不是四条独立路径**）和「FBRX 是幸存者」（**它 07-20 起停牌，那根是零成交量占位符**）。三处已在事故档更正 |
| 统计 | **REFUTED as stated** | 我原本报了 `failed` 格的中位 −0.7% / CI [−1.1,−0.5] / 8 个日期全负 / p≈0.008。**三个数字全部作废**：n=223 实为 **37 只票 × 8 个重叠日**，按票聚类 CI **[−2.32, +0.27] 跨零**，一票一观测是 **21/41**（掷硬币）。**而且 24 格家族的最小可能 p 是 0.0078，Bonferroni 后 0.1875——这个检验物理上报不出显著。** 它还发现了 §②·3 那个字母截断（我去核，范围比它报的大一个数量级） |

**剩下能写的一句**：*缺口被填破的名字，三日内跑输同日 cohort 超过 5% 的比例是 24.7%，未破的是 8.8%；上行侧没有差别。
中位数层面本轮测不出可信差异。* **不许写中位数 / CI / p 值 / 「8 个日期全部为负」。**
已改成一条按票、按 cohort 基准的预注册，前置条件是**字母截断先修好**。

---

## ④ safe-merge 已合的 commit

全部只碰白名单路径（`pipeline/tools/audit_*` + 其测试 · `data/reference/incidents/**` · `data/research/**`），
全套 `pytest` 绿后自合：

| commit | 内容 |
|---|---|
| `87aadff6` | `audit_calendar_gaps` + 21 测试 + 事故档 |
| `ac595362` | 事故档补爆炸半径 |
| `7db2ada8` | 字母截断事故档 |
| `1e9090e8` | Delayed-EP 复盘（脚本 + results + 原始输出） |
| `95c82aff` | `audit_universe_shape` + 15 测试 |
| `20bbf9e6` | **闸 v2 + 事故档三处更正**（C5 / 三方对账 / grace；见 ⑨·① 那个事故） |
| `9019bd5f` | §七 第十四节契约行 → DATA ALEX |
| `0a5e58de` | 素材箱两条 + 晨报补未提交事故 |
| `a2464121` | 晨报补待合分支一节 |
| `b249b2d5` | **根因**：`max_pages` 帽子，08-09 已修；事故档与 §14 同步改写 |
| `f3a8b47b` | **量化今晚影响**：昨收误差 0.52%/1.49%/3.10%（三个样本） |
| `27f07073` `1679041e` | **提案**：修复之后的回溯（+ 更正我自己的过度断言） |

**契约行**：§七 新增「十四」节（→ DATA ALEX / Andy），走公箱直推 main，已做「零删除行」自检。
⚠️ **报备一句**：`DATA_CONTRACTS.md` 不在我任务书的 safe-merge 白名单里，我按宪法「直推 main 的标准动作」
（该节明写覆盖**契约行**）走的。**若 OPS 认为该走留分支，下次我改。**

## ⑤ 待合分支

**我今晚的：无**，全部已合进 main。

⚠️ **但收工跑 `audit_unpushed` 时发现我这条线有一条旧分支挂着，且它已经该删了**：
`auto/night-20260828-bfbf5c`（3 个 commit，只在本机）碰的是 `pipeline/tools/federation_board.py`
——**不在白名单，所以当时留分支是对的**。但 main 现在已经是 **v6（路径优先的 lane 归属 +
`test_federation_board_lane.py` 都在）**，OPS 已经把这件事重做了。
**结论：这条分支不该合——合了会把 main 的 v6 退回去。建议删。**
（它所在的是别的会话的 worktree，我不进去动，见 ⑦。）

`audit_unpushed` 同时报了另外 3 条不属于我这条线的：`auto/night-20260819` ·
`claude/friendly-chaplygin-b46c13`（160 commit）· `design/marketing-visual`（16 commit）。

## ⑥ 需要 Andy / 数据端决定的

1. **⏰ 今晚 cron 跑在坏掉的源上。** **收盘后（ET 16:30）我又测了一次：08-28 仍然缺，8/8 只票。**
   跑前/跑后各跑一次 `python3 -m pipeline.tools.audit_calendar_gaps --days 30`。
   （顺带一个好消息：C2「盘中实时价」已自动停止报警——08-31 现在是完整 session。**闸跨收盘的行为是对的**，
   这条是它自己在真实时间轴上走过一次的验证，不是我构造的测试。）
   **在定下补法之前，别用现在的源重建任何历史归档**——重建一次就把 08-28 永久烧掉。
2. **⭐ 字母截断的历史数据怎么办**（**代码不用修了，根因已定位且 08-09 已修复**）。
   Finviz 不给历史 screener 结果 → 多半回填不了；**拿不回来时正确动作是打「宇宙不完整（A–L only）」标记，
   不是假装完整**。且至少 5 项已有研究（`b4_gates`/`tightness_study`/`momentum97_shadow`/`oratnek_diff`/`leaders_log`）
   需要**重报一次宇宙**——**不是说结论错了，是说它们那段的宇宙是 A–L，而 M–Z 里有 NVDA/MSFT/TSLA/PLTR。**
3. **⭐ 盲选 20 个字母**（结转自 08-31）——喜剧/说唱研究的 device 生死全卡在这一步。

## ⑦ 门铃待按（只列不按）

- **DATA ALEX** · §七 第十四节 A/B/C 三条，两份事故档已在 main。**A 的第 1 条今晚就相关。**
- **DATA ALEX** · `delayed_ep_scan --review` 的默认 horizon 是 `(5,10)`，**+10d 在可预见的将来都是 dates≤2**，
  建议默认改 `(3,5)`。`delayed_ep_scan.py` 不是 `audit_*`，**不在我白名单，我没改。**
- **Plumber Joe** · 两个新闸各自的变异杀死率（51% / 43%）可以进你那条基线。
- **OPS Fable / repo-janitor** · 请删 `auto/night-20260828-bfbf5c`（我这条线的旧分支，
  已被 main 的 federation_board v6 取代，**合它会导致回退**）。它在别人的 worktree 里，我不进去动。

## ⑦.5 今晚 cron 的状态（我等到 06:41 JST 为止的实况，**不是事故**）

我留了两轮轮询等今晚的 `Daily Market Data Update`，想验证 08-28 缺口对它的实际影响。**到 06:41 JST 它还没落地。**

⚠️ **我差点把这个报成事故，读了排程口径之后收回**——这里如实记录，因为它正好是今晚的主题：

| 事实 | 判读 |
|---|---|
| 主排程 `30 21 * * 1-5` = 21:30 UTC；我查时 **21:43 UTC，迟 13 分钟** | **正常。** workflow 自己的注释写着「median 63 min late, worst 213」 |
| `gh run list --event=schedule` 最近一条是 **08-29T08:00Z** | **正常。** 08-30 是**周日**，`1-5` 和 backstop 的 `2-6` 都不覆盖它 |
| 08-30 那三个 run 是 `workflow_dispatch` | 是人按的，不是排程漏了 |
| 其他定时 workflow 今天正常触发（Pre-Market Digest 19:04/19:15Z） | Actions 排程本身没坏 |
| 已有 **backstop `30 1 * * 2-6`**（01:30 UTC，4 小时后） | 08-27 被 GitHub 丢弃那次之后就加上了 |

**数据现状**：`watchlist.json` 与三个归档的最新 session 都是 **2026-08-28**——
08-29/30 是周末，标签正确；**08-31（周一）这个 session 的数据今晚才会产生。**

**⏱ 我等到 07:53 JST（22:53 UTC）为止的最终读数**：

- **过期 83 分钟，`gh run list` 里连一条排队条目都没有**——这正是 workflow 注释描述的
  08-27 那次「dropped outright — no queued entry, nothing in the run list」的签名。
- 但**还不到判定线**：那次是过期 **327 分钟**才判为丢弃的，注释记录的 worst 是 **213 分钟**。
  83 分钟仍在历史区间内。**所以我不判它是事故，只交出观测。**
- **backstop `30 1 * * 2-6` 会在 01:30 UTC = 10:30 JST 触发**，那已在我的窗口之外。

**所以：无需动作，我也没有动**（任务书红线「不开 cron」）。
**交给 Joe / 数据端的一句**：若 backstop 也没补上 08-31 的数据，**那才是事故**，
且它会是 08-27 之后的第二次「GitHub 丢弃排程」——**第 2 次，离三次律只差一次。**
⚠️ 补跑时请**先跑一次** `python3 -m pipeline.tools.audit_calendar_gaps --days 30`：
08-28 在源里仍然是坏的（我 ET 16:30 复测过），补跑会把它一起吃进去。

⚠️ **顺带一条规矩打架，请 OPS 裁**：宪法「外部动作」节把
「重跑幂等的定时任务（`gh workflow run` 夜间数据）」列进**「直接做，不问 Andy」**；
而我的任务书红线第 4 条写的是**「不开 cron」**。今晚我按任务书办（不碰）。
**两条哪个优先，请给一句写死的**——不然下一个夜班会在同一个岔路口再站一次。

## ⑧ 没动的大件

- 喜剧/说唱第二轮（NULL-2 对仗配对 ≥12 对）——**故意没做**，理由见 ①。
- TML `leaders_log` 前瞻验、`momentum97` shadow、`shock_days` —— 本轮没碰。
  ⚠️ **而且现在有新理由先别碰**：后两个的样本都跨过字母截断窗口。

---

## ⑨ 收工三问（宪法 08-27）

**① 这轮踩了什么坑？**

**⚠️ 最严重的一个是收工时才抓到的：`pitfall_wrote_it_is_not_delivered` 又一次。**
验证者驱动的三处更正、C5、三方对账、grace ——**全部写完之后一直躺在未提交区**，
而我已经把它们**之前**的版本 push 上了 main。是 `git rebase` 报「You have unstaged changes」
才暴露的，不是我自己查出来的。
**可复用的判据**：我这一晚的节奏是「改文件 → 跑测试 → 看结果 → 改下一个文件」，
**测试全绿会给人一种"这件事完成了"的感觉，而绿的是工作区，不是 main。**
下轮起：每次 `git push` 之前先跑一次 `git status --short`，非空就停手。

**「那格里有东西」被我读成了「那格里是真数据」。** FBRX 是 90 只里唯一在 08-28 槽位还有东西的，
我据此写下「89/90 丢失，FBRX 是幸存者」，并把它当成 C4 判据的示范案例。
验证者一查：**它 07-20 就停牌了，那根是 `O=H=L=C=76.99, Volume=0` 的陈价占位符。**
**唯一一个还有数据的 ≠ 唯一一个没受影响的。** 已写进闸（C5）而不只是写进 memory。

第二条：**我把「四种写法一致」当成了四条独立路径互证**，而那四种写法**共用同一个条件**（宽窗口）。
换成截断窗口，第五种写法立刻给出不一致的结果。
→ 这是 `pitfall_audit_greps_miss_the_other_spelling` 的近亲：**换函数名不等于换条件。**

第三条：**我又一次报了一个当场就会过期的数。** 写「8 个 as-of 日全为负」的时候，
08-31 正在收盘，样本几分钟后就变成 9 个日期。`pitfall_a_measurement_expires` 再一次。

**② 哪条规矩帮了 / 碍了？**

**⭐ 另有一条提案（不是规矩，等 OPS 周检裁决）**：
[`data/research/retro_after_the_fix/README.md`](../retro_after_the_fix/README.md)。
今晚字母截断的根因 **08-09 就被人查出并修好了**，那条 commit 的注释甚至写清了症状，
**但没有人回头问「那之前那些天呢」**——于是 19,850 行半宇宙数据躺了三周。
近三个月标题明写「静默/从没/一直」的修复 **17 条**（grep 出来的，可信）；
我**手读了其中 8 条，2 条做了回溯**（另 9 条没判——我写过一个自动判据想一次判完，
**它给 7/17 而那个数不能用**，因为它把与损害范围无关的数字也算成了回溯）。
提案：**完成定义加一句——修一个静默失效时，交付物是补丁 + 它已经吃掉多少历史的度量。**
⚠️ 我引不出 Andy 原话，按宪法**没有**写进 `CLAUDE.md`。

**帮了，而且是决定性的：「研究结论走 fan-out，≥2 个不同视角 verifier」。**
两个验证者一个改写了我的机制描述、一个作废了我三个数字，**其中一个还顺手挖出了今晚最大的发现**。
没有它，我今天会发布一条错误的机制解释和一条不成立的统计结论。

**建议修订（给 OPS 周检）**：宪法说「**研究结论类**默认走 fan-out」。今晚的证据是——
**事故档也算研究结论。** 我第一版事故档的三处措辞全错，而事故档比研究报告更容易被当成事实引用
（它就是给别的线读的）。建议把判据从「研究结论」扩成「**任何要被别的线当依据引用的断言**」。

**③ 下轮第一件事？**

**看 Andy 的盲选结果**（若已回）→ 判 15 条 device 的生死。
若仍未回：**做「修复之后的回溯」这件事本身**。今晚的根因（`max_pages` 帽子）已经查到底，
但它暴露了一个更一般的洞——**我们修静默失效时，从不回头量它已经吃掉了多少历史**。
下轮第一件：**把 17 条候选逐条回溯**（清单已在提案文件里），每条问「它从哪天开始、影响了多少」。
纯读操作，全在夜班边界内。预期产出是一份**「已知脏数据区间表」**——
**我们现在没有这张表，而每一次研究都默认它是空的。**
