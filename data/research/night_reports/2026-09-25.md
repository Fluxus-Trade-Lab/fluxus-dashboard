# 夜间研究班晨报 · 2026-09-25（JST）· linda

ET now 2026-09-24 15:31（周四，盘中）| last completed session **2026-09-23**（周三）| 今天是交易日
任务 `T-0925-11` · 窗口 04:31 JST（在 04:00–10:00 内）· 时间盒 150 分钟

> 昨夜（`T-0924-11`）把 `audit_universe_freshness` 38%→93%、`audit_unpushed` 47%→90%，
> 收尾写明下轮第一件事：**`audit_ledger`（42/80，53%）与 `audit_archives`（54/101，53%）现在并列最低**，
> 并留了两句叮嘱——**先复算再动手**（那两个数是 09-02 的 `repeat=1` 读数）、
> **超 80 个变异点必须切片跑**（前台 10 分钟上限）。今晚照办。

## 一、回执

| 来源 | 内容 | 状态 |
|---|---|---|
| 任务板 `list --owner linda` | 名下只有本单 `T-0925-11`（claimed）；09-21/09-22 两张过期日班已由 ops 09-24 closed，无需补跑 | 已核 |
| INBOX 🔗 收藏夹 | 最后一条 09-12 已 ✅，**无新条目** | 不适用，跳过 1.5 节 |
| INBOX 📌 给 Andy 的待办 | 本线名下无未核销条目 | 已核 |
| 09-24 晨报「下轮第一件事」 | 打 `audit_ledger` / `audit_archives`，先复算 | 本班执行中 |

**已认领**：无新挂单指名本线。

## 二、进行中

- **今晚的题**：全库并列最低的那两道闸。两个都在本线边界（`pipeline/tools/audit_*` 及其测试）。
- ⚠️ 第一步不是补测试，是**复算**：那两个 53% 都是 09-02 那份 `repeat=1` 报告里的读数，
  而 09-02/09-03 两晚都补过这两道闸的测试。不能转抄——按 09-24 的账，「加固」和「被钉住」是两个量。
- ✅ 复算完了，两个数都不对。详见第三节。

## 三、今晚做了什么

**一句话**：昨夜转抄的两个「53%」都不对——现场复算是 66% 和 57%，
**而那句「先复算再动手」的叮嘱正是为此写的**；复算完把两道闸补到 97% 和 98%。

### ① 复算：两个「最低」都不是最低

| 闸 | 09-24 晨报转抄的 | 今晚现场量到的 | 差在哪 |
|---|---|---|---|
| `audit_ledger` | 42/80 = 53% | **53/80 = 66%** | 09-03 夜班已补过（`776a7c6f`「52->66%」）；66% 就是那晚的收工读数 |
| `audit_archives` | 54/101 = 53% | **58/101 = 57%** | 09-02 当晚补到 57%（`bb88d995`） |

**两处错都不是量错，是引错了同一份文档里的前一栏。** 09-02 那份报告每行都写成「54→57%」，
抄的时候取了箭头左边那个数。代价很具体：**它把今晚的题选错了**——真正最低的是 archives，
按两个 53% 看则「并列、先打哪个都一样」。复算四段切片共 10 分钟。

### ② `audit_archives` 57% → 97%（测试 39 → 55 条 / 75 用例）· `beecb9da`

43 个存活点里 **19 个长在同一张表上**：文件顶上 `ARCHIVES` 注册表，
每行两个布尔值决定这份归档被哪几道检查看着。把 `ticker_events.csv` 的 `nightly`
翻成 `False`，**I5 就不再问「写这份归档的人是不是死了」**——而那是 I5 存在的唯一理由
（08-18，delayed_ep 在一次限流下归档 0 行）。

**⚠️ 我第一版把这 19 个全放过了，而且是我在注释里刚点名要避开的那个形状。**
第一版逐个归档问行为，看起来很干净：

```python
assert bool(w) is bool(spec["counts"])     # spec 取自被变异的那本字典
```

变异体把 `counts` 翻掉，**期望值跟着一起移动**——对真代码绿，对变异体也绿。
重跑尺子：0:50 段 25 个存活只死了 3 个。修法是把期望值写死成一张带日期的对照表
（`WATCHED_ON_2026_09_25`，十一行逐个签名）+ 一条防腐断言。改完 0:50 段 49/50。

另补 24 处：I4 双峰回看窗口 · `--repair` 的 `to_csv(index=False)`（翻转后修一次归档多一列索引列）·
`keep_default_na=False`（一只真名叫 `NA` 的票和一只叫 `NULL` 的票都会变成缺失值，
I2 报一个不存在的重复，`--repair` 接着删掉一行真数据）· I6a 缺省值 ·
`main()` 干净时的退出码 · 四处报告初值 · I7 消息截断。

剩 3 个存活：两条实跑验过是等价（`len>21` vs `>=21` 在 len=1..40 全等；
`and`→`or` 在 6 种 repair 形状上返回值与写回文件逐字节相同），一条是 JSON 缩进。

### ③ `audit_ledger` 66% → 98%（测试 28 → 48 条）· `d8e70930`

**27 个存活点的形状和 archives 完全不同**：不是配置被翻转，是
**「递进来一条没人写过的账本行时它做什么」**。5 个 `and`↔`or` 全长在让审计器**不崩**的判断上——

| 递进去一条什么样的行 | 崩在哪 |
|---|---|
| 某 guard 被写成裸词 `"legacy_stage": "degraded"` | `str.get` AttributeError |
| `fundamentals` 有 `failed` 没 `due` | `failed / None` TypeError |
| 整夜没有 `universe_quality` 这个 guard | `None.get("rows")` AttributeError |
| 三场有 universe 行数、第四场没有 | `abs(None - med)` TypeError |

**一个会抛异常的审计器等于不报告**，而它跑在夜间闸里面。
其余 22 个：L6 两个阈值边界（漂移 10% / 失败率 20%）与 3 场最小历史 ·
`window` 的两个默认值（默认 2 ＝每晚重判昨天）· 四处计数精确性 · 三处摘录长度 ·
L5 比对的是「前一场」而不是 `sessions[-1]` · `main()` 干净时的退出码。
剩 2 个：`window > 0` 的 `>=` 等价（实跑 5×9 组合全同）、JSON 缩进。

### ④ 两道闸放一起看，欠的是两笔不同的账——但有一处一模一样

| | `audit_archives` | `audit_ledger` |
|---|---|---|
| 主漏洞 | **配置**：注册表里哪份归档被谁看着 | **鲁棒**：畸形行会不会让它崩 |
| 补法 | 写死对照表逐行签名 | 逐个造一条没人写过的账本行 |

**一模一样的那一处**：两道闸的 `main()` 都只测过报错那一路，
`return 0 if ok` 的 `0 -> 1` 在两处各活了三周——一个**会让 CI 每个干净夜晚都 exit 1**
的变异。「测过 `main()`」和「测过 `main()` 的两条路」不是一件事。

## 四、收尾

**测试**：`pipeline/tests` + `tests` 两根一起跑，**3616 passed / 1 skipped / 12 deselected**，
全绿（6 分 46 秒；两件各跑一次，这是第二次）。

**已合的 commit**（gate=none，逐次核过 `git log origin/main` 上的真 sha，三次 push 全是快进、没 rebase）：

| commit | 内容 |
|---|---|
| `279602d4` | 本晨报开工 |
| `beecb9da` | `audit_archives` 测试 39→55 条，57%→97% + 研究档 |
| `d8e70930` | `audit_ledger` 测试 28→48 条，66%→98% + 研究档补第七八节 |

**待合分支**：无。gate 三次都判 `none`，全部自合，没开过分支。

**收藏夹判定清单**：INBOX 🔗 节最后一条是 09-12，已 ✅，**本轮无新条目**，不适用。

**预览稿分数表**：本夜未做 UI 预览稿（2.5 节）。时间给了上面两件——
按任务书「宁可 1 件做透」，且这两件是同一把尺子的两次读数，分开做会重复一遍同样的复算。

**素材箱**：已追一行（数字 + 两个好故事 + NULL 的那一半）。

**只差 Andy 一步的交付**：**无。** 本夜产出全部 gate=none 自合，不需要他回任何一个字。
（故 `needs-andy` 与 INBOX『📌 给 Andy 的待办』两处均不适用。）

**没动的大件**：`audit_ci_test_coverage`（162 个变异点，全库最大，**从没量过**）·
`audit_calendar_gaps` 61% / `audit_universe_shape` 63%（09-02 读数，同样该先复算）·
`DATA_RELIABILITY.md` §六第 6 条那四条欠账（都要动 `.github/workflows/tests.yml`，不在本线边界）。

**下轮第一件事**：量 `audit_ci_test_coverage`，三段切片 `0:55` / `55:110` / `110:162`。
⚠️ **「全库最低」这句话在它量出来之前，都只是「量过的里面最低」**——
今晚两次复算说明，**转抄的排名比转抄的数字更容易过期**：数字错了还只是一个数，
排名错了会选错今晚的题。
