# 夜间组晨报 · 2026-09-08（JST）

> ET now 2026-09-07 15:32（**Labor Day，休市**）| last completed session **2026-09-04** | today is trading day False
> 分支 `auto/night-20260908-c4cb0a` · 开工 04:32 JST

## 一、回执

| 收到的 | 状态 |
|---|---|
| **Marketing Steve → Zac（Andy 09-06 批了推广）**：往台账写「结论/清单/判定」前，派全新上下文子 agent 复核 | **✅ 已认领**，本夜结论走它；执行情况见 §三 |
| 我 09-07 的门铃 · `fbclock` 分支「建议合 y」 | 见 §二，**第四晚未合**；今晚重新验过，仍成立且**没有过期** |
| 我 09-07 的门铃 · `audit_stranded` 待接线（→ OPS） | 未接；今晚先把它变成**能被接**的样子（12 分钟 → 见 §二） |
| 我 09-07 的门铃 · MR. FLUXUS 已推 `origin/design/marketing-visual` | 无回音，纯追加、无风险 |
| 我 09-07 的门铃 · DATA ALEX 三条声明未销 | 见 §二末 |

## 二、本夜做了什么

### 1. 主活：陈旧的一天，会带着供应商的修订回来

研究档 [`frame_progress_2026-09/results.md`](../frame_progress_2026-09/results.md) ·
工具 [`audit_progress.py`](../../../pipeline/tools/audit_progress.py) ·
测试 23 条 · 已合 main：`fba312bf`（9 个 commit，全部在 safe-merge 白名单内）

我 09-06 留了一句「09-02 那天的四态判定也可疑」。量出来是：
**36/36 逐位等于 09-01 —— 不是几个字段错了，是整场重放。**

它躲过了我们所有的闸，因为**有两列确实变了**：`today_relvol` 36/36 变了
（中位 0.30%，**最大 +46.4%**：HTHT 0.5643→0.8262），`recent_range_pct` 19/36 变了。
一根已经收掉的 bar 被重抓时**带着盘后修订回来**，在「有没有变化」这个维度上完美伪装成新的一天。
`audit_archives` I3 问的正是「变没变」——**一个 46% 的跳动会让它判定这是新鲜的一场**。

计数器不吃这一套。`days_since` 跟价格无关，**必须等于交易日历上 `(ep_date, as_of]` 的交易日数**。

**真实阳性对照**：08-17 是 `DATA_RELIABILITY` §六.7 已立案的坏日，本闸用完全不同的判据
**独立报出它**，`base_high` 那条还指出掉的是哪一根（CORT 118.53→115.00，731 对里唯一一次下降，
08-18 又弹回 —— 瞬时缺陷不是永久丢失）。
**新发现两天**：08-19（12 行）与 **08-20（8 行）**，此前没有任何闸报过；
另有 **10 行幽灵**（修正后 `days_since=16` > `--max-days 15`，本不该在归档里）。

**给全线的一句**：我 09-06 写的「09-02 那天在文件内部完美自洽，任何只看一个文件的检查都判绿」——
**那句话是错的**。单调量在文件内部就报得出来，我当时没想到去看计数器。
准确的说法是：**自洽性检查的盲区是「值」，不是「文件」——凡是只能朝一个方向走的量，一个文件就够。**

### 2. 认领的挂单：Andy 09-06 批的子 agent 复核闸，本线首次执行

两个全新上下文的 agent（算数视角 + 反驳视角），只给原始 CSV、不许读我的工具和报告。
**两个 agent 各退回了一件东西，都不是算错的数**：

- **算数视角**：12 条断言 12/12 对，**但退回一条读法** —— §五 那两行「零翻转」是**平凡真**，
  11 只票没有一只靠近阈值（最近的差 3.9%），拿 1~3% 的误差去测它不翻转是必然的。
- **反驳视角**：**推翻了我的一条支柱**。我原来的判据是「Δ`days_since`=0 ⇒ 帧没前进」，
  它用 08-17/08-19 的 24 行直接证伪（Δ=0 但 `close` 全变了 ⇒ 帧确实前进了，
  只是更早的一根消失了），并指出**差分口径在 08-20 漏报 8 行** ——
  赤字一旦形成就持续，之后每天 Δ 都是正常的 +1。
  已把 P1 改成**对交易日历绝对重算**：差分报 60 行，绝对报 **68** 行（漏 25%），
  并补了「退回差分口径就会红 12 条」的回归测试。
  它还推翻了我私下的字母序假说，并提出「受影响的票只有下界」这条限制。
  ⚠️ 它自己也漏了：只报 08-20 的 6 行幽灵，全库实际 **10** 行。

**结论：这道闸抓的不是算错的数，是站不住的读法和站不住的判据。强烈建议保留，且比只核数字值得多。**

### 3. 昨晚那句「12 分钟」一天就过期了

`audit_stranded` 昨晚在一条 2500 文件的分支上跑了 12 分钟，我据此写了「太慢，接不上线」。
**今晚那条分支已经没了，全量 28 秒。** 性能不是瓶颈 —— 没接上线才是。
（同一个形状第 N 次：`pitfall_a_measurement_expires`。）

### 4. 收藏夹

🔗 节 7 条链接、7 条 ✅ 已处理 —— **本轮零新增**，无待判。

## 三、待合分支（`audit_stranded` 读数，2026-09-08 04:5x，全量 28 秒）

11 条有未合 commit 的分支，工具判三态：

| 状态 | 分支 | 一句话 | 建议 |
|---|---|---|---|
| 🔴 真滞留 | `auto/night-20260905-805da3-fbclock` | 55 行 · **第 4 晚未合** | **合 y**（见下） |
| 🔴 真滞留 | `design/marketing-visual` | 2219 行 · MR. FLUXUS，我 09-07 代推的保命副本 | 归视觉线，**不催** |
| 🟡 过期 | `night-20260831-2a66fb` / `night-20260903-5cea87` / `night-20260905-805da3-metricsrc` / `night-20260906-8acfa1` / `fix/alex-stockbee-s2-prev-volume` | 缺的行全落在 main 自分叉后改过的文件里 | **别写「建议合」**，要合先人工比对 |
| ✅ 内容已全在 main | `auto/vol-dedup-2026-09-04` / `feat/morning-three-pages` / `voice/deslop-ammo` | 分支是垃圾 | 可删（⚠️ `feat/morning-three-pages` 是主工作树当前所在分支，删远端前先看一眼）|

### ⚠️ `audit_unpushed` 收工读数：两条本机独有的活（都不是我的，都别去救）

- `claude/friendly-chaplygin-b46c13` —— **160 个 commit 只在一块硬盘上**（远端 ref 已不在；
  09-07 它还在 origin 上）。⚠️ **别推**：内容是 `data/portfolio/*.csv`，
  main 故意 gitignore 掉的 live 仓位，推它等于把实盘发上远端。**这不是滞留是设计。**
- `auto/night-20260819` —— 1 个 commit 只在本机（`AI-Trading-System-night` 树），已放 20 天。

## 四、门铃待按（本线只列不按）

### → 动 `.github/workflows/` 的线：`fbclock` 第 4 晚未合，今晚用两把尺子重验，**没有过期**

`tests/test_no_naive_clock.py` 在 main 上**红了 12 天**（自 `6f66f5f9`，08-27 16:18 JST）。
今晚现场跑，仍红，只剩 3 处，全在 `pipeline/tools/federation_board.py`。

**重验的两件事**（不是照抄昨晚的判断）：
1. `git log $(merge-base)..origin/main -- <该文件>` → `pipeline/tools/federation_board.py` **0 次**、
   `scripts/gex_levels.py` **0 次**。main 自分叉后一次都没碰过这两个文件 → 真滞留，不是过期。
2. 分支内容读过一遍：它**不是**给 `now` 挂一个 `localtime-ok` 就完 ——
   真 bug 在减法的另一边（`--date=format:` 按 commit 自己的时区渲染，
   近 14 天 686 个 commit 里 8 个落在错的本地日），分支同时修了格式串。

**代价**：`§六.6` 写明顺序是「先合 fbclock，再改 workflow」，反过来 CI 首日就红。
所以这条分支挡着的是 **614 条测试进 CI** 这件事。

一条命令：
```
git push origin origin/auto/night-20260905-805da3-fbclock:main
```

### → OPS Fable：`audit_stranded` 仍未接线，但**它不慢**

我 09-07 写的「2500 文件跑 12 分钟」**一天就过期了** —— 那条分支已经没了，今晚全量 **28 秒**。
接线的阻力不是性能。它已按既定通道登记在 `audit_wiring.KNOWN_UNWIRED`。

### → DATA ALEX：两条，都在 `data/history/`（本线不碰）

① **09-02 的 36 行是 09-01 的重放，不是错值** —— 撤下或重算都行，
   但别只修 `close` 一列，那天整帧都是上一场的。
   这条销掉 `DATA_RELIABILITY` §六.7 ⓪ 里「四态判定也可疑」那句悬着的话：不是可疑，是 36/36 复制。
② **08-19 是新的** —— 12 只票 `days_since` 冻住而价格照走（帧掉了一根 bar），
   与 08-17 重合 11/12，`ep_date` 全部未变。**此前没有任何闸报过这一天。**
③ 生产接线点：`delayed_ep_scan.py` 追完归档后自查一次。

（另：09-07 报的三条声明 —— 08-14 gainers volume · 08-17 的 604 行 `preset:*` ·
09-02 —— 今晨仍是三条，一条没销。）

## 五、没动的大件

- §六.7 的 08-14 / 08-17 两条声明（gainers volume · 604 行 `preset:*`）—— 归 DATA ALEX，本线不碰 `data/history/`。
- ~~`universe_quality.csv` 的 `days_since_52wh` / `bars_stale`~~ **当晚查掉了：不适用。**
  那张表**每一列都是「该字段当场的空值率」**（`close` 那列是 `0.002136` = 0.2% 空值），
  一场一行，这两列全是空串 —— 不是计数器。**同一个「凭列名猜语义」的错，一晚犯了两次。**

## 六、收工三问

**① 这轮踩了什么坑**

**最大的一个：我的检测器比的是差分，而它要量的东西是累积的。**
「Δ`days_since`=0」只看得见赤字**形成**的那一场；赤字**持续**的每一场，Δ 都是正常的 +1，
它全报绿 —— 08-20 那 8 行就这么漏掉的（差分 60 行 vs 绝对 68 行，漏 25%）。
而且我给 Δ=0 编的那个理由（「帧没前进」）**本身就是错的**：那 12 只的 `close` 全变了，
帧确实前进了，只是更早的一根消失了。**两个错都是复核 agent 用我自己的数据打出来的。**
→ [[pitfall_ask_whether_it_advanced_not_whether_it_changed]]

**第二个：我拿字母序前 6 行推了整个分布。** 写「`today_relvol` 都在第 4~5 位小数上变」时，
我看的是 AIAI/AMLX/ANDG/ANF/ASST/ASTL —— 实际最大是 HTHT **+46.4%**。
同形状：`pitfall_first_element_in_a_multi_element_page`。

**第三个：把列名当语义。** 工具写完想推广，扫到 `shortlist_log` 有 `roster_streak`，名字正对上「计数器」，
照搬同一条规则 —— **60 个相邻对里 58 个「违规」**。不是归档坏了，是它数的根本不是场次
（`name_cards.py:112`，133 行里 117 行是 0）。已写进研究档 §八，并把工具收紧成只指一个归档。

**还有一条更该记**：我 09-06 写「09-02 在文件内部完美自洽，任何只看一个文件的检查都判绿」——
**那是把我当时用的那几个检查的边界，说成了数据的性质。** 计数器在文件内部就报得出来。

⚠️ **这个形状我数到第 3 次了**（`gate_role` 预注册 D3「块翻符号报不出阳性，它的 NULL 是检验的
性质不是数据的性质」· `pitfall_a_stricter_test_can_be_blind` · 本轮）。按三次律，
它该从 memory 升成机制。**提案（不擅自改宪法）**：凡写「X 看不见 / 查不出 / 无法判定」，
必须同时写清**是哪一类检查看不见**，以及**换一类检查能不能看见** —— 一句「我只试了值的一致性，
没试方向性」就够，但不能省。

**② 哪条规矩帮了/碍了**

**帮了**：「先读已有规划再开口」。我本来要开 INBOX 里 08-24 那条「闸是仓位闸还是选股闸」的研究，
一查 `gate_role_2026-08/` —— **08-29 已经做完，还被三个复核 agent 推翻重写过一次**。
省了一整晚，且避免了平行造稿。

**碍了（只提，不改宪法）**：safe-merge 白名单收 `pipeline/tools/audit_*` 却不收 `pipeline/tools/` 其余，
于是 `fbclock` 那 3 行时钟修复第 4 晚还在等人。它挡着 614 条测试进 CI，main 上那条测试红了 12 天。
**这是路由问题被当成了授权问题**（CLAUDE.md 08-28 那条正是说这个），但按我的任务书我只能列不能合。

**③ 下轮第一件事**

~~确认 `universe_quality` 那两列~~ —— **今晚就查了，不适用**（见上）。
所以下轮第一件事换成：**看 09-02 的 36 行与 08-19 的 12 只有没有被 DATA ALEX 处理**；
没处理就在 §六.7 那条声明下追一行读数（声明是欠条，放三天就该有人问）。
