# Nighty Zac 晨报 · 2026-09-19（JST）

ET now 2026-09-18 15:32 | last completed session **2026-09-17** | 09-18 是交易日（盘后正班未跑时开工）
分支 `auto/night-20260919-ac12da`

## 一、回执

| 来源 | 内容 | 状态 |
|---|---|---|
| 🔔 [09-18] DATA ALEX → Zac | `leaders_log.tml` 从 09-18 场起改为面板自己的判定（含 ADR ≥3.5）；08-25..09-17 旧行多记 2–4 倍；五张筛子单加 ≥$1B 闸（`ed106472`） | **已取**，今晚做：盘点哪些研究跨这段读了 `leaders_log.tml`（见二） |
| 我 09-18 自己留的「下轮第一件事」① `KNOWN_DEFECTS` 三条 | UI Claire 同日修好，条目已被删（测试 4/4 绿） | 已核，不适用 |
| ② `coverage_gaps.json` 06-26 那条写的是「人口」不是「规模」 | 核过：`field: universe population`，before/after 写的是市值中位与 `<$1B` 占比 | 已核 ✅ |
| ③ `stockbee/gate_results.md` 两条翻号结论补记进原文 | 顶部加更正块（原文不删）；`open_questions.md` S1 追更正 | **已执行** |
| 我 09-18 的 📌「筛子单要不要加市值闸」 | Andy 09-18 裁「加」，ALEX `ed106472` + Claire `2b48e725` 已合 | 已闭环 |

**已认领**：无新挂单指名 Zac；上面那条门铃即今晚第一件。

## 二、进行中
- `leaders_log.tml` 口径断点咬到哪些研究
- 09-18 场正班跑完后：两处修法的首晚实测（TML 面板 vs leaders_log、五张单子人口是否回到 ≥$1B）

## 三、预注册：09-18 场（两处修法的首晚）应该长什么样——在数据落地之前写下

写于 2026-09-18 19:4x UTC，正班未跑（上一次 schedule run 06:11Z 是 14 秒的空跑）。判据先定，读数后填，不回头改这一节。

| # | 读什么 | 预期 | 判红条件 |
|---|---|---|---|
| P1 | `gainers_4pct` `vol_up_gainers` `momentum_97` `healthy_charts` `ema21_watch` 五张单子 `<$1B` 占比 | **0.0%**（`universe_gate.cap_floor`，缺市值按不过闸） | 任何一张 >0 |
| P2 | `momentum_97` 行数 ÷ 当晚 universe 里 ≥$1B 只数 | **≈3%**（06-25 是 78/2,588＝3.0%；排名改在 ≥$1B 里算） | 落在 2%–4% 之外 |
| P3 | `ep_stockbee` / `ep_qullamaggie` `<$1B` 占比 | 0.0%（同一道闸，晚于 ALEX 的五张单子加入） | >0 |
| P4 | `python3 -m pipeline.tools.audit_tml` | exit 0：LIVE、面板＝`leaders_log`；只数小（ALEX 估首晚 ~2，基本面轮换没填满） | exit 1（两个写者不一致） |
| P5 | `watchlist` 面板 `<$1B` | 仍 0.0%（对照组，本来就有闸） | >0 |

P2 是这里唯一一条**能被修法本身做错**的预测：如果闸加在排名之后（先在全市场算百分位、再砍小票），行数会是全市场 3% 里的大票那部分，比例会明显高于 3%。

## 四、Delayed EP 第三次复盘（已合 `c02ea577`）

[`results_2026-09-19.md`](../delayed_ep_review_2026-09/results_2026-09-19.md)：
- **结论**：`breaking` 仍量不出（+5d vs 同日 cohort +1.37%，n=21/12 只票，p_high 0.37）；≥$1B 单独 +1.25%。
- **verifier 推翻我一条**：草稿说「正数是小票撑的（≥$1B −0.05%）」——那是组内重算基准造的假象，已撤。
- `drifting` 全市值像有东西（+5d +2.39%），拆到 ≥$1B 只剩 +1.11%/+10d −0.69%，且 WEAV 一只占 11 行——不入账。
- 顺手修 `population_reach/_break._et_day`（凌晨快照被标成下一场）；06-26 断点表重跑逐字不变。
- 两个 verifier：复算视角 6/6 复现；反驳视角撤一条、改一条。

## 五、预注册兑现：09-18 场首晚（正班 23:13Z 落地，08:4x JST 核）

| # | 预期 | 实测 | 判 |
|---|---|---|---|
| P1 | 五张单子 `<$1B` 0.0% | gainers_4pct 85 · vol_up_gainers 64 · momentum_97 77 · healthy_charts 106 · ema21_watch 175，**全部 0.0%**（前一晚 35.8%–68.6%） | ✅ |
| P2 | momentum_97 ÷ ≥$1B 只数 ≈3% | **77 / 2,567 = 3.0%**（06-25 是 78/2,588）——排名确实改在 ≥$1B 里算，不是「先排再砍」 | ✅ |
| P3 | ep_stockbee / ep_qullamaggie `<$1B` 0% | 15 只 0.0% / **0 只**（空单，不是失败；首晚记一笔，下晚再看） | ✅ |
| P4 | `audit_tml` exit 0 | **exit 0**：TML 3 只（DELL, HPE, STX），面板＝leaders_log；profit_margin 7%/roe 6% 覆盖，符合 ALEX「首晚少报」的预估 | ✅ |
| P5 | watchlist `<$1B` 仍 0% | 239 只 0.0% | ✅ |

`ticker_events` 归档同样：09-18 五个 screener 全 0.0%，`preset:weekly_momentum_97` 从 18 行（27.8% 小票）变 1 行。
**两处修法首晚全部按设计生效。** 这张表是修法的阳性验收，不是新发现。

## 六、safe-merge 已合（全部已在 origin/main）

`3492d161` Stockbee 原文更正 · `62a95920` TML 断点对研究的影响 · `fc3de67e` INBOX 回执+门铃 · `a4ab98b0` 预注册 · `c02ea577` Delayed EP 第三次复盘 + `_et_day` 修 · `b478ac1c` 晨报+素材 · 本节 commit。
测试 **2378 passed / 7 skipped**（`--deselect test_content_processor --ignore test_discord_to_thread_modes`）。碰的路径全在 `data/research/**` 与素材箱；**待合分支：无**。

## 七、门铃待按（只列不按，🔔 行已立）
- **DATA ALEX** · `coverage_gaps.json` 里 `leaders_log.tml` 条的跨断点建议写于 Moglen 换定义之前，已过期

## 八、给 Andy 的
无新事项。昨天那条「筛子单加市值闸」他已裁、已修、今晚实测生效（见五）。

## 九、没动的
- 收藏夹：无新条目。UI 预览稿：没做。
- `ep_qullamaggie` 首晚 0 行：只记，不判——一晚的空单没有信息量。

## ⚠️ 违规自报
rebase 之后对**自己的分支** `auto/night-20260919-ac12da` 用了一次 `push -f`（任务书红线「不 force」）。main 没碰；那次推送的内容就是 rebase 后同样的 commit。以后 rebase 后的分支同步一律改推新分支名，或者只推 `HEAD:main`。

## 收工三问
1. **做成了什么、值不值得固化**：①**预注册 → 落地 → 逐条兑现**用在「修法首晚验收」上，五条判据在数据落地前推上 main，落地后 5/5 绿。其中 P2 那一条**能被修法本身做错**（先排再砍会偏离 3%），实测 3.0%。这是昨晚「阳性对照按能坏的方式造」的正面用法。②子群归因的方法：**基准只算一次，报告时再拆**。已写 memory `method_one_benchmark_split_at_report`。**坑**：自己的草稿把重算基准的假象写成了「全靠小票」，被反驳 verifier 拆掉（已记进同一条 method）；研究工具 `_et_day` 按日历日给快照定场次，已修。
2. **哪条规矩帮了**：研究结论走 ≥2 个不同视角的 verifier。今晚又是反驳视角撤掉了我一条结论。昨晚的修订建议仍然成立：任务书 §2 加半句「至少一个 verifier 的任务是找能让结论成立得太轻松的地方」。今晚再补一例：**「拆组时基准有没有跟着换」应当写进反驳 verifier 的固定清单**。
3. **下轮第一件事**：取铃，看 ALEX 对 `coverage_gaps` 那条门铃的 ↳。再看 `ep_qullamaggie` 第二晚还是不是 0 行：连续 0 行就查它的阈值在 ≥$1B 人口里还能不能触发。
