# 夜间研究班晨报 · 2026-09-20（JST）· linda

ET now 2026-09-19 15:30（**周六**）| last completed session **2026-09-18**（周五）| 今天不是交易日
任务 `T-0920-10` · gate=none · 无分支，直推 main

> **本夜没有新的交易日**。ET 周六，最近一场仍是 09-18——而 09-18 那场已被 09-19 班逐条验收过（预注册 5/5 绿）。
> 所以今晚不做「数据落地核对」，做只需要历史就能做的那一件：把上一班留下的那个 0 查到底。

## 一、回执

| 来源 | 内容 | 状态 |
|---|---|---|
| 取铃 `doorbells --to "RND Linda"` / `--to "Nighty Zac"` | **open: 0**，两条线名下的门铃全部已闭（09-17 的 13 个主树残留文件、METRIC_SOURCES 的 FTD 节、半字母表重报、regime_ledger 09-16 补写、regime 47/63/75 定标）| 已核，无新铃 |
| 任务板 `list --owner linda` | 名下只有本任务 open；`T-0919-23`（窗口外触发）blocked、`T-0919-32`（pytest collection）已 closed | 已核 |
| 我 09-19 的「下轮第一件事」① ALEX 对 `coverage_gaps` 门铃的 ↳ | INBOX L2952 那条 🔔 **仍无 ↳**（挂 ~1 天，未到 48h 滞留线）| 只记账，不催 |
| 我 09-19 的「下轮第一件事」② `ep_qullamaggie` 第二晚还是不是 0 行 | **无第二晚可看**（09-19 周六休市）。改做它的历史底：见二 | 已改做 |
| `feat/linda-regime-caveat-0918` | 已合进 main（`76819e4f` 尾部校验文案 + `43ca3a2c` 卖压改口径后复核），远端无此分支 | 已闭环 |

**已认领**：无新挂单指名本线。

## 二、进行中

- `ep_qullamaggie` 首晚 0 行：是**阈值在 ≥$1B 人口里打不着**，还是**这一晚本来就没有**？走历史底基率，不看一晚。
  → **已结**，见三.1。
- 上一件被反驳 verifier 逼出的那道私有闸（「时钟对了货可以不对」），搬成常驻闸。
  → **已结**，见三.2。

> ⚠️ 本夜分两段跑：04:30 那班在写完三.1 之后被 worker timeout 释放（任务板 `RELEASED worker timeout`），
> 05:16 本班接着干。三.1 的 commit 是上一段留下的，本班没有重做。

## 三、今晚做了什么

### 1. `ep_qullamaggie` 的 0 行是常态不是故障 —— `c6d46b81`

25 场里 58 只命中，**中位 2、均值 2.32，印 0 行 5/25 = 20%**。09-18 当晚 ≥$1B 里
gap≥10% 的票是 0 只，gap 腿一只都没过。**不必开工单。**
`gap_pct`/`avg_vol50_prev` 是 09-18 当晚才进 `universe.json` 的，之前 129 场覆盖率全 0.0，
原生回放那 130 个零是字段没出生、不是市场没跳空；改用
`(1+change_pct)/(1+from_open_pct)-1` 重建 gap 腿，09-18 阳性对照逐票零分歧 23/23 与 0/0。
研究件 [`ep_qullamaggie_baserate_2026-09-20/`](../ep_qullamaggie_baserate_2026-09-20/README.md)。
留了一件没答案的：C 段命中在往下走（slope −0.144/场，r=−0.43，t=−2.28，n=25），
淡季对淡季 3.71 vs 1.43，分不开行情/季节/输入。判据已定：**10-13→11-13 若月均仍停在 2 就不是季节。**

### 2. 时点维的闸：盘前 payload 盖着 ok 章 —— `8e466eb2`（已合 main）

上一件里那道私有闸搬成 `pipeline/tools/audit_universe_freshness.py` + 16 条测试。
它是同一条家谱的第四节：数量维（`audit_archives`）→ 内容维（`shape`）→ 人口维（`population`）→ **时点维**。

**一句话**：管线给 2026-08-10 05:31 那份**盘前** payload 盖的章是 `quality.status: "ok"`，
给同周一份真收盘（`d2337e85`）盖的是 `"degraded"`。质检量的是「有没有值」，
从来没人量过「这批值是哪一刻的」。

| | |
|---|---|
| 尺子 | aggregate RVOL = `sum(volume)/sum(avg_volume)`，全宇宙加总 |
| 口径 | RVOL **逐票是标准指标**（ChartSchool / TradingView / Finviz，标准做法还带按时段校正）；**市场聚合版检索无公开口径 → 聚合方式与两条阈值明写自造** |
| 读数 | 156 份快照 / 141 场，**22 条违规**：F1 盘前 5 · F2 `avg_volume` 断供 8 · F4 整场无可用货 9 |
| 阈值 | 取自空档：143 份好货落 0.611–1.969，最近的坏值是 0.024 与 95.4；0.5/3.0 落在 25 倍与 48 倍宽的空档里，测试钉了「阈值挪一倍不改判」 |

两个量出来的设计决定：①**不加市值闸**——全宇宙口径与 ≥$1B 口径 156 份**逐份零分歧**，
去掉它让这把尺子对 06-26 那次人口换血免疫（分母跟着宇宙动的闸，会在出事时变哑）；
②**缺失值两边都记 0**——这是 F2 能成立的原因，剔掉缺失行它会安静地停在 1.0。

**它咬到谁**：5 个回放 git 历史的消费者逐个判。
`ticker_events.csv` **未受污染**（9 场 F4 零行、`preset:*` 重复键 0）；
`breadth_archive.csv` 也没脏——08-10 那行 `universe_size`=5618 正是盘前快照的行数，
但那份 payload 的 `change_pct` **一行都没有**、中位 `volume` **1 股**，
复算只能得 0/0 而归档写的是 548/647，所以只有行数那一列取自它，
**而行数恰恰是不随时刻变的量**。population 两家暴露但未受损（市值 05:31 和 21:03 一样）。

**留下的真问题有可复现证据**：`backfill_preset_hits.payload_disagrees()` 已经有两个时钟
（`timestamp` + 逐行 `bar_date`），把三份盘前 payload 按**它们真会拿到的标签**喂进去，
**三份全部返回 `None`＝接受**，新闸全判 F1。因为它们的时钟本来就是对的。

**互证**：`coverage_gaps.json` 的 `ticker_events.csv` 条目列了 12 个日期，
我的 9 场 F4 逐日落在里面。两条路径（缺口申报走行数、本闸走 RVOL）指向同一批日子。

研究件 [`universe_freshness_2026-09-20/`](../universe_freshness_2026-09-20/README.md)。

## 四、已合的 commit / 待合分支

| commit | 内容 | 落点 |
|---|---|---|
| `d0dbc149` | 晨报开工（回执 + 进行中） | main |
| `c6d46b81` | `ep_qullamaggie` 历史底 | main |
| `8e466eb2` | `audit_universe_freshness` + 16 条测试 + 研究件 + `KNOWN_UNWIRED` 登记 | main |

**待合分支：无。** 本夜三个 commit 全部只碰白名单路径（`pipeline/tools/audit_*`、
`pipeline/tests/`、`data/research/`），任务板 `gate` 判 **none**，已直推 main 并核实在 `origin/main` 上。

**测试**：`pipeline/tests` + `tests` 两个根，**3094 passed / 1 skipped**
（deselect `test_content_processor`）。新闸首次跑全套时被 `audit_wiring` 的 W1 棘轮当场判红
（「新闸没有触发点」）——**那是它按设计工作**，已按 `audit_ci_test_coverage` 的先例
登记进 `KNOWN_UNWIRED`（owner DATA ALEX），W2 会在有人接上的那天逼着删掉那条欠条。

## 五、收藏夹判定清单

**本夜无新条目。** INBOX 🔗 节现有 8 条**全部已 ✅ 处理**（最近一条 09-12 的 LanceB/Muninn 合判），
按任务书「有新条目才做」跳过 1.5 节。

## 六、预览稿分数表

**本夜没做 UI 预览稿**（2.5 节），理由写在明处：本夜的两件都在可靠性线上，
时间花在把一道私有闸变成常驻闸 + 把它咬到谁逐个量清。**没做就是没做，不补一张凑数的表。**

## 七、转给别人的 / 建议 Andy 决定的

**转给 DATA ALEX 两件**（任务板已开，不是门铃）：

| 任务 | 一句话 |
|---|---|
| `T-0920-11` · wiring | 把 `audit_universe_freshness` 接进夜班——写完 `universe.json` 后自查一次。它比人口闸便宜得多：两个加总、不回看历史，接进去是真正的三行 |
| `T-0920-12` · bug | `oratnek_diff.py:53-64` 取「`asof` 之后最近一份」，当晚没提交就会取到次日清晨那份盘前货；顺带判 `backfill_preset_hits` 的双时钟闸要不要一起加这一条 |

**需要 Andy 拍板的事：无。** 本夜没有「只差他一步」的交付，
所以 INBOX『📌 给 Andy 的待办』本夜不追行（验收该项写**不适用**）。

## 八、没动的大件 / 下轮第一件事

- `oratnek_diff` 那个暴露**没结案**：它实际被调用的日期仓库里没有记录，
  没法回溯判它有没有真取到过坏货。已整包交给 ALEX（`T-0920-12`），本线不并行修别人的文件。
- 09-19 挂给 ALEX 的 `coverage_gaps.json` note 过期门铃（INBOX L2950）**仍无 ↳**，
  挂约 1 天，**未到 48h 滞留线**——只记账，不催。
  本班取铃 `doorbells --to "RND Linda"` → **open: 0**，本线名下无未取件。
- **下轮第一件事**：①核 `T-0920-11`/`T-0920-12` 有没有被 ALEX 领走；
  ②`ep_qullamaggie` 的 C 段下滑到 10-13 才有判据，中间不碰，别提前下结论。
