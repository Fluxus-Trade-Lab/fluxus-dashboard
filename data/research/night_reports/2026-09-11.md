# 夜间组晨报 · 2026-09-11（JST）

**时钟**：ET now 2026-09-10 15:32（盘中，未收盘）· last completed session **2026-09-09** · today is trading day True
**分支**：`auto/night-20260911-53dc06` · ⚠️ **收尾在窗口外（10:2x JST），Andy 明说 override 后才做**；只收尾，没开新活。

---

## 〇、回执

- **09-10 收工三问③「下轮第一件事」**：✅ 已执行。开工第一个动作就是读 `DATA_RELIABILITY §六`，然后跑 `audit_ci_test_coverage`：
  **614 条仍不在任何自动运行里**，没变（仓库测试函数 1,988 → 2,242），也没有 T2。
- **OPS 09-11 ① `.github/workflows/` 归 OPS 主管**：✅ 已知悉。所以「CI pytest 改宽」那张挂单现在有主人了，挂给 OPS，今夜没人动。
- **OPS 09-11 ③ `collection.md → authority-clips.md` 由 Zac 点名、Steve 搬**：✅ 认领，**今夜没做，今晚第一件就做**（点名 08-31 之后的判定条目）。
- **§六.7 欠账（归 DATA ALEX）**：`audit_progress` 5 条、`audit_event_agreement` 声明原样，**第 5 天没人动**。今夜不再重报，§六.9 又加了一条新欠账。

## 一、今夜主活：拿别家的 K 线核对归档，逮到 08-07

- **闸** `pipeline/tools/audit_events_vs_bars.py`：拿 `ticker_events`（Finviz）的 change_pct 对本地 `ohlc_2y`（yfinance），
  **两家厂商各取各的数**。补的是 §六.8 说的「`ticker_events` 上带宽为 0」那个口子。
- **分辨率**：110 个可判日里 108 天 ≥0.95，最差干净日 0.956；坏日 0.014 / 0.328。判定线 0.90 放在这两者之间的空档里。
  6 天判「查不了」，不判绿。覆盖 13%，明写它只量整场级别的错。
- **新发现 2026-08-07**：72/72 配 08-06 的 K 线，0/72 配当日；volume 复述。**机制**：预设回填按 UTC 日历给快照定日期，
  08-06 21:08 ET 提交的 `69754ed3` 被记成了 08-07。改成按 ET 挑会换掉 36 个日期，只有 08-07 真写坏了。
- **给 08-17 补反证**：preset 行配不上任何一天，所以「携带 08-14 读数」没有外部证据。撤行的动作照旧。
- **验证**：16 测试（含往真归档里注入一场重放的真阳性对照），6/6 变异体被杀；有一条测试逮到我自己的一个真 bug
  （默认参数 `= STORE_DIR` 在 def 时就绑死了，monkeypatch 不生效，空库测试会读到真库，给出假绿）。全套 1778 passed。
- 研究档 [`events_vs_bars_2026-09/results.md`](../events_vs_bars_2026-09/results.md) · `DATA_RELIABILITY §六.9` · INBOX 工单 → DATA ALEX · 素材箱一行。

## 二、safe-merge

全部在白名单内（`pipeline/tools/audit_*` + `pipeline/tests/**` 新增 + `data/research/**` + §六 追行 + INBOX + 素材箱），收尾时 rebase 后自己合进 main。commit 见汇报。

## 三、门铃待按（只列不按）

- **→ DATA ALEX**：INBOX [2026-09-11] 工单，08-07 的 287 行 + `snapshot_dates` 按 ET 挑（不改的话下次回填还会再写坏）。
- **→ OPS Fable**（`.github/workflows/` 新主人）：CI 仍漏 614 条测试，改法见 §六.6「仍欠」四条。

## 四、没动的

收藏夹 [09-10] LanceB + Muninn 两条没判（Steve 代录）· authority-clips 待搬点名 · UI 预览稿。三件都挪到今晚。

## 五、收工三问

**① 踩了什么坑**：一是默认参数在 def 时绑死模块常量，monkeypatch 改不到它，于是「空库」测试静默读到真库，差点给出假绿。测试逮住了，修法写进了 docstring。
二是我拿 `git diff --stat` 去验证一个**未跟踪**文件有没有还原干净，那个命令对未跟踪文件永远是空的，等于什么都没看。改用 `diff` 对原件。这就是 [[pitfall_a_pathspec_that_matches_nothing_looks_clean]] 同一个形状，已经踩够三次，**建议升机制**：变异测试的还原检查改用 `cmp` 对备份。
三是 rebase 之后，我自己那条真数据阳性对照红了：cron 已经写进 09-10，K 线库还停在 09-09，那一场「查不了」，注入进去自然不会报。修法是把注入挪到 K 线库覆盖得到的最新一场，并补一道变异验证它还能红。这条也写进了闸的第 5 个盲区：**最该在写入当晚查的那一场，恰好最可能没有 K 线。**
四是时间盒跑过了窗口，被打断后到 10:21 才续上。

**② 规矩**：「先量分辨率再信阴性」帮了大忙。有了噪声底 0.10–0.14，我才敢说 08-17「配不上任何一天」，
也才知道一个 60 种组合搜出来的 14/99 算不上发现。碍事的没有。

**③ 下轮第一件事**：先给 authority-clips 点名（OPS 裁决，已认领），然后判收藏夹两条，再看 DATA ALEX 有没有接 08-07。

skill-none: 本轮无适用 skill
