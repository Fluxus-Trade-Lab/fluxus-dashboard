# 夜间研究班 · 2026-09-27（JST）

> ET now **2026-09-26 15:31**（Sat）· last completed session **2026-09-25**（Fri）· today is trading day **False**
> 任务 `T-0927-08` · 时间盒 150 分钟 · 开工 04:30 JST（窗口内）

## 一、回执

| 上一轮留下的 | 状态 |
|---|---|
| `needs-andy` 挂起 | **无**——09-26 班晨报明写「只差 Andy 一步的交付：无」 |
| `handoff` 转出 | 无 |
| 任务板 linda 名下 open 单 | **零**（`taskboard.py list --owner linda`：除本单外全部 done/closed） |
| 09-26 复核员挑出的计数错（22 函数 / 25 用例） | 已执行——正文已在 `8e665b65` 改正，commit message 按第 10 条禁 force 留原样 |

## 二、今晨做了什么

一条线索串三件事：**账上的读数会过期，而过期的读数会让你选错今晚的题。**
09-26 的晨报把三个数留给今晨，写着「先复算再动手」。复算结果：一个对、一个分子分母双错、一个真洞清零。

研究档全文（含逐条判词与复跑命令）：
[`data/research/audit_mutation_2026-09-27.md`](../audit_mutation_2026-09-27.md)

### ① `audit_ci_test_coverage`：09-26 留账的三个真洞补掉 → 96%

**154/165 → 158/165（93% → 96%）。** 三条测试，各杀一个变异体，
**每一个都先在变异体上跑过红再写下来**（不是「读自己那个常量」）：

| 变异 | 它坏掉的是什么 |
|---|---|
| L506 `And->Or` + L506 `In->NotIn` | 一条杀两个：`skipif` 里没有向机器提问时 `history_gated` 必须是 `False` |
| L501 `GtE->Gt` | `@pytest.mark` 后面什么都不跟时，变异体在下一行 IndexError——一道闸为一个畸形 decorator 抛异常，等于对另外三千多个测试零报告 |
| L699 `3->4` | 恰好四条是 `> 3` 与 `> 4` 唯一分家的宽度（09-26 量的是 7 和 3） |

⚠️ **已有的那条正向测试从哪个方向都看不见这两个变异**：
`skipif(not _has("abc"), ...)` 对 `In -> NotIn` 两边都绿——`any(h not in seg)`
只在 seg 同时含上**全部** hint 时才为假，真实条件永远不会。
**一条只从「该报阳」那一侧写的测试，对「不该报阳」那一侧是瞎的**，
而这里不该报阳才是贵的方向：`skipif(False)` 被记成 `history_gated`，
就会被「浅 checkout 跳过了」写销账，从此不是任何人的问题。

剩 7 个全是 09-26 已逐条判过的等价 / 帮助文本 / 造不出真实 YAML——**全库最大那道闸真洞清零**。

### ② 复算两个旧读数：一个对，一个分子分母都错

| 闸 | 账上（09-02 读数） | 今晨复算 | 判 |
|---|---|---|---|
| `audit_universe_shape` | 63% | **31/49 = 63%** | ✅ 逐位一致 |
| `audit_calendar_gaps` | 61%（92 站点） | **62/99 = 63%** | ⚠️ 分子分母都过期 |

**三晚连着第三次转抄的读数不成立**：09-24 数字错（53% 实为 66%/57%）·
09-26 站点数错（162 实为 174）· 今晨两个都错。量一次五分钟，选错一晚的题贵得多。

### ③ `audit_calendar_gaps` 63% → 87%：给两个半函数建首份测试

37 个存活点里 19 个长在 `read_archive_dates()` 与 `main()` 上——**各一条测试都没有**，
整条 `--archive` 入口路径加上退出码。

⭐ **L423 `return 0 if out["ok"] else 1` 是同形第四次**
（`audit_archives` / `audit_ledger` / `audit_ci_test_coverage` 各活过一次，09-25、09-26 补掉）。
它坏的是安静那条路：每个干净的夜晚 CI 都 exit 1，仓库没毛病，
而**人学会的修法是不再看这个检查**。这次钉住，不再记账了事。

| 批次 | 测试数 | 杀死率 |
|---|---|---|
| 开工（origin/main） | 50 | 62/99 = **63%** |
| + `read_archive_dates` 10 条 | 63 | |
| + `main()` 11 条 | 74 | 80/99 = **81%** |
| + `fetch()` 6 条 | **80** | 86/99 = **87%** |

`read_archive_dates` 那六个坏法**全是静默的**，最贵的一个：
`c in rows[0]` 翻成 `not in` 后第一个候选永远胜出，col 变成文件里没有的列，
`r.get(col)` 每行都是 None，**整份归档静默读成空集** → D1 对我们写过的每一场都报红。

`fetch()` 值得单说：`classify_bar` 09-02 被抬出 `fetch()` 就是为了能被测到，它现在一个存活点都没有——
**但接住它判词的那三行还是自由的。L343/L345 任一翻转，每个 placeholder 都进 `good`、真会话被整条丢掉**，
2026-09-01 把 FBRX 的 `O=H=L=C / Volume=0` 读成「唯一还有数据的票」那个错，
在修好它的那一层**上面一层**原样重演。
修法不是上网：`monkeypatch.setitem(sys.modules, "yfinance", 桩)` + 真 pandas DataFrame——
**桩给的是厂商的形状，不是厂商。**

剩 13 个逐条归类：`reconcile` 的算术 **4 个真洞可达**（L203/L250/L257/L258，下轮第一件）·
格式与精度 6 个 · `progress`/`threads` 两个纯配置 ·
L159 `else 0.0` **按构造不可达**（到得了那行就意味着 `missing` 非空，而 `missing ⊆ tickers`，所以 `n ≥ 1`）——
它是一道除零守卫，**为了刷杀死率去删它是错的交易**，留着并记账。

## 三、收尾

**测试**：`pipeline/tests` + `tests` 两根一起跑，**3681 passed / 1 skipped / 12 deselected**（6 分 32 秒）。
本轮新增 **33 个用例 / 30 个测试函数**；`test_audit_calendar_gaps.py` 50 → 80，
`test_audit_ci_test_coverage.py` 98 → 101。

**已合的 commit**（gate 判 **none**——只碰 `pipeline/tests/`、`data/research/`
与 append-only 公箱 `material_inbox.md`，全在 linda 可直合路径内；
**未删 main 上任何可执行行**，源码一行没动，本轮只加测试）：

| commit | 内容 |
|---|---|
| `45348d151` | 本晨报开工（回执 + 进行中占位） |
| `4933585be` | `audit_ci_test_coverage` 补 09-26 留账的三个真洞 |
| `07c4e98c6` | `audit_calendar_gaps`：`read_archive_dates` + `main()` 首份测试 |
| `f21940c72` | `audit_calendar_gaps`：`fetch()` 首份测试（yfinance 桩） |
| `aaa1b5c4f` | 本晨报收尾 + 研究档 + 素材一行 |

⚠️ 本轮 **push 前没有 rebase**（`origin/main` 自开工起未前进），
所以上表五个 sha 就是 `git log origin/main` 现场读到的号，
不是分支上那个会随 rebase 作废的（09-23 栽过一次，素材箱链接点成 404）。

**待合分支**：**无**（gate=none，五个 commit 全部直推 `origin/main`）。

**收藏夹判定清单**：INBOX 🔗 节最后一条是 09-12，已 ✅，**本轮无新条目**，不适用。

**预览稿分数表**：本夜未做 UI 预览稿（2.5 节）。时间给了上面三件——
09-26 把「先复算再动手」写成下轮第一件事，复算当场证明账上有一个数分子分母都错，
而那个数正是「下一个该修哪道闸」的唯一依据。

**素材箱**：已追一行。

**只差 Andy 一步的交付**：**无。**（故 `needs-andy` 与 INBOX『📌 给 Andy 的待办』两处均不适用。）

**没动的大件**：`audit_universe_shape` 18 个存活点（09-02 起就写着「没人逐条读过」，今晨只复算了总数没逐条读）·
`DATA_RELIABILITY.md` §六第 6 条那四条欠账（都要动 `.github/workflows/tests.yml`，不在本线边界）。

**下轮第一件事**：`audit_calendar_gaps` 的 `reconcile` 四个真洞（L203 `LtE->Lt` 的窗口边界 ·
L250 空样本早退里的 `tickers: 0` · L257 `have_feed` 的分母 · L258 `feed_has` 的阈值方向）。
然后逐条读 `audit_universe_shape` 那 18 个——它 09-02 至今三周没人读过，
而今晨证明了**三周前的读数连总数都会错**。
