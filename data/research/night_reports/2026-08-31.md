# Nighty Zac 晨报 · 2026-08-31（JST）

**时钟**：ET now 2026-08-30 15:32 | last completed session **2026-08-28** | today is trading day False
**分支**：`auto/night-20260831-2a66fb` · **窗口**：04:32 JST 开工（窗口内）

> ⚠️ **给 Andy 的一句话**：你 08-31 定的那条「有可以验证的吗？没有就删除」，我拿「金九银十」跑了一遍——
> **银十可以关掉，金九关不掉**（它在长样本里是黄金第 2 强的月份，但从来没到过显著）。
> **更值钱的不是这个读数，是我验错了两次然后被三个 verifier 抓住的过程**（→ ②·2）。
> 另有**一件需要你或 UI 线动手的**：Breadth 页此刻正在显示我们自己判定为反向误导的那个数（→ ②·4）。

---

## ① 回执

- **[2026-08-30 Andy 立项 · Steve 线代挂] 喜剧与说唱的语言技法研究** → **✅ 已认领并交付第一轮**（②·1）。
  ⚠️ **卡在你身上**：任务书的通过线是「Andy 盲选 ≥7/10」，盲选还没发生 → 两张卷子在 ②·1，要你 20 分钟。
- **[2026-08-31 §七 挂单 → 研究线（Zac／RND Linda）] 金9银10 预注册季节性检验** → **✅ 已认领并交付**（②·2）。
- **[2026-08-30 收藏] threeui.com** → **✅ 已判定并入册**（④）。🔗 节现在零条未处理。
- **[2026-08-29 Joe → Zac] 回归闸** → 我 08-30 夜已交付并自合（`0f5961ec` / `b7847c08`），本条闭环，无新动作。
- **[2026-08-30 OPS 挂单 ×3]** ①GAS writing 拉取 → 数据端；②Discord briefing 归集器 → Gary；
  ③WHAT CHANGED 检测器 → **我 08-30 夜已交付**。本轮无遗留。
- **INBOX / §七 / §12 其余条目**：无标「建议 Zac」或无主而属我地盘的新挂单。

---

## ② 本夜做了什么（4 件）

### 1【待合】喜剧与说唱的语言技法研究 · 第一轮

`data/research/comedy_rap_2026-08/`（[README](../comedy_rap_2026-08/README.md)）

**交付**：名单 8+8+Rubin **及我用的判据**（含**被判据挡在外面的人**——挡不住人的判据不是判据）·
**15 条 device**，每条含机制/可执行动作/用在哪/**怎么证伪** · Rubin 的「**删什么**」七问 ·
**「试过但不该用的」6 条** · **20 组盲选卷**。

**⚠️ 要你 20 分钟**（别看别的文件，答案卷会污染盲选）：
- [`BLIND_A_compression.md`](../comedy_rap_2026-08/BLIND_A_compression.md) —— 10 组两版选一版
- [`BLIND_B_closings.md`](../comedy_rap_2026-08/BLIND_B_closings.md) —— 10 个收口位四选一

回 20 个字母即可。

**设计上我做了一件事，请你知道**：如果只交「原句 vs 我的改写」，你大概率整体偏好改写，
**那什么也没证明**——只证明了「重写过的句子更好」。所以压缩卷是 **6 + 4**：6 组是原文在做说明的段落
（预测改写胜），**4 组是你本来就一句一画面的句子，是对照组**（预测原句胜）。
**预测已封存在 commit 里。** 若两组都是改写胜，说明赢的是「重写」不是技法，下一轮必须改设计。
收口卷另有一个**校准位**测「这份研究对你偏好的读法本身」，位置我不说（说了就是给答案）。

**不依赖盲选就成立的三条**：
- **「比喻优先于数据」有边界，现在能写成一句可执行的话**：C6 是唯一没过 −30% 词数线的（**9%**），
  因为那段的宽度数字**是判断的证据不是修饰**。→ 建议 §4.8 第 3 条改成
  **「比喻优先于当修饰用的数据。当证据用的数据不参与比喻，它换个位置。」**（Voice Bible 是 Steve/Q 的文件，已列门铃。）
- **你已经在用其中 6 条 device，只是没有名字。** 这份研究不是给你换写法，是把你已经会的写成 Steve/Q 能复现的动作。
- **减法清单里有一条在你身上零命中**（「删限定词」在 1,678 词里一个 `basically`/`arguably` 都没有）——如实记录。

**三处没照任务书做**（README §四逐条）：不交语料（不复制歌词与逐字稿）· 不追进 `Fluxus_Swipe_File.md`
（A/B 未判 + 那是 Steve 的地盘）· 你给的那条 Instagram reel 需登录，没看。

### 2【已合】「金9银10」预注册检验 —— 银十可以关，金九关不掉

`data/research/gold_seasonality_2026-08/`（[results.md](../gold_seasonality_2026-08/results.md)）·
预注册封存于 `b8e25a22`（**跑第一行计算之前**）

| | 结论 |
|---|---|
| **银十** | 1968 年以来**任何窗口任何口径都排不进前五**。可按「验过，没有」处理 |
| **金九** | Pink Sheet 四个长窗口里**都是黄金第 2 强的月份**、超额恒为正，**但一次都没显著**（最好 p_raw 0.082） |
| **本义** | 这句话中文里**不是讲金属的**——是销售旺季（秋收+中秋国庆）。借用到金价上是后起用法，**没有价格证据** |

**⭐ 但过程比结论值钱：按宪法派了三个不同视角的 verifier（统计/代码/反驳），他们各自打穿了我两条结论。**

- 我原本用「**玉米 12 月在多重比较校正后仍然显著**」来证明「这个 NULL 不是检验瞎了」——
  **那是 Yahoo 连续合约换月的跳空伪影**，12/13–16 四天贡献了 12 月均值的四成，剔掉换月窗后
  Holm 从 0.038 变 **0.62**。**我拿一个数据构造伪影当了阳性对照。**
- 我把「标普九月」报成全研究唯一存活的显著格——**三处独立扰动各自都能杀死它**
  （换保留自相关的零分布 / 砍到 1990 年后 / 剔掉 1930 年代最差的三个九月）。
- 预注册里我写「2000–2026 是**最长可得**」——**假的**，那只是 yfinance 上最长。
  World Bank Pink Sheet 1960 年起免费月度，而**被我切掉的 1975–1999 恰是金九最强的 25 年**。
- v1 头条那个「−0.24pp / 排第 8」**是月末价口径的产物**：同年代换成月均价是 **+0.56pp / 排第 3**。

**然后我在修的过程中又撞了一次同一堵墙**：换上更严格的循环平移检验之后，
它只有 310 次旋转、最小可能 p = 0.0032，**乘 24 之后 0.077——在看到任何数据之前就越过了 0.05**。
这正是我 08-29 自己记下的 `pitfall_a_stricter_test_can_be_blind` 的字面重演。
改用 2 格校正（这句老话**事先就点名了两格**，不是我们捞出来的）后地板才可用：
**金九能测出 +4pp、测不出 +3pp；银十连 +4pp 都测不出。**

顺带修了一个代码缺陷（带两向阳性对照）：判「最后一个月走完没有」原写成 `last_day < me.index[-1]`，
**月末落在周末的完整月会被整月丢掉**（2020-05 实测被误删）。
并补跑了预注册预算里漏掉的变体 5/6，都没翻号。`claims.jsonl` 登记 2 条 null。

### 3【已合】收藏夹：threeui.com 判定

见 ④。

### 4【已合】UI 预览稿：Breadth 页正在显示我们自己判定为反向误导的那个数

`data/research/ui_previews/2026-08-31/`（[README](../ui_previews/2026-08-31/README.md)）·
四稿 + 两轮迭代，**v4b 六条满分 12/12**

08-31 数据端换成标准普通股口径（`b1c7d907`），前端仍读原始计数。同一天 08-28：

| 口径 | 新高 | 新低 | 净 |
|---|---|---|---|
| 原始（页面现在显示的） | **68** | 19 | **+49** ← 读起来偏多 |
| 标准普通股 | **8** | 18 | **−10** ← 偏空 |

**符号翻转，而 Market State Summary 那四格里根本没有这一项。**
⚠️ **n=1**：归档 100 行只有 08-28 一行有标准口径读数（回填未铺开），所以「翻转」是一次观测不是规律。
`frontend/` 一个字节没改；零新增颜色（HTML 里十六进制色值命中 0）；对比度**未测量**（不写成「通过」）。

---

## ③ safe-merge 已合 / 待合分支

**本夜全部改动都在 `data/research/**` 与 `Fluxus_Brand/ops/material_inbox.md` 内**（`git diff --name-only origin/main...HEAD` 逐目录核过），
在 safe-merge 白名单内，**已自合进 main**。**没有待合分支。**

### 已合 commit（全部已核实在 `origin/main` 上）

| commit | 内容 |
|---|---|
| `4678b005` | 喜剧/说唱技法第一轮 —— 15 条 device + 20 组盲选卷，预测先封存 |
| `ca6453fe` | 金9银10 **预注册**（跑第一行计算之前） |
| `3be62750` | threeui.com 收藏判定 |
| `de1bc58c` | Breadth 预览稿 v4b 12/12 |
| `4f369689` | 金9银10 结果 —— 三个 verifier 推翻我两条结论 |
| `6fafe6f7` | 本晨报 + 素材箱两行 |

⚠️ **远端分支 `auto/night-20260831-2a66fb` 停在 rebase 之前的版本**（`52519404`）：
收工前 rebase 改写了历史，而红线不许 force push。**内容零损失——全部 6 个 commit 都已在 main 上**
（`git diff origin/main...HEAD` 为空已核过）。**那条远端分支可以直接删。**

⚠️ **`audit_unpushed` 报了 4 处未推工作，没有一处是我的**：
`auto/night-20260828-bfbf5c`（3 commit）· `auto/night-20260819`（1）·
`claude/friendly-chaplygin-b46c13`（160）· `design/marketing-visual`（16）。
前两条是旧夜间分支，后两条是长命工作树。**不归我处理，列在这里让它们有人看见。**

---

## ④ 收藏夹判定（🔗 节现在零条未处理）

**[08-30] threeui.com → ✅ 采纳，但只能用 Community 那一半。** 前端代录留的三个点全部查实：

1. **许可证是两套**：Community = **MIT**（须保留 item-specific attribution）；
   Pro = 付费非独占（**$99/年** 或 **$199 终身**，Stripe），**禁止把 Pro 源码当独立资产再分发**。
2. **是组件库不是教程站**（copy-ready 组件 + 模板 + Pro MCP），所以问题确实是「抄哪几个」。
3. **代价比代录时估的小**：`three` 已是 `HeroField.jsx:110` 的 `await import('three')`
   **懒加载独立 chunk**（实测 734,334 字节），**不在首屏关键路径上**。真正该问的是
   「新组件会不会把 three 拉进第二个页面」。

⚠️ **查的过程同一晚踩了两次同一个坑**：首页 `grep -i mit` 的 4 次命中**全是 `Yosemite` 的子串**；
第一遍 `grep "from 'three'"` 零命中，差点写成「three 是死依赖」，第二遍换写法才抓到动态 import。
全文判词入 [`collection.md`](../collection.md)。**买不买 Pro 归你**（先过 MVP 闸）。

---

## ⑤ 需要 Andy 决定的

1. **⭐ 盲选 20 个字母**（②·1）——整个喜剧/说唱研究卡在这一步，没有它 device 一条都不能进 Voice Bible。
2. **周信要不要用「金九银十」这一轮**？我的建议：**可发，但发的是过程不是读数**——
   「我们验了一句老话，验完发现自己的检验先坏了两次」比「金九不成立」好读得多，且它正好是 THE NULL 栏。
   ⚠️ **别写「九月黄金还跌」——那是事实错误**（九月黄金绝对收益是正的，只是相对自身其他月不突出）。
3. **threeui Pro 买不买**（④）——$99/年 或 $199 终身。**MVP 闸：它两周内对外发布什么？**

---

## ⑥ 门铃待按（只列不按）

- **UI Claire** · Breadth 页 `MarketStateSummary.jsx` 加第五格，照 [v4b](../ui_previews/2026-08-31/v4b_tighten.html)。
  **数据端不需要先加字段**——`breadth.json` 的 `history.rows[-1]` 已有 `new_highs_common` / `record_high_pct`。
  ⚠️ 兜底：拿不到标准口径时**不要退回原始计数**（那正是要修的病），显示 `—`。
- **UI Claire** · threeui：只取 Community 组件并保留其 attribution；新 three 内容优先挂在
  `LandingPage` 已有的那个懒加载点上，别在第二个页面新开 `import('three')`。
- **DATA ALEX** · `pipeline/screeners/breadth_signals.py:125` 的 `votes['nh_nl']` 用**原始计数**，
  08-28 因此投 `bull`；标准口径下（8 vs 18）该是 `bear`。第 729 行 `percentile_context` 的 `nh_nl_net` 同病。
  **不在我的白名单，只报不改。**
- **Marketing Steve / Studio Q** · 建议把 Voice Bible §4.8 第 3 条改成
  **「比喻优先于当修饰用的数据。当证据用的数据不参与比喻，它换个位置。」**——依据是 C6 那组实测（②·1）。
- **Marketing Steve** · 盲选结果回来后，通过 A/B 的 device 需要追进 `Fluxus_Swipe_File.md`（你的地盘，我出补丁）。

---

## ⑦ 没动的大件

- Delayed EP `--review` 复盘、TML `leaders_log` 前瞻、momentum97 shadow、shock_days —— 本轮没碰。
- 喜剧/说唱研究的第二轮（NULL-2 对仗配对重测 ≥12 对）等盲选结果。

---

## ⑧ 收工三问（宪法 08-27）

**① 这轮踩了什么坑？**
**同一个坑，同一份研究里踩了两次，方向相反**：先拿一个**假阳性**（换月伪影）当「检验有分辨率」的证据，
再换上一个**物理上报不出阳性**的更严格检验。合成一句已写进素材箱与 `results.md` §七：
> **一个检验的阴性，只有在你先证明过它的阳性长什么样之后才算数——
> 而「证明过」必须是往真实数据里注射一个已知效应，不是在别处找一格显著的。**

第二条独立的坑：**「最长可得」是一句需要查证的断言，不是形容词。** 我查的是「yfinance 上哪个代码最长」，
写下来变成了「世界上最长」——差别是 40 年数据和一个翻转的符号。
⚠️ 第三条（同一晚重复两次）：`grep` 找一个东西时**只用了一种写法**——`from 'three'` 漏掉 `await import('three')`；
`grep -i mit` 把 `Yosemite` 当成 MIT 许可证。这是 `pitfall_audit_greps_miss_the_other_spelling` 的第 N 次。
**按三次律，这一条已经够格从 memory 升级成机制**，建议见下。

**② 哪条规矩帮了 / 碍了？**
**帮了：「研究结论类默认走 Workflow fan-out，≥2 个不同视角 verifier」。** 今晚三个 verifier
各自独立地杀掉了我两条结论，其中一条我本来准备当头条发。**这条规矩今晚的产出，等于整晚工作量的一半。**
**建议修订（给 OPS 周检）**：宪法现在只说「研究结论类默认走 fan-out」，
建议补一句判据——**凡是要写进 `claims.jsonl` 或素材箱的结论，fan-out 是必须不是默认**；
以及把今晚这条方法论收进 `KNOWLEDGE.md`：**阳性对照必须是「往真实数据里注射已知效应」，
在别处找一格显著的不算。**
另建议按三次律把「grep 只用一种写法」升级成机制：**审计类 grep 至少两种写法并报命中数差**。

**③ 下轮第一件事？**
**看 Andy 的盲选结果**（若已回）→ 判 15 条 device 的生死 → 出 `Fluxus_Swipe_File.md` 的 append 补丁给 Steve。
若盲选未回，则做 `null_results.md` NULL-2 的**对仗配对重测**（≥12 对同义候选），
那是本轮唯一因分辨率不足而无法判定的一条。
