# 夜间组晨报 · 2026-08-28（JST）

> ET now 2026-08-27 15:32 · **last completed session 2026-08-26** · today is trading day True
> 分支 `auto/night-20260828-bfbf5c` · 窗口守卫：04:32 JST，窗口内，正常开工。

---

## 一、回执

**① 已认领挂单 · 联邦看板（OPS 08-27 二更递活）** — 认领，但**卡面已过期，按现状重写**。
挂单说「给 v2 打分」，实际 main 上已经是 **v4**（`a2494136` v3 重构成控制台应用、`cc970efe` v4 生意主角化，
Andy 评 v2 只有 2/10）。按宪法「先读已有规划再开口」，我不给一份停在 v2 的评分表；
**保留挂单里版本无关的那一件——lane 归属启发式的准确率实测**（OPS 原话：抽 20 张卡人工核对，报个准确率数字），
并按现状对 v4 走 §2.5。详见 §二。

**② ADR≥3.5 闸 —— 已结案，Andy 08-27 拍板保留**（`fb6a4f90`，知情决策，已见 50.8% 砍幅与幅度闸定性）。
我 08-27 挂的「待 Andy 拍板」解除。§12 已同步。本条不再占今晚。

**③ Joe 08-26 派的 ADR 两写口一致性测试 —— 08-27 已复核结案**（ALEX `3a026935`/`642eba2e`，我做过独立阳性对照）。

**④ Gate 声明制 + 封顶三行制挂单（`df159160`）—— 不认领**：卡面写明「建议 Plumber Joe 认领」，
且交付物是 `update_scheduled_task` 补丁（改别人的任务书），不在我边界内。留给 Joe。

**⑤ 收藏夹**：INBOX 🔗 节**零条未处理**（六条全 ✅；`L1vsun` 那条标 📦 需真浏览器留交互会话，不是我能推进的）。
今晚无收藏夹活。

**⑥ main 基线**：见 §三。

---

## 二、今晚做了什么

一件事做透：**联邦看板的 lane 归属准确率实测**，普查全部 59 张卡（不是抽 20 张），两名独立裁判盲判。
过程里撞出两条比 lane 更要紧的东西。全文
[`federation_board_2026-08/lane_accuracy.md`](../federation_board_2026-08/lane_accuracy.md)，
逐卡原始判定在 [`adjudication_raw.md`](../federation_board_2026-08/adjudication_raw.md)。

### ① OPS 要的那个数字：**38.5%**

在产的 v4 关键词版把 lane 判对 **20/52**。**看板上每 3 张卡有 2 张挂错线。**
两名裁判（视角一个按 TEAM.md 边界、一个按「Andy 该敲谁的门」）一致度 88%，只用一致的 52 张当分母。

病因三类，没一类是「关键词不够多」：
- **花名册顺序压过文本位置**（8 张）：`→ 风险线(模型 R&D):…(数据端研究协议回填时发现` 判给了 DATA ALEX——
  收件人排在文本最前，但 ALEX 排在 ROSTER 最前，先扫到谁算谁。
- **顺带提到的人名当成归属方**（6 张）：`…建议 Joe 认领` → Joe（实为 OPS 派活）；`verdict(adr)` → Zac（命中子串 `adr`）。
- **关键词表对不上实际提交习惯**（18 张）：OPS 的键是 `rules(`（带括号），实际 commit 写 `rules:`，于是宪法级改动全落「联邦」。

**修法**：路径优先（规则逐条抄自 TEAM.md 文件边界表）→ 箭头收件人 → 关键词兜底。
**84.6%（44/52）**，未归属从 36% 降到 3%。⚠️ **in-sample，折扣声明在报告 §五**；真检验要等下个 14 天窗口的新卡。

**结构性上限**：`origin/main` 近 14 天 596 个 commit 里，**142 个（24%）路径判不出线**，
其中 61 个只碰三个 append-only 公箱。这一段靠加关键词补不上——
真解是 OPS 自己在 08-27 卡里提过的 **commit message 线名前缀规范**。猜的上限就是 84.6%。

### ② ⚠️ 首页「等你拍板」是**假零**

那格印着「现在没有等你的事」，实测**至少三件在等**，全在 `status: 待办`：
**T1 回收两个 Discord 付费角色**（Andy 08-25 原话「这个是要处理的，**提醒我**」）·
**T5 `#welcome` 加升级入口**（Andy 08-26「**要做！**」）· **T3 PII 清史**。
病因：`blocked` 列只扫契约行与 NOW.md，而这三件挂在增长台账与 INBOX 的「给 Andy 的待办」节——两处都有核销协议，只是没接。

已修（分支）：接上这两个源 + 两处登记只出一张卡的去重，首页由 0 变 3。
**特意没接晨报的「建议 Andy 决定的事」**——晨报没有核销协议，接进来会把已拍板的事当成还在等；
**假阳性比假零更快让人不再信这一列**。

### ③ ⚠️ 「待认领」列 **91% 是坟头**

22 张待合分支卡里 **20 张最后提交早于 08-23**，最老两张来自 **2026-03-31**（五个月前，
`marketing` 与 `pine-indicators` 的 tip 还是同一个 commit，后者里没有任何 pine 改动）。
两张真待办被埋在二十张噪音里——正撞 Andy 六条里最硬的「简洁整齐压倒一切」。
**没动**（产品决策，且不在我白名单）：建议给待合分支卡加保鲜期，或交 `repo-janitor` 出删除清单。

### ④ OPS 交办的第二件（每日自动生成挂法）：**已经有了**

`ops-console-refresh` 定时任务已在 09:55 JST 跑并 republish 到固定链接。不提新方案。
**只报一个缺口**：它在共享主树里跑脚本，而主树停在 `feat/morning-three-pages`、**落后 origin/main 411 个 commit**。
今天 md5 恰好一致，但那是巧合——我今晚的修法合进 main 后，09:55 拿不拿得到新版取决于有没有人手动同步。
建议改成在基于 `origin/main` 的临时树里跑（一行，报告 §七 有现成命令）。**OPS 的任务书我不动。**

### ⑤ §2.5：**这轮不出竞品稿**

看板一天之内 v0→v4、Andy 已当面评过分，我再出三个变体就是宪法警告的平行造稿。只报可量的：
动效 0 处 ✅ · 决策优先结构对 ✅ · **31 个颜色字面量与现网 58 个 token 交集 = 0**（立项卡硬约束写着「现网 token」，
事实与约束不符，该 Andy/OPS 认一下）· 对比度**未测量**（file:// 在项目目录外只出静态快照，拿不到渲染结果，不猜）。

---

## 三、safe-merge / 待合

**已自合进 main**（`data/research/**`，白名单内）：见收尾行的 commit。
- `data/research/federation_board_2026-08/lane_accuracy.md` + `adjudication_raw.md`
- 本晨报

**待合分支：`auto/night-20260828-bfbf5c` · 建议合 y**
只剩两个文件、零数据/前端改动：
- `pipeline/tools/federation_board.py`（lane 归属 v5 + blocked 两个新数据源 + 去重）
- `pipeline/tests/test_federation_board_lane.py`（19 条，新增）
`pipeline/tools/` 非 `audit_*`，不在我白名单，按规矩不自合。**全套 909 passed**。
⚠️ **合之前 OPS 该看一眼**：这份文件今天被 OPS 改过四版（v0→v4），我的 diff 基于 `58fd7ecf`，可能要 rebase。

**每个修法都带阳性对照**（不是只验了绿）：
- lane：三个 bug 逐个注射回新代码，分别精确报红 1 / 4 / 1 条。
  ⚠️ 注射③**第一版全绿**——那条用例里收件人恰好排在发件人前面，补了「箭头之前有转投递人」的用例才报红。
- blocked：撤掉两个新数据源（= 修之前的状态），守卫精确报红「台账有 3 条 status: 待办 而看板是空的」。

## 四、门铃待按（只列不按）

- **OPS Fable** · 三件：①`auto/night-20260828-bfbf5c` 建议合（看板 lane 38.5%→84.6% + 假零修复）；
  ②`ops-console-refresh` 任务书第 2 步建议改成在临时树里跑（现在读的是落后 411 commit 的主树里那份脚本）；
  ③看板「待认领」列 91% 是陈年分支，要不要加保鲜期 / 交 repo-janitor。
- **Andy 决定** · 看板卡的 lane 语义没定义：是「**谁欠这件事**」还是「**谁做了这件事**」？
  现在 `done` 列是作者语义、`claim`/`blocked` 列是收件人语义，同一个函数两边用。
  两名裁判正是在这 7 张卡上分歧——**在定义之前这部分准确率量不出来**，不是我量不出，是题目没答案。
- **Andy 决定** · 首页三件在等你（T1 回收两个 Discord 付费角色 / T5 `#welcome` 升级入口 / T3 PII 清史），
  你 08-25 说过「提醒我」——这是提醒。

## 五、收工三问

**① 这轮踩了什么坑** —— 两个，都是「我以为验过了」：
(a) 第一次拿新测试去跑旧代码，**16/16 全红**，我差点当成阳性对照收工——那红的是 `KeyError`（函数不存在），
    **只证明函数没了，不证明断言能抓住答错**。真对照必须是把 bug**注射回新代码**。
(b) 注射③第一版**全绿**，因为我那条用例里收件人恰好排在发件人前面，两种写法答案一样。
    已写成 memory 防坑账：**「测试跑红」和「测试因为我要它抓的原因而跑红」是两件事**。

**② 哪条规矩帮了/碍了** —— 帮了：**「先读已有规划再开口」**，今晚救了两次——
挂单卡说「给 v2 打分」而 main 已是 v4；卡又问「评估每日生成怎么挂」而 `ops-console-refresh` 已经在跑。
两次照卡面做都会产出废稿。**建议**：挂单卡加一行「本卡快照于 `<commit>`」，认领方一眼看出隔了几版。

**③ 下轮第一件事** —— 看 `auto/night-20260828-bfbf5c` 合没合。
合了：这轮 84.6% 是 in-sample，等新的 14 天窗口攒出**没被我看过**的新卡，做一次 out-of-sample 复量。
没合：先在晨报回执里报「待合已 N 天」，别重做。
