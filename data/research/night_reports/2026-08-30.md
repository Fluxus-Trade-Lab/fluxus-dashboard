# Nighty Zac 晨报 · 2026-08-30（JST）

**时钟**：ET now 2026-08-29 15:32 | last completed session **2026-08-28** | today is trading day False
**分支**：`auto/night-20260830-acf037` · **窗口**：04:32 JST 开工（窗口内）

> ⚠️ **给 Andy 的一句话**：昨天早上 Joe 记下一个「至今未发作」的潜伏 bug，**九小时后它发作了**，
> 而**修法早就写好了，只是搁在一个没人合的分支上**。数据没坏，纯属这次那班坏得够明显——
> 拦住它的是另一道完全无关的闸。→ 见 ③，需要你或 OPS 点一下头合分支。

---

## ① 回执

- **[2026-08-30 OPS 挂单 ×3] ③「WHAT CHANGED 检测器」标「建议夜班/数据端认领」→ ✅ 已认领并交付**（见 ②）。
  ①GAS writing 拉取 / ②Discord briefing 归集器 分别挂给数据端与 Gary，不适用于我。
- **[2026-08-29 Plumber Joe → Zac]「回归闸」新靶子（"我不抢，你说一声就归你"）→ ✅ 已认领并交付**（见 ①）。
  Joe 同轮认领了 `audit_mutation_sweep` 接巡检建基线，归他，我不动。
- **[2026-08-30 Andy 拍板 T3 PII 选 (b)]** — 不适用于我（OPS 已销账）。
- **🔗 收藏夹**：1 条未处理（08-30 LoopX）→ ✅ 已判定（见 ④），🔗 节现在零条未处理。
- **INBOX / §七 / §12 其余条目**：无标「建议 Zac」或无主而属我地盘的新挂单。

---

## ② 本夜做了什么（4 件，全部有测试或可复现数字）

### ①【已合】回归闸 —— 第一道问「它比它替换掉的那份更好吗」的闸

`pipeline/tools/audit_regression_gate.py` · `pipeline/tests/test_audit_regression_gate.py`（98 条）

Joe 08-29 的诊断：**我们所有的闸都在问「这份数据自己对不对」，没有一个在问「它比在库那份更好吗」。**
`audit_ledger` L6 看得见同 session 的重复班，然后明说「re-run; both happened, **not a violation**」——
它对「重跑合法」的判断没错，它只是**从不比较那两份**。

判据里最要紧的不是阈值是**方向**：`bars_missing` 涨是坏、`tradeable` 涨是好、当日事件数**两头都坏**。
一道只知道「变了 X%」的闸分不出改善和退化。

**真账本上的对照（4 对全部来自 `run_ledger.jsonl`，零人造样本，零误报）**：

| session | 变化 | 判定 |
|---|---|---|
| 08-19 | degraded → ok（**真修复班**） | 🟢 GREEN |
| 08-27 | ok → ok（两班都健康） | 🟢 GREEN |
| 08-27 | ok → **degraded**（事故本体） | 🔴 RED — R1 + bars_missing 64→266 + unmeasurable 75→277 |
| 08-28 | ok → **severe**（24h 内重演） | 🔴 RED — R1×3 + tradeable 2556→**6** |

`breadth.regime_score` **故意不入表**：43.8 → 34.4 是行情在动，不是数据在烂。

**变异测试：68% → 99%**（唯一存活是 `json.dumps(indent=2→3)`，等价变异）。
首批 53 条一次写完全绿——**这本身是可疑信号**，普查报出 25 个存活，第二批 45 条每一条都对着一个具体存活变异写。
对照：`audit_archives` 54% / `audit_ledger` 51% / `audit_unpushed` 50%。**全库最高。**

### ②【已合】WHAT CHANGED 检测器 —— 周信段 2 从「回忆」变「勾选」

`data/research/what_changed_2026-08/`（[README](../what_changed_2026-08/README.md) · [本周候选清单](../what_changed_2026-08/candidates_2026-08-28.md)）· `pipeline/tests/test_what_changed.py`（56 条）

判据不是「它变了」（什么都在变），是「本周变化在该读数**自身历史周变化**里排第几」。

**⚠️ 本周读出来的东西和模板范例正好相反**：
头名是 **净上涨 +1,431 → −1,408（p98，568 次历史周变化里排前 2%）**；
而模板范例用的「20 日线上方占比」只跌了 8.58 点 = **p50，中位数水平**。
**照模板写，写的是上周最不值一提的那个读数。**

三档，因为三个归档历史差两个数量级（574 / 8 / 7 个交易日）：
regime 一族标 `no-baseline` **列出来给眼睛看，不给号**——7 个观测的百分位分辨率地板约 14 个点，
任何变化都会印成 p100。（08-29「先量分辨率地板再信阴性」反着用。）

**代码放在 `data/research/` 不在 `pipeline/tools/`**：挂单主人未定（「夜班/数据端」），
那不是我的合并白名单。升产线 = 一次 `git mv` + 改 3 行 import，测试已按产线标准写好不用重写。

### ③【已合】incident：Joe 记下「至今未发作」的 bug，九小时后发作了

[`incidents/2026-08-30_backstop_gate_fired_the_day_after_it_was_filed.md`](../../reference/incidents/2026-08-30_backstop_gate_fired_the_day_after_it_was_filed.md)

这是 memory 早核 todo「查『覆盖成更差』重演」的答案：**重演了，24 小时内。**

`gh run view --log` 的**真输出行**（不是命令回显——带 `[36;1m` 前缀的是源码，
[[pitfall_read_the_source_took_it_for_the_behavior]]）：

```
08-29T08:00:55Z backstop: newest session in breadth.json = 2026-08-28, looking for 2026-08-29
08-29T08:00:55Z ##[warning]Backstop firing — 2026-08-29 missing (newest is 2026-08-28).
```

它去 `breadth.json` 里找**一个周六**，因为迟到 390 分钟跨过了 04:00Z 的 ET 日期翻转线。
**它自己打印的理由是假的**：21:30 那班既没掉也没失败，03:17Z 跑完、03:42Z 提交了 `807ccce9`。

放行的这班算出 `severe`，被 `run_all.py:912` 一道**完全无关**的闸拦下，零提交。
**这不是好消息**：那行注释写着 `Degraded only warns`——同一个洞，08-27 进来的是 `degraded` 所以落地了，
这次进来的是 `severe` 所以被挡了。**差别只在坏得多明显。**

**台账诚实性**：12 行 / 8 个 session，3 个有重复班 = 37.5%，但这是**下界不是发生率**——
「失败夜晚也记台账」是 `abc18a54` 于 08-28T05:12Z 才加的，在那之前跑失败的班不写行。
所以 08-28 这对是台账**有史以来第一次**能记下 `severe` 重复班；更早的 `ok→severe` 覆盖发生过几次
**不可测，不是零**。新建回归闸对 `severe` 档的有效覆盖也只从 08-28 起。

### ④【已合】收藏夹判定：LoopX

六件套我们**已有五件半**。唯一真缺口是**状态投影**——他主张「看板本身成为执行系统的一部分」，
而**实测我们仓库里没有任何程序消费联邦看板的输出**
（`grep -rln 'federation_board|board.html'` 只命中生成器、它的测试、几份说「去读它」的文档；
CLAUDE.md 叫各线开工读它，那是**人读**，不是可执行依赖）。

后果不是理论的：一块没有下游动作依赖的看板，**错了也不会有任何东西坏掉**——
08-28 实测的 38.5% 分线准确率 / 「待认领」91% 是坟头 / 首页「等你拍板」假零，三个都错了好几周，
**没一个是被系统发现的**，全是有人专门去查才查出来。

- **✅ 采纳成一个问题交给 OPS**：看板每一列问「有任何下游动作真的读它吗？」答不出的列是装饰。
- **📦 存档不采纳**：writeback-resume（我们有等价物；我们的病是**按时钟恢复不按状态恢复**，照抄他的协议治不了）·
  能力自进化（我们已经在做只是没起名字——今晚这轮就是；给已发生的事加协议是仪式不是杠杆）。
- ⚠️「200+ 小时不漂移」是作者自己的 showcase，**n=2 无第三方复现**，可当方向不可当证据。
- ⚠️ 收藏比 1.56 低于本库交易方法帖（Hrundel75 2.79 / Muninn 2.60）：**题材不同尺子不可比**，别写进 Steve 对标表当「差」。

判词全文入 [`collection.md`](../collection.md)，INBOX 该行已打 ✅。

---

## ⑧ 测试基线

开工 **952 passed / 0 failed**（`--deselect test_content_processor.py`）→ 收工 **1,106 passed / 0 failed**。新增 **154 条**（回归闸 98 + WHAT CHANGED 56）。

---

## ③ safe-merge：已合的 commit

本夜全部改动只碰白名单路径（`pipeline/tools/audit_*` 及其测试 · `pipeline/tests/**` 新增 ·
`data/research/**` · `data/reference/incidents/**` · `material_inbox.md`），**已自合**。
commit 列表见收工核实节。

## ④ 待合分支

**无。**（`what_changed.py` 我主动放进 `data/research/` 避免制造一条卡住的分支——见 ②）

## ⑤ 门铃待按（只列不按）

- ⚠️ **OPS Fable / Andy** · **合 `origin/fix/joe-backstop-gate-date-2026-08-29`**。
  main 上仍是 `WANT=$(TZ=America/New_York date +%F)`，**bug 是活的且已发作一次**。
  `.github/workflows/` 不在我白名单。→ 这是本夜唯一有时效性的一件。
- **Andy / DATA ALEX** · `degraded` 那一档怎么办（Joe 08-29 列的三选项）。
  本夜证据只加强一件事：**触发条件一点不罕见，两天两次**。
- **OPS Fable** · 看板每一列问「有任何下游动作真的读它吗？」——答不出的列是装饰（LoopX 判词的采纳项）。
- **Plumber Joe** · 回归闸已交付，可接进晨检当量尺；它**默认只有状态降级致命**，计数回退是警告，
  `--strict` 才升级。**别急着设阈值**——按你自己 08-29 的话，没有基线的阈值是随机报警器。
- **DATA ALEX** · `atr_ext` **没有时间序列归档**（`ticker_events.csv` 是逐事件的），
  WHAT CHANGED 挂单点名要它但出不来，要出得先建档。

## ⑥ 素材箱

已追 3 条（回归闸的方向判据 / backstop 发作 / WHAT CHANGED 的 p50 反直觉），
每条带「一句可发的话」与出处。

## ⑦ 没动的大件

预览稿（§2.5）本夜没做——四件正事占满，且 08-29 刚出过两批预览稿等 Claire 消化。

---

## 收工三问

**① 这轮踩了什么坑** → 往 INBOX 某一行**后面**追判词时用了 `s.replace(old, old+add)`，
而 `old` 只是那行的**前缀**（后半截是别人写的 OPS 配注），于是判词被插进了**行的中间**，
把一行劈成两半。`git diff --numstat` 报 `2 1`，长得像一次正常的行内修改；
**是「origin/main 的每一行是否都还在新文件里」那个逐行自检报出 `lost lines: 1` 才暴露的**。
已记 memory `pitfall_replace_with_a_prefix_splices_midline`：
往某行后插东西要按行读、用唯一子串定位、`assert len(hit)==1`、`lines.insert(i+1, …)`。

**② 哪条规矩帮了/碍了** → **帮了：主树保护第 6 条的逐行自检。**
但那条规矩现在只写在「写公箱」的语境里，我这次是**改一行**不是**追一行**，
差点因为「我又不是拷贝整份」而跳过它。**建议修订**（需 Andy 批）：把自检的适用范围
从「拷贝公箱」放宽成「**任何对公箱的写**」，并把判据从 `git diff | grep '^-'`
换成逐行比对（那个 grep 在 markdown 列表上本来就会漏，这一条 08-25 已经吃过一次亏）。

**③ 下轮第一件事** → 回来先看 `origin/fix/joe-backstop-gate-date-2026-08-29` 合了没有。
**没合就再报一次，并把它写进 §七当契约行**（这一次它是活的 bug，不是提醒事项）。
合了就跑一次 `audit_regression_gate --window` 看有没有新的重复班对。
