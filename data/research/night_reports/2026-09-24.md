# 夜间研究班晨报 · 2026-09-24（JST）· linda

ET now 2026-09-23 15:31（周三，盘中）| last completed session **2026-09-22**（周二）| 今天是交易日
任务 `T-0924-11` · 窗口 04:31 JST（在 04:00–10:00 内）· 时间盒 150 分钟

> 昨夜（`T-0923-10`）把全库最低的那道闸 `audit_universe_population` 从 21% 补到 82%，
> 并在收尾写明下轮第一件事：**`audit_universe_freshness`（21/56，38%）是现在最低的一道。**
> 今晚照搬同一套做法。这两道闸是同一次事故（06-26 宇宙换人口）的两半，
> 测试欠账大概率是同一个形状——边界、`main()`、读取端。

## 一、回执

| 来源 | 内容 | 状态 |
|---|---|---|
| 任务板 `list --owner linda` | 名下无新 open 单；三张 blocked（`T-0919-23`/`T-0921-12`/`T-0922-09`，均前几晚 worker timeout）；其余已 done | 已核 |
| INBOX 🔗 收藏夹 | 最后一条 09-12 已 ✅，**无新条目** | 不适用，跳过 1.5 节 |
| INBOX 📌 给 Andy 的待办 | 本线名下无未核销条目（SPX GEX 那条 09-17 已核销） | 已核 |
| 09-23 晨报「下轮第一件事」 | 打 `audit_universe_freshness` | 本班执行中 |

**已认领**：无新挂单指名本线。

## 二、进行中

- **今晚的题**：`audit_universe_freshness` 的 35 个存活变异点（21/56，38%）。
  这道闸问的是「这份货是一整场，还是只是一个钟头」——2026-08-10 那份 05:31 的盘前快照，
  字段齐、null 率漂亮、`quality.status` 印着 `"ok"`，背后没有一天的交易。
  **它是为了「没人看着时钟」而建的，而它自己有 35 处可以坏掉而没有任何测试会响。**
- 做法：先复算前测读数（不转抄昨夜那个 38%）→ 分清真窟窿与等价变异 → 只对真窟窿补测试 →
  重跑同一把尺子出前后对照。

## 三、今晚做了什么

**两道闸，都从「一半的行可以坏掉而没人会说话」补到九成以上。** 欠的是同一笔账。

### ① `audit_universe_freshness` 38% → 93%

这道闸问的是一句很短的话：**这份货是一整场，还是只是一个钟头。**
2026-08-10 那份 05:31 的盘前快照，字段齐、null 率漂亮、`quality.status` 印着 `"ok"`——
它和一份真收盘货的差别，**在所有被人看着的维度上都不存在**
（同期 `universe_quality.csv` 里它和邻近四场收盘的中位差最多 0.33pp）。
唯一分得开的量是时钟，而没人在看时钟。它为此而建，它自己也没人看。

| | killed/总 | 杀死率 | 存活 | unstable |
|---|---|---|---|---|
| 补测试前 | 21/56 | **38%** | 35 | — |
| 补测试后 | **52/56** | **93%** | 4 | **0** |

测试 16 → 36 条。补的是边界（阈值正好卡在线上那一格 · 非有限数必须贡献 0 ·
无时区时刻按 UTC 不按本机钟 · 单快照的场次也是 F4）、`main()` 的三个退出码与 `-q` 的取舍、
`snapshots()` 的读取契约（此前一行没测过）。
剩下 4 个存活点逐条判为等价/不可达/化妆，其中三条**实跑验过不是嘴上说**。

commit `b3fcd33b`

### ② `audit_unpushed` 47% → 90%

**这道闸的处境最尴尬**：它是查「有没有活死在这块盘上」的那把尺子，
每条夜班任务书的最后一步都写着跑它——而它 30 个变异点里 16 个活着。

| | killed/总 | 杀死率 | 存活 | unstable |
|---|---|---|---|---|
| 补测试前 | 14/30 | **47%** | 16 | — |
| 补测试后 | **27/30** | **90%** | 3 | **0** |

测试 8 → 19 条。补的是读取端的失败态（不是仓库的目录计数必须是 0 不是 1 ·
**路径带空格的 worktree 不能被读成「目录没了」**——本班自己就跑在这种路径下）、
U1/U2 互斥、`main()` 的两个退出码与人读的那几列。

commit `592ad72e`

### ③ 两条「对真代码和变异体一样绿」的旧测试，两夜各抓到一条

这是本轮最值得留下的东西，不是那两个百分比。

| 哪一条 | 它长什么样 | 为什么它什么都没在查 |
|---|---|---|
| 09-23（`audit_universe_population`）| `f'{0.2692*100:.0f}'` 断言印出 `'27'` | `*101` 印出来**也是** `'27'`，`:.0f` 把多出来的 1% 扔了 |
| 09-24（`audit_unpushed` U3）| 1 个已跟踪改动 + 1 个未跟踪文件，断言 `dirty == 1` | 判据反过来写（只数未跟踪的）**同样得 1**。改成未跟踪的比已跟踪的多，两个数才分得开 |

本夜因此在 freshness 那边**主动没写**一条 `assert HIGH == 3.0`——那是读自己那个常量，
同一个形状的第三条。改用只在正确阈值下才改判的数去问它（`classify(3.5)` 必须是 F2；
HIGH 一挪到 4.0 它就读成干净，而「95.4 是 F2」这种实测坏值根本钉不住 HIGH）。

## 四、收尾

**测试**：`pipeline/tests` + `tests` 两根一起跑，**3477 passed / 1 skipped / 12 deselected**，全绿（5 分 28 秒）。

**已合的 commit**（gate=none，逐次复核过 `git log origin/main` 上的真 sha；两次 push 都是快进，没 rebase 过）：

| commit | 内容 |
|---|---|
| `267946c4` | 本晨报开工 |
| `b3fcd33b` | `audit_universe_freshness` 测试 16 → 36 条 + 研究档 |
| `592ad72e` | `audit_unpushed` 测试 8 → 19 条 + 研究档 |

**待合分支**：无。本夜全部 gate=none 自合，没有开过分支。

**收藏夹判定清单**：INBOX 🔗 节最后一条是 09-12，已 ✅，**本轮无新条目**，不适用。

**预览稿分数表**：本夜未做 UI 预览稿（2.5 节）。时间给了上面两件——按任务书「宁可 1 件做透」，
且这两件是同一笔账的两半，分开做会重复一遍同样的判读。

**只差 Andy 一步的交付**：**无。** 本夜产出全部 gate=none 自合，不需要他回任何一个字。
（故 `needs-andy` 与 INBOX『📌 给 Andy 的待办』两处均不适用。）

**没动的大件**：还没量过的 6 道闸（`audit_ci_test_coverage` 162 个变异点最大）·
`audit_ledger` / `audit_archives` 53% · `data/reference/DATA_RELIABILITY.md` §六第 6 条那四条欠账
（都要动 `.github/workflows/tests.yml`，不在本线边界）。

**下轮第一件事**：`audit_ledger`（42/80，53%）与 `audit_archives`（54/101，53%），现在并列最低。
⚠️ 两个都是 09-02 的 `repeat=1` 读数，**先复算再动手**——三次复算全部逐字复现，
这一步值 2 分钟，换的是「后面那个涨幅是不是真的」。
⚠️ 两个都超过 80 个变异点，`--repeat 3` 必须切片跑（本夜 `audit_unpushed` 就顶穿过一次前台 10 分钟上限，
三段切片 `0:8` / `8:20` / `20:30` 跑完，做法记在研究档三之三节）。
