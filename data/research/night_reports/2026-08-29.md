# 夜间组晨报 · 2026-08-29（JST）

> ET now 2026-08-28 15:32 · **last completed session 2026-08-27** · today is trading day: True
> 分支 `auto/night-20260829-7c24ed` · 04:32 开工 · 时间盒 300 分钟

---

## 一、回执

| 上一轮我提的 / 收到的 | 状态 |
|---|---|
| 08-28 待合分支 `auto/night-20260828-bfbf5c-2` | ✅ 已合进 main（`a8c74612` / `7318717b`）；原分支 `auto/night-20260828-bfbf5c` 可删 |
| Joe 08-28「Daily Content Threads 连红 8 班」 | ✅ 已闭环——Andy 拍板停用，OPS 执行 `f1437aa3`。不适用于我 |
| Joe 08-28 Gate 声明制审计（我 08-28 明确不认领 → Joe 认领） | ✅ Joe 已交付 `06220562`。不适用于我 |
| §12 C/E（ADR 两个宇宙 + claim 性质改判） | ✅ DATA ALEX 已修 `6b49f106`；实测 `watchlist.json` 已有 `universe_tradeable`（956）与 `universe_tradeable_exempt` |
| §12 D（前端 `gateWords()` 缺 `min_adr_pct` 子句） | ⏳ **仍待修，但阻塞已解除**——C 落地了，D 现在有正确的数字可显示。见门铃 |
| 收藏夹 🔗 节 | ✅ 零条未处理（六条全 ✅；`L1vsun` 那条标 📦 需真浏览器，留交互会话）。**今晚无收藏活** |

**已认领的挂单**：无。看板挂单 08-28 已办结；INBOX 与 §七/§12 里没有标「建议 Zac」或无主而属我地盘的新单。
本轮三件全部自选，优先级按任务书 §2 的 ④可靠性缺口 / ⑤研究复盘 / ⑦UI 预览稿。

## 二、今晚做了三件

### ① 研究：每一道筛子是「选股闸」还是「仓位闸」——**第一版被推倒重写**

[`data/research/gate_role_2026-08/`](../gate_role_2026-08/results.md)（预注册在先，含偏离记录）

**⚠️ 请只读更正后的版本。** 我先出了一版，头条是「`vcp` 是最干净的仓位闸」。
按宪法派了三个不同视角的对账 agent（统计 / 代码 / 反驳），**三个全判 BROKEN**，而且各自独立地指向同一处：

> 我在论证「除以 ADR 是干净剥离」时，**引用了自己刚打印的那张表，抄的却是相邻的一列**。
> 真实那一列是 1.6236 → 1.0326，**单调下滑 36%**（log-log 斜率 0.823，次线性）——
> 也就是说除以 ADR¹ **系统性地给安静的票送分**，而 `vcp` 恰好是全表 ADR 最低的那一道。
> **我拿一张否定我的表，当成了支持我的证据。** 同轮还有五处计数错，病根同一个：用眼睛数打印结果。

更正后（改用两种配平法 + 依赖稳健检验）：

| | 结果 |
|---|---|
| **选股维度（中位超额）** | **0 / 14 存活** —— `b4_gates` NULL 的形状在 14 道筛子上普遍复现 |
| **幅度维度（剥掉 ADR 成分后）** | **6 / 14 存活**，全是动量族（`momentum_97` / `4_bullish` / `pocket_pivot` / `weekly_20_gainers` / `weekly_momentum_97` / `monthly_leader_97`） |
| **`vcp`** | **不在其中**——它只在朴素归一下成立。第一版据它提的产品建议**撤回** |

⭐ **本轮最值钱的一条（方法论）**：统计镜头指出每日观测互不独立（10 日前瞻窗口相邻日重叠 90%，
自相关 0.01–0.76，`n_eff` 低到 7.7）。我换上「块翻符号」这种更保守的检验，结果**一道都不存活**——
差一点就把这个漂亮的全线 NULL 写进结论。**阳性对照救了它**：67 天切成 7 个块，
零分布只有 128 种排列，最小 p ≈ 0.0156，乘 Holm 的 14 = **0.219**，
**在看到任何数据之前就越过了 0.05**；实测注射 +0.80R 它仍然报不出。
那个 NULL 是**检验的性质，不是数据的性质**。换 HAC（实测 +0.10R 即可报）才有判据。

> **换上一个更严格的检验之后，仍然要先证明它能报阳性。**

顺带修了三个代码缺陷（全由对账查出）+ 报了一条数据问题：ADR 面板里 **88 行 `adr_pct > 100`，最大 13,590**，已剔除并记账。

### ② 可靠性：审计自己的审计——**我们最贵的守卫一条测试都没有**

[`data/research/audit_mutation_2026-08-29.md`](../audit_mutation_2026-08-29.md) ·
新工具 [`pipeline/tools/audit_mutation_sweep.py`](../../../pipeline/tools/audit_mutation_sweep.py)

把宪法那条「先证明能报阳性再信阴性」批量化：往守卫源码注射语义突变，看它自己的测试会不会变红。

| 工具 | 首轮杀死率 |
|---|---|
| **`audit_archives`** | **23%**（101 个变异体存活 78 个） |
| `audit_ledger` | 51% |
| `audit_unpushed` | 50% |

存活的包括 **`I6a` 那行 `!=` 翻成 `==`（判据完全反转）**——而 I6a 正是 08-25 拦住整晚数据发布的那道闸；
还有 CI 日志里 `OK`/`BAD` 标志取反。**病根**：现有三条测试**全部传 `output=None`**，
整个 `reconcile()` 与 `ticker_shells()` 一行都没被跑过。

补 18 条测试（只碰 `pipeline/tests/`，生产代码零改动）→ 再跑一次普查：**23% → 41%**，
新杀掉 18 个，**七行危险逻辑变异全部在内**。这就是这批测试的阳性对照。

剩余 60 个存活按区域列了下一批清单（建议先钉 I4/I5 的边界）。

### ③ §2.5 预览稿：Watchlist 的「配方」披露

[`data/research/ui_previews/2026-08-29/`](../ui_previews/2026-08-29/README.md) · **`frontend/` 一个字节没改**

现网 19 个面板的 `recipe` 字符串 **41–655 字符**，4 个 >250，长的那几段里**规则只占开头一小截，其余是出处与考古**。
要下决定的那句话，被埋在为它辩护的那段话里，而且要点一次才看得到。

## 三、safe-merge 已合的 commit

本夜全部改动都在白名单内（`data/research/**`、`pipeline/tools/audit_*`、`pipeline/tests/**`、
`night_reports/INBOX.md`、`Fluxus_Brand/ops/material_inbox.md`），**生产代码零改动**，已自合：

| commit | 内容 |
|---|---|
| `9c706e81` | 立晨报骨架 + 回执 |
| `2bfa84e3` | 研究第一版（**结论已作废**，留档以便对照更正） |
| `957feb22` | §2.5 Watchlist 配方披露四稿 + 两轮迭代 |
| `bc327fc1` | 三镜头对账后的**结论重写** + 三个代码缺陷修复 + 偏离记录 |
| `738d483a` | `audit_mutation_sweep` 工具 + `audit_archives` I6/I7 的 18 条测试 |
| `c1958536` | 变异测试普查报告（23% → 41%） |
| *（本 commit）* | 晨报补全 + INBOX 回执 + 素材箱三行 |

**测试**：开工 921 passed / 0 failed → 收工 **939 passed / 0 failed**（新增 18 条）。

## 四、待合分支

无（本夜产出全在 safe-merge 白名单内，自合）。

## 五、收藏夹判定清单

🔗 节零条未处理，今晚无收藏活。

## 六、建议 Andy 决定的事

1. ⚠️ **撤回**：我 08-29 第一版据 `vcp` 提的「给 VCP 一格按 R 排序」的产品建议**作废**，它建立在一个错的归一化上。
   `vcp` 的**绝对**幅度更小（−1.83pp）、右尾更低（−11.0pp）这两条描述性事实仍然成立。
2. **筛子的用法**：更正后仍然站得住的一条是——**动量族的六道筛子携带的是「下一段有多大」，不是「哪只更好」**。
   这直接对应你 08-24 说的第③类问题。要不要在产品里把这件事说出来，是你的决定，不是调参。

## 七、门铃待按（只列不按）

- **UI Claire** · **§12 D 的阻塞已解除**——ALEX 已在 `watchlist.json` 落了 `universe_tradeable`（实测 956 vs `universe_gated` 1,996），
  所以 `gateWords()` 补 `min_adr_pct` 子句现在有正确的数字可显示了。预览稿在 [`ui_previews/2026-08-27/`](../ui_previews/2026-08-27/README.md)（v1b 12/12）。
- **UI Claire** · 今晚新出的配方披露四稿，v2a 是可以直接照做的（纯前端，`recipe` 字段不动）：[`ui_previews/2026-08-29/`](../ui_previews/2026-08-29/README.md)。
- **DATA ALEX** · 两条建议（我不改 `pipeline/screeners/`）：
  ① `watchlist.py` 的 panel 定义把 `recipe` 拆成 `rule` + `why` 两个字段——出处夹在句子中间时客户端启发式找不到，这是实测结论不是偏好；
  ② `claims.jsonl` 里凡是以「大赢家概率 / 右尾」为证据的 claim，按本轮的 holdout 读数打折。
- **Plumber Joe** · `audit_mutation_sweep` 可以接进你的巡检当量尺（恒返回 0，不是闸）；`audit_ledger` 51% / `audit_unpushed` 50% 的存活清单在 `_after.json` 的同族文件里。

## 八、预览稿分数表

| | 简洁整齐 | 反多巴胺 | 只留交易内容 | 不新增颜色 | 推理被看懂 | 决策优先 | **合计** |
|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| v0 现网 | 1 | 2 | 1 | 2 | 1 | 0 | **7** |
| v1 分层 | 1 | 2 | 2 | 2 | 2 | 1 | **10** |
| **v2 规则常驻** | **2** | **2** | **2** | **2** | **2** | **2** | **12** |
| v3 条款逐行 | 0 | 2 | 2 | 2 | **0** | 1 | **7** |

**v3 判负的依据是量出来的**：它把 **2 个面板的 OR 渲染成了 AND**（`anticipation` 的 `any of A / B / C` 被切成四行，
读起来像四条都要满足）——**页面因此会说错话**，这比不说更糟；展开总行数也从 45 涨到 77（+71%）。

**胜者两轮迭代**（每轮先量再改）：

| 稿 | 常驻行数（19 张卡） | 最长卡 | 超过 2 行的卡 |
|---|---|---|---|
| v2 | 28 | 3 行 | 2 / 19 |
| **v2a**（尾括号阈值 60） | **26** | 3 行 | **1 / 19** |
| v2b（手工拆，上限稿） | **23** | **2 行** | **0 / 19** |

阈值 60 是量出来的：**40 会吃掉真条款**（`ll_hl_trend_break` 70→25 字符）。
v2b **不是可实现方案，是上限**——它和 v2a 的差额就是「数据端补 `rule`/`why` 两个字段」能买到的东西。

⚠️ 对比度绝对值**未测量**（预览是静态快照，本轮没有可跑的取色环境）；只声明四稿**没有引入新的 token 对**。

## 九、没动的大件

- **`momentum97_shadow` / `leaders_log` / `shortlist_seat_log` 的前瞻验**：归档只有 **7 / 10 / 5 天**（首行分别 08-19 / 08-14 / 08-21），
  **今天测不了**，不硬测。按 08-27 的估算约 8 周后可验。
- **`delayed_ep_scan --review`**：`delayed_ep_review_2026-08/` 已有 `replay.py`，本轮没跑（时间给了前两件）。
- **`audit_ledger` / `audit_unpushed` 的存活变异**（38 + 15 个）：清单已出，测试没补。

## 十、收工三问

1. **这轮踩了什么坑** → 两个，都已写进 memory：
   `pitfall_i_misread_my_own_table`（memory，不在仓库里）—— 引用自己刚打印的表时抄了相邻的一列，于是拿一张**否定我**的表当成支持；
   `pitfall_a_stricter_test_can_be_blind`（memory）—— 换上更严格的检验之后也要先量它的分辨率地板，否则它的 NULL 是检验的性质。
   另有一个工具级的坑已写进 `audit_mutation_sweep` 的 docstring：**只在没出事时才跑的清理不是清理**
   （变异体直接写仓库文件 + `finally` 还原，被超时 kill 之后留了一个变异体在工作区）。
2. **哪条规矩帮了 / 碍了** → 帮了：宪法「研究结论走 Workflow fan-out，≥2 个不同视角 verifier」**这一轮直接救了一份错报告**——
   三个镜头各自独立地指向同一处，没有它我会把一个归一化伪影当发现交上来。
   **修订建议**：把这条从「研究结论」扩到「**任何要写进报告的数字表**」——今晚五处计数错和那处致命的抄错列，
   都不是推理错，是**取数环节**错，而对账 agent 恰恰最擅长抓这个。配套动作：散文不许出现手打的数字，表格由脚本生成。
3. **下轮第一件事** → 补 `audit_archives` 剩下的 I4/I5 边界测试（`audit_one` 主体那 23 个存活变异，
   它们决定「什么时候只是警告、什么时候该有人看」），清单在 `audit_mutation_2026-08-29.md` §五。
