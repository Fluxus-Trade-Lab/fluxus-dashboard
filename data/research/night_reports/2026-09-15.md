# 夜间组晨报 · 2026-09-15（JST）

**时钟**：ET now 2026-09-14 15:31（周一，盘中）· last completed session **2026-09-11** · today is trading day True
**分支**：`auto/night-20260915-1182f5` · 开工 04:31 JST（窗口内）· 本班成果**已全部合进 main**（见第四节）

---

## 〇、回执

- **门铃自取（第 0.5 步）**：`grep pending` 命中 4 行，写给 Zac 的 1 行（[09-13] DATA ALEX → Zac）下面**已有** `↳ ✅ 已取（09-14）`——是 OPS 那条「已办门铃仍写 pending」的同形（INBOX [09-13] OPS 行），不重复处理。**零新门铃。**
- **09-14 收工三问③「下轮第一件事」**：
  - INBOX 📌 两条待合分支：**仍无 ↳**。`auto/night-20260912-bb6565-protocol`（第 4 晚）、`auto/night-20260914-4ef60f-metric`（第 2 晚）均未进 main；`audit_stranded` 仍判两条都是 S3「真未送到」。📌 条目已在，不重复追。
  - 🔔 [09-14] → DATA ALEX（④生产接线两条一起接）：**无 ↳**，不催。
- **收藏夹 🔗 节**：全部已处理，无新条目。

## 一、`audit_calendar_gaps` 的 C2 在盘中恒红——把事故档里的一句提醒搬进工具（已合 `5ac7bfac`）

**现场**：开工基线里手动跑一遍所有闸，这把报 `BAD C2 2026-09-14: 8/8 tickers carry a bar past the last completed session`、exit 1。源是健康的。
C2 的判据是「源给了晚于上一收盘场次的 bar」，而**任何盘中下载的 yfinance 日线都带当天的实时 bar**——所以它在盘中必然红，报的是几点钟，不是源坏没坏。

⚠️ **不是新发现**：09-01 事故档 `:115` 早写了「C2 在盘中必然为真，接线时加开关」。提醒只在文档里，工具没执行；我先当新 bug 修完才 grep 到。事故档已追 ↳，memory 记为 `pitfall_the_ruling_was_in_the_doc_the_tool_was_not` 第 2 次。

**改法**：新增纯函数 `session_in_progress(now)`；`check(in_progress=…)` 对**正在进行的那一场**的实时 bar 记 WARN（提示下游裁掉），晚于上一收盘、又不是进行中那一场的 bar 仍判红；`main()` 接上。半日市 13:00–16:00 会被当成进行中（marketcal 不认半日市），偏向 WARN。

| 验证 | 结果 |
|---|---|
| 本文件测试 | 44 → **50 passed**（+6：进行中降级 / 晚于进行中仍红 / 盘中·盘前·收盘边界·周末·劳动节） |
| 变异（每轮清 `__pycache__`） | **8/8 杀** |
| 真源盘中 04:42 JST（ET 15:42） | 改前 exit 1 → 改后 **exit 0 + 1 WARN** |
| 真源收盘后 05:03 JST（ET 16:02） | **exit 0、0 WARN**（last completed 翻到 09-14） |
| 全套 `pipeline/tests` | **2148 passed / 7 skipped**（基线 2142 + 6） |

未覆盖：`main()` 里 `in_progress` 的接线本身（要网络），靠上面两次实跑证。

## 二、UI 预览稿 · Model Books：首屏没有一本书（已合 `3a011de9`）

→ [`ui_previews/2026-09-15/README.md`](../ui_previews/2026-09-15/README.md)

**先排除了一个假目标**：headless 量 14 个页面的字号占比，Model Books **99% 是 11px**——但 09-02 五档裁决（`a1c39af5`）把表格定在 11，合规。

**真正的病在数据形状**：1,514 条里带形态和教训的只有 **50** 条；1,464 条的 `outcome` 只是把 Gain/Duration 再印一遍；默认按涨幅排，**第一本书在第 61 行**。
迭代时又量出一件：有标注的条目卡上两句涨幅**量的不是同一个窗口**——38 条能读出的里，线放 25% 有 **32 条**对不上（50% → 22–24；100% → 4–8，给范围）。

| 稿 | 简洁 | 反多巴胺 | 交易内容 | 不新增色 | 推理 | 决策 | 合计 |
|---|---|---|---|---|---|---|---|
| v0 现状 | 1 | 1 | 2 | 2 | 0 | 0 | **6** |
| v1 删重复 | 2 | 1 | 2 | 2 | 0 | 0 | **7** |
| v2 两本书 | 2 | 2 | 2 | 2 | 1 | 2 | **11** |
| v3 标注置顶 | 1 | 2 | 2 | 2 | 1 | 1 | **9** |
| **v2a 两句署名** | 2 | 2 | 2 | 2 | 2 | 2 | **12** ✅ |
| v2b（v2a 另一页） | 2 | 1 | 2 | 2 | 2 | 1 | 10 |

**读者比 v0 多知道**：这页是两种东西、先看到的是书；卡上两个涨幅是两个窗口（`This chart …` / `The book …`）；纯价格行不会有标注、数只印一次。
对比度成对量过（15.34 / 8.30 / 5.61 / 8.84 / 8.06）；未选段药丸对页面底只有 1.16，已在 README 标出。
顺带看到（归 UI Claire，只记事实）：GNUS 图价格轴顶端 `12.00` 被裁、底部轴标签被成交量标签压住。

## 三、09-11 主排程失败的真因：GAS 接口生产 404（已直推 main `3f14cf1d`）

`audit_ledger` 的 L5 警告牵出来：09-11 主排程 run `34654994500` 22:55Z，`shortlist_feedback.fetch_rows` 302 跳转后 **`HTTP Error 404`** → 台账 L4 判死 → 整班 exit 1；3.5 小时后 dispatch 重跑同一接口正常（44 行）。
台账里该接口实际跑过的 **27 班只有这 1 班**失败。`8fb4ec76`（09-11 23:24Z）已降级为 skipped——**不需要动作**。
价值在于 09-13 那条「`sheets_source.py` 疑似会 404」ALEX 答「生产上没有」：同一个 GAS 主机在兄弟模块上真 404 过一次。INBOX ↳ 补在 ALEX 那条下面（核过落点 INBOX:2934，删除自检 0 行）+ 素材箱一行。
**顺带排除一条假线索**：日志里「Ticker refresh skipped」是 `echo` 命令回显不是真输出——两班 annotations 里都没有（`pitfall_read_the_source_took_it_for_the_behavior` 那个形状，这次先核了）。

## 四、safe-merge

全部在白名单（`pipeline/tools/audit_*` 及测试 · `data/research/**` · `data/reference/incidents/**`），全套测试绿后**自合**，rebase 后 FF 推 main、无 force：
`a9bdd494` 骨架 · `5ac7bfac` C2 修复 · `3a011de9` 预览稿 · `0afb2908` 口径敏感度 · `ce1b079e` 事故档 ↳ · `443ec9ae` / `409d0d6a` / `9c10139b` 晨报快照 · `6f73d2c7` 引用改成 main 上的 hash。
另直推：`3f14cf1d` INBOX ↳ + 素材箱一行（临时树基于 origin/main）。
⚠️ 远端分支 `auto/night-20260915-1182f5` 停在 rebase 前的 hash。逐文件对过：与 main 只差 **6 行 = 3 处 hash 引用**，内容已全在 main；**没有 force 推回分支**，`audit_stranded` 会判它 S2 可删。

**待合分支**（碰了白名单外路径，不自合，均已在 INBOX 📌）：
- `auto/night-20260912-bb6565-protocol` · `RESEARCH_PROTOCOL.md` §五补「NULL 必须带分辨率」7 行 · **建议合 y**（第 4 晚）
- `auto/night-20260914-4ef60f-metric`（`1486755c`）· `METRIC_SOURCES.md` 登记 `audit_events_vs_bars` 两条恒等式的自造容差 · **建议合 y**（第 2 晚）

## 五、09-14 场归档巡检（cron 落地后）

（待 cron 落地后填）

## 六、收藏夹判定清单

无新条目。

## 七、建议 Andy 决定的事

无新的。预览稿只积累不催；两条待合分支已在 INBOX 📌。

## 八、门铃待按（只列不按）

无。第三节的 404 线索不需要任何线动作，已作 ↳ 落在 ALEX 那条下面。

## 九、看了没做

- **`audit_universe_shape` 的棘轮**：对 `ticker_events.csv` 永远 exit 1——31 条全在 06-26→08-24（20 条 U2 截断 + 11 条 U1 恢复偏移），08-24 之后 0 条；新的截断会被淹在这 31 条里。
  没加 DECLARED：①工具 docstring 明文选择不压恢复告警；②§七 [2026-09-04] Andy 裁「如果可以全部回填」那行 **11 天仍无 ✅**，回填还是打「宇宙不完整」标记没定，现在具名声明等于替 ALEX 先定。**建议 ALEX 定了之后再加**，只记不开单。
- **Delayed EP 第三次复盘 / momentum97 shadow**：没开（账本自 09-09 复盘后只多 2–3 场；m97 17 场，分辨率不够）。
- **开工基线**（origin/main `9430673c`，09-11 场）：pytest 2142 passed；`audit_archives` / `event_agreement` / `events_vs_bars` / `ledger` / `wiring` / `metric_names` / `reads_declarations` 全绿（声明内）；`audit_progress` 同样 5 条；`audit_stranded` 与 09-14 一致（外加 4 条从未推送的本地分支，真未送到 0 行，不变）。

## 十、收工三问

（收工填）
