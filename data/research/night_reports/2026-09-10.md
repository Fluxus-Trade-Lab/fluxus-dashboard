# 夜间组晨报 · 2026-09-10（JST）

> ET now **2026-09-09 15:32**（交易日，未收盘）· last completed session **2026-09-04**
> 分支 `auto/night-20260910-f5790c` · 窗口守卫：04:32 JST，在窗口内
> 全套测试 **1721 passed / 6 skipped**（`pipeline/tests`，deselect `test_content_processor`）

**一句话**：全库扫完「裁决写在 .md 里、工具还是旧的」——**29 个脚本被裁决点到，27 个已正确标死，漏 4 条（已修）**；
顺手撞出两个更大的：**CI 从来没跑过仓库里 607 个测试**，以及 **main 上的素材箱带着三行冲突标记**。

---

## 〇、回执

| 上轮的问题 / 裁决 | 状态 |
|---|---|
| **我 09-09 收工三问③**：扫全库「裁定过 X 是错的」的结论，逐条看脚本改了没有 | **✅ 已执行**，见 §一 |
| Joe 09-09 挂单：四条从未推送的分支等人 push | **不适用**——那四条是主机上**别的 worktree** 的本地分支，我的临时树看得见 ref 却推不动它们（权限闸同样拦我）。**复列门铃** |
| Joe 09-09 挂单：`fbclock` 可快进，`git push …:main` 即落地 | **⚠️ 该命令已过期**——今晨实测不可快进。我 rebase 后推了新分支，见 §三 |
| DATA ALEX §六.7 欠账 ⓪①②③ | **第 4 天零动作**。09-06 以来碰过 `data/history/` 的只有 cron 的 `chore: market data` ×2 与 `chore(ledger)` ×1 |
| 收藏夹 🔗 节 | **无新条目**（7 条全 ✅），本夜不开新判定 |

---

## 一、主活：横幅是按文件记的，裁决是按条记的

📄 [`data/research/verdict_drift_2026-09-10/results.md`](../verdict_drift_2026-09-10/results.md)

**21 个研究目录 / 51 个脚本 / 48 份文档全读，无抽样**（Workflow fan-out，7 批 + 每条候选两个不同视角的复核，共 39 个 agent）。

| 类 | 数 | 处置 |
|---|---|---|
| **CONFIRMED** | **4** | **已加横幅并自合**（全在 `data/research/**`） |
| PLAUSIBLE | 2 | 不动，列出等对应线判 |
| 被复核推翻 | 10 | 其中 **2 条是复核比扫描更对** |
| 已正确标死 | **27 个脚本** | 做对是常态，不是例外 |

> ### 头条：**4 条漏网里，3 条住在「已经有横幅」的文件里。**
> 一个写着「⚠️ 这里第一版错了」的脚本，读起来像已经被处理过；
> 对同一文件的**第二条**裁决，正是没人再去标的那一条。
> 与 [[pitfall_has_x_is_a_bool_the_gap_is_a_set]] 同族：`banner_present` 是个 bool，缺口住在集合里。

**这一节比结果重要——我先造的两把便宜尺子都不能用，而且失败方式不同：**

- **「脚本比裁决旧」对已知阳性一次都报不出来**：09-09 那颗炸弹里，裁决句和被裁的脚本是**同一个 commit**
  （`1e9090e8`，09-01 05:10）进仓的。**裁决和它推翻的工具是一起出生的**，时间戳这把尺子在这种形状上永远读 0。
- **关键词段落扫：8 条命中、真裁决 2 条**。就此收工会写出「扫完了，只有 1 个」——实际是 4 漏 + 27 已标。
  裁决在本仓多半写成一句自我更正，不是命令句。**手写白名单量的是我的词汇量。**

<details><summary>四条漏网的明细（已修）</summary>

| 脚本 | 漏在哪 |
|---|---|
| `delayed_ep_review_2026-08/replay.py` | **裁决住在另一个目录**；09-09 补横幅时只看了 `2026-09` 那个目录。⚠️ 它的 docstring 自己说出了正确答案又选错对照：「各 stage 的 cohort 起始日不同」正是要用同日 cohort 的理由，减 SPY 消不掉它 |
| `gate_role_2026-08/measure_adr_matched.py` | **宣布「已修」的那个 commit，自己新写了一个带同一缺陷的脚本**（Holm 少 step-down 单调化）。实测 train A 5 对 / train B 3 对非单调；本轮没有一格判定因此翻转 |
| `leaders_tml_2026-09/analyze.py` | 被推翻的那段功效曲线**还在同一个文件里跑**，且**先于**正确版本执行，标题写着「阳性对照 / 分辨率地板」 |
| `screener_overlap_2026-09/overlap.py` | 只输出中位数，而裁决恰恰是「中位数在这里会骗人」（中位 0.81 那对最低一天 3%） |

**两条 PLAUSIBLE（归对应线判，我不改别人那轮的结论）**：`gate_role_2026-08/measure.py`（`|超额|÷ADR` 已判伪影，脚本仍在算）·
`gold_seasonality_2026-08/build_tables.py`（v1 对照组作废、沪深 300 已剔除，`TABLES.md` 仍以干净结果渲染）。

**复核比扫描更对的两条**：`label_permutation.py` 的 `BENCH="SPY"` 在**场内置换里直接抵消**——
**同一个 token 出现在两个脚本里，不代表它担同一个角色**；`pipeline/screeners/watchlist.py` 的标记在 `:50` 指向 `claims.jsonl`，
**本仓已有的绑定机制**，我差点报成缺失。
</details>

---

## 二、CI 是绿的，它跑 1,580 个测试，仓库里有 2,187 个

| | 文件 | test 函数 |
|---|---|---|
| `pipeline/tests`（CI 跑） | 96 | **1,580** |
| 仓库根 `tests/`（**CI 从没采集过**） | 53 | **607**（27.7%） |

`.github/workflows/tests.yml` 的那一行是 `python -m pytest pipeline/tests`。根 `tests/` 一次都没进过 CI。
里面 **`tests/test_no_naive_clock.py` 已经红了两周没人看见**——它就是 fbclock 那条。

**根因是我自己的**：09-02 那份「1,302 条测试没有任何自动触发点」的交接包，附的可 copy YAML 写的就是 `pytest pipeline/tests`。
**我数了哪个集合，交出去的闸就只盖哪个集合。**

同形状第 3 次（09-02 CI 接线 / 09-09 `audit_stranded` 看不见没推过的分支 / 本条）→ **按三次律②升级为机制**：
`pipeline/tests/test_ci_covers_all_tests.py`（棘轮：①新增测试文件落在采集不到的地方就红；②已登记欠账 53 只许变小；③欠账每跑一次念一次）。
**三个阳性对照实测都红过**（未登记目录新增 / 已登记目录里 53→54 / workflow 那行解析不出来时**红而不是安静报 0**），阴性复位 4 passed。
顺带：按文件名数会多算——`scripts/test_conditional_claim.py` 是个名字以 `test_` 开头的 CLI 工具，pytest 收集到 0 个用例。

---

## 三、fbclock：门铃里那行 push 命令已经过期

Joe 09-09 给的 `git push origin fix/joe-fbclock-rebased-2026-09-08:main`，**今晨实测会被拒**：
该分支**落后 main 33 个 commit、领先 3 个**，`merge-base --is-ancestor` → **NO**。
（同形第 2 次：09-08 Joe 查出我给的那行会把 main 回退 192 个 commit。**push 命令天生会过期。**）

我在独立树里 rebase 到今天的 main（**零冲突**），推了新分支 **`fix/fbclock-rebased-2026-09-10`**，此刻可快进。

**我自己实测的验收数（不转抄）**：

| | main | rebase 后的分支 |
|---|---|---|
| `tests/test_no_naive_clock.py` | **FAILED** | 1 passed |
| `tests/gex/test_resting.py` | **collection ERROR（0 个跑到）** | **7 passed** |
| `pipeline/tests` 全套 | — | **1713 passed / 5 skipped** |

⚠️ **更正一个转抄数**：分支 commit message 写「挡住了 96 条测试的收集」，**我这台机器量到的是 7 条**
（另外 4 个 gex 文件卡在 jinja2，本机没装）。96 我复现不了，别再传。

---

## 四、main 上的素材箱带着三行冲突标记

`Fluxus_Brand/ops/material_inbox.md` 从 `986bb7a4`（09-09，**我自己那次**）起带着 `<<<<<<< HEAD` / `=======` / `>>>>>>>`，
一天没人报过。全仓只此一个文件。**本次没丢内容**（冲突块 theirs 侧是空的，三条素材逐条都在，已核）。

**为什么三道自检全放过它**：治公箱的自检是 `git diff origin/main -- <file> | grep '^-'`——它量的是**删除**；
**冲突标记是纯新增**，在那把尺子下和正常追加长得一模一样。**一把只朝一个方向看的尺子，对另一个方向永远是绿的。**

已修 + 加闸 `pipeline/tests/test_no_conflict_markers.py`：两个阳性对照（一个合成、一个拿 origin/main 那份真坏文件跑，报出 `:388`），
一个阴性对照（markdown 的 setext 标题下划线不许误报）。

---

## 五、safe-merge 已合 / 待合

**已自合进 main**（全部在白名单内：`data/research/**` · `pipeline/tests/**` 新增测试 · `material_inbox` · `INBOX`）：见收尾 commit 列表。

**待合分支**
- 🔴 `fix/fbclock-rebased-2026-09-10` · 3 commits · 只碰 `pipeline/tools/federation_board.py` + `scripts/gex_levels.py`（白名单外）·
  **今天可快进**，`git push origin fix/fbclock-rebased-2026-09-10:main` · 验收见 §三 · **建议合 y**
- 旧的 `fix/joe-fbclock-rebased-2026-09-08` **不要再用**（不可快进）

## 六、门铃待按（只列不按）

- **→ 有 `pipeline/tools/` 与 `scripts/` 落地权的线（OPS Fable / Andy）**：`fix/fbclock-rebased-2026-09-10`，**今天**可快进。
- **→ 有 `.github/workflows/` 落地权的线**（挂单，不指名）：把 `tests.yml` 的 pytest 参数改宽到覆盖根 `tests/`。
  改宽前先解决 `tests/gex/` 的 `ib_async`（上面那条分支已修）与 CI 那步没装 jinja2（`pipeline/requirements.txt` 里有）。
- **→ DATA ALEX**：§六.7 欠账**第 4 天**零动作。
- **→ Joe**：四条从未推送的本地分支我推不动（不是我的 worktree + 权限闸），仍在挂单板上。
- **→ `gate_role_2026-08` / `gold_seasonality_2026-08` 那两轮的作者**：两条 PLAUSIBLE 等你判（§一）。

## 七、没动的大件

UI 预览稿（§2.5）本夜未做——三件主活都溢出了时间盒，宁可三件做透。Delayed EP / TML / momentum97 shadow 未新开轮。

---

## 八、收工三问

**① 这轮踩了什么坑**

1. **我造的头两把尺子都不能用，而我差点信了第二把的阴性。** 「脚本比裁决旧」对已知阳性一次报不出来；
   关键词扫只报出 2 条真的。**这两次都是先跑阳性对照才发现的**——先量分辨率，再信读数。
2. **我数的那个集合，就是我后来交出去的那个集合**（§二）。这不是新坑，是 [[pitfall_has_x_is_a_bool_the_gap_is_a_set]]
   的第 3 次，已升机制。
3. **`grep '^-'` 那条公箱自检只朝一个方向看**（§四）。它救过我两次删除，也让三行垃圾在 main 上躺了一天。
4. 小坑：我用 `grep -cE '⚠️|分母是错|cohort'` 判 replay.py 有没有横幅，数出 3 —— 那 3 个 `cohort`
   是它**为减 SPY 辩护**的话。**关键词命中不等于语义命中**，最后是打开文件看头 20 行才判对的。

**② 哪条规矩帮了 / 碍了**

- **帮了**：宪法「审计要求全覆盖 / 同构批量 >10 项 → Workflow fan-out」。这轮 39 个 agent 里，
  **复核推翻了 10 条候选，其中两条是复核比扫描更对**（§一）。单线程扫我会把 `label_permutation.py` 报成缺陷。
- **碍了（只提，不改宪法）**：白名单让我能修 4 条横幅、能加两道测试闸，
  但**改一行 `tests.yml` 的 pytest 参数不行**——于是「CI 漏掉 607 个测试」这件事今夜只能变成一条挂单。
  同一晚我既是发现者又有能力修，却要等别人。

**③ 下轮第一件事**

跑一遍 `pytest pipeline/tests/test_ci_covers_all_tests.py -s`，看欠账数是不是还是 53：
如果有人把 `tests.yml` 改宽了它会掉，如果有人在根 `tests/` 里加了新测试它会红。
**这是新闸第一次被用来验它自己提的那张挂单。**
