# 夜间自学 2026-08-19

ET now 2026-08-19 06:31（盘前）· last completed session **2026-08-18** · 今天是交易日。没碰 cron，没 stash，没动共享树、frontend/、data/output/、data/history/。

## 0. 一件要注意的（Andy 已回应）

~~1. 触发时间不对（19:31 JST 而非 04:00）~~ —— **Andy 已回应：这只是一次性的测试跑，排程仍是 04:00 JST。非问题，划掉。**

1. **`/Users/taolezhu/Documents/AI-Trading-System-night` 当时正被另一个会话写着。** 我进去时 `pipeline/adapters/finviz_adapter.py`、`run_all.py`、`audit_archives.py`、`delayed_ep_scan.py` 的 mtime 是 19:32–19:33，就在我读文件的那几十秒里；`ListAgents` 显示 5 个 interactive 会话、3 个是 2–4 分钟前起的。**两个 agent 同一个工作树 = 上周那类事故的形状。** 我只做了一次 `git checkout -B auto/night-20260819 origin/main`，而该分支当时正好就在 origin/main（760d7ca），所以是空操作、没有覆盖任何东西——但这是运气不是设计。之后我退出该目录，另起了工作树和分支 **`auto/night-20260819-b`**，本晚全部工作在那里。
   - 建议：夜间任务的工作树名字里带会话 id，或起手先检查 mtime/锁，别固定用一个 `-night` 目录。

## 1. 做了什么

**测了 4% Bullish 的两道闸，以及「第一波」子格 —— 后者是 NULL。**

`scanner_validation_2026-08/summary.md` §三.2 提的两道闸（`atr_from_sma50 ≤ 4`、`change_pct ≤ 8%`）一直挂在「建议，等 Andy」，`DATA_CONTRACTS.md` §八.1 又已经把「第一波」当成 entries 区的一格写进了前端规格。现在有数了。

- 新工具 [pipeline/tools/bullish4_gate_study.py](pipeline/tools/bullish4_gate_study.py) — 把归档里每条 `preset:4_bullish` 命中按闸切成组，喂给**已有的** `scanner_event_study.run()`（同日随机票基准照旧），不重写前瞻收益那套。
- 新测试 [pipeline/tests/test_bullish4_gate_study.py](pipeline/tests/test_bullish4_gate_study.py) — 9 条，只钉 as-of 特征那段自创算术（ATR 倍数的符号、20 日新高窗口、RS 线比的是比值不是价格、闸值 = 报告里的 4/8%/15%、gated+dropped 是完备划分、去重）。
- 结论文档 [data/research/scanner_validation_2026-08/b4_gates.md](data/research/scanner_validation_2026-08/b4_gates.md)，逐票表 `study_b4_gates*.csv`。

**样本不是 11 个截止日，是归档 88 个交易日、3,866 条命中（去重可测 3,546），2026-03-19 → 08-18。** `atr_from_sma50` 在归档里全空（该列 08-17 才加），所以按 as-of 从 K 线算，用的是仓库已有的那一份实现（`outcomes.atr` + `atr_enrichment.atr_multiple_from_levels`）——没有给已有的量起新名字。对照：归档 `change_pct` 与 K 线算出的当日涨幅，中位差 **0.0000**。

| 组 | n | fwd20 中位 | 同日随机 | 跌破 −10% |
|---|---|---|---|---|
| 全部 4% Bullish | 3,546 | +0.92% | +0.89% | 52.1% |
| **两闸都过** | **1,568** | **+2.35%** | +1.14% | **44.8%** |
| 被闸掉 | 1,978 | −0.16% | +0.64% | 57.9% |
| 涨幅 ≥ 15%（追高） | 325 | −0.37% | +0.44% | 62.5% |

gated vs dropped 的 fwd20 **MWU p = 0.0022**（fwd10 p = 0.0009）；76 个交易日里 62% 当日中位为正；**逐日留一法** pooled 中位在 +1.76% ~ +2.91%（全样本 +2.35%）——不是一天扛起来的。

**老实话两条**：过闸的票**中位仍跑输同日 SPY 0.69%**（均值 +1.70%，右偏）。闸的价值在 MAE 那一列，是风险口径的收益，不是把这张刀变成能赢指数的刀。

**「第一波」NULL**：两闸之上再叠 20 日新高 / RS 线 21 日新高 / 两者，样本砍掉 35–47%，**中位一个都没抬**（+1.66% / +2.26% / +2.24% vs gated +2.35%）。

验证方式：`pytest pipeline/tests -q --deselect pipeline/tests/test_content_processor.py` → **724 passed**。

## 2. 请 Andy 决定

1. **两道闸做成「折到格底灰显」而不是删行** —— 依据是 MAE 不是收益：被闸掉的票 57.9% 会见到 −10%，过闸的 44.8%；口径和已经上线的追高折叠一致。
2. **§八.1 的「第一波」子格撤掉** —— 三种叠加都没抬中位，建了是给页面加一格没有边的东西。我已在 `DATA_CONTRACTS.md` §八 追了一行 NULL 记录，前端别按原规格开工。
3. **闸值不要再扫参数** —— 4/8%/15% 只验了这三个点；在同一段样本上扫就是过拟合。要继续的话，值得测的是同一组闸在 `weekly_20_gainers` 上（那张刀的追高更极端）。
~~4. 夜间任务工作树的独占性~~ —— **已办**：Andy 拍板「固化成默认」，`fluxus-data-night-study/SKILL.md` §0 已改成每次在**本会话 scratchpad 下新建**工作树、分支名带会话 uuid 前 6 位（`WT=$SCRATCH/wt-night`、`BR=auto/night-$(date +%Y%m%d)-$SID`），并明令不用固定的 `-night` 目录、不 `checkout -B` 裸日期分支。配方已 dry-run 验过。

## 3. 网页上的占位（只列，没改）

`grep -rn "Placeholder\|TODO\|未接" frontend/src` 共 17 处，其中真占位页 7 个，都在 `Layout.jsx`：
- `rs-live`（market 组，RS Live Tracker）
- Library 五页：`defense` / `offense` / `psychology` / `portfolio-management` / `news` —— 与 memory 的 `todo_library_visual` 一致：**这五页现在是从零建，不是改版**。
- `masterclass`（course 组）

另有两处是"将来接"的注释，不是空页：`portfolio/lib/demonFinder.js:191`（等 screener 快照做交叉引用）、`ticker/TickerStats.jsx:4`（Phase 3b/3c）。

## 4. 积压里今晚仍没动的大件

- **Delayed EP `--review`**：目标日是 ≥08-21（要 5 个以上已记 session），今天 08-19 还早；且今晚另一个会话正在改 `delayed_ep_scan.py`，我不碰。
- **08-20 早核**（Yahoo 429 四轮重试是否把缺 K 线压回 ~2%、`ma_reclaim` 第二晚、fundamentals 是否又撞 40 连败墙）：**要等今晚 21:30 UTC 的 cron 跑完**，此刻数据还没产生。
- **GEX 覆盖核对**：要开盘时段的 OI，盘前 OI 是 nan。
- **DATA_RELIABILITY §六 的三条**：I4 阈值校准、run_ledger 月报都要攒够一个月；Finviz 宣称总数对账**今晚已被另一个会话动工**（`finviz_adapter.py` 里已有 `claimed_total` 捕获 + `run_all.py:852` 消费），我避开了。
- **TML / leaders_log 前瞻**：4–6 周后才够。
- **oratnek Momentum 97 shadow**：08-19 才起记，1 天。
