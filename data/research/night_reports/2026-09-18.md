# 夜间组晨报 · 2026-09-18（JST）

**时钟**：ET now 2026-09-17 15:32（周四，盘中）· last completed session **2026-09-16** · today is trading day True
**分支**：`auto/night-20260918-dbdc74` · 开工 04:32 JST（窗口内）

**一句话**：Market State 那张牌面上的 thrust 那个词**还在拿 300 判**（引擎 08-09 就改成按宇宙缩放了，今天 634，27 场里 20 场两边说反）——顺着它往上游追，撞出一件更大的：**「全市场」在 06-26 那晚换了一批人，十二周没人记账**，半字母表事故只是它的下游症状，而它至今没修。

---

## 〇、回执

- **门铃自取**（`doorbells.py --to Zac`）：开工 **open: 0**，收工再取一次。
- **上轮三问③（下轮第一件事）＝核 DATA ALEX 对 §七 [2026-09-17] Zac 行的回执** —— 他 09-17 已办，我**独立复核完毕，四条全对得上**：

  | 他声明的 | 我怎么核的 | 结果 |
  |---|---|---|
  | `breadth_archive` 06-26..08-07 共 30 场，22 行 3000 + 8 行 2974–2980（07-15..07-24 回填） | 直接读归档 | ✅ 逐个对上；窗口前一场 2,588、08-10 起 5,618 |
  | `universe_truncated` 放宽成「08-09 前且 size ∈ [2950,3000]」后 30 场全认出 | 调用函数跑全部 586 行 | ✅ 命中 30 场，与窗口集合**完全相等**，零误报；无日期行仍走旧判据 |
  | `shortlist` heat 前 50 的 M–Z 占比 08-19 .10 → 08-28 .58 | 用 `compute_heat` 从 `ticker_events` 重算 | ✅ 逐日复现（.10/.18/.20/.22/.24/.34/.42/**.58**），干净后稳定在 .54–.62 |
  | `delayed_ep_log` 缺 09-16 整场 | 扫全部归档找「有 09-15 无 09-16」 | ✅ 命中它；**另一个是 `regime_ledger.csv`**，已知，ALEX 09-17 已挂门铃给 RND Linda（`6b105dd0`），不是漏账 |

  一处值得记：`coverage_gaps.json` 里 09-16 这一条只声明了 `delayed_ep_log`，读这份文件的人看不出 `regime_ledger` 也缺同一场——那条在 INBOX 门铃里，**两个世界**。Linda 判完之后若是「留缺口」，请顺手登记进 `coverage_gaps.json`（本条只是提醒，不另立门铃，她那条门铃还开着）。
- **收藏夹 🔗 节**：无新条目（末条仍是 09-10，已判）。
- **开工认领**：§七 无标「建议 Zac」的新挂单；[2026-08-31] 那条「金9银10 预注册检验」仍**卡在数据**（本地与黄金相关的最长序列 9 个交易日），不认领。

## 一、宇宙翻倍之后，牌面上那个词没跟着改（已合，见第四节）

→ [`data/research/breadth_universe_break_2026-09-18/README.md`](../breadth_universe_break_2026-09-18/README.md) · 复现脚本 `measure.py` · 闸 `pipeline/tests/test_frontend_count_thresholds.py`

**怎么撞上的**：昨晚「没动的」里记了一句「宇宙在 08-10 从约 2,590 跳到约 5,620，计数类读数的历史百分位跨这一天在比不同规模的宇宙」，DATA ALEX 把它登记成了 `definition_breaks` 的一条并标「未量」。今晚去量它，量出来的东西比预想的大一号。

| | thrust 的家数门槛 | 出处 |
|---|---|---|
| 后端 | `max(60, 0.113 × universe_size)` = 今天 **634** | `breadth_signals.py:83`，`16db6a88b`（08-09）|
| 前端 | 硬编码 **300** | `MarketStateSummary.jsx:15-17` |

`16db6a88b` 的 commit message 自己写了这件事该怎么防——「state_board imports the same function rather than keeping its own copy of the constant, **which is how the two would have drifted**」。后端那侧照做了，前端这份拷贝没人知道它存在。

**量出来的**（断点后 27 场）：

- 牌面与引擎**不一致 20/27 场**；断点前是 22/559（3.9%）——所以准确说法是**08-10 把一个近似正确的常数变成了系统性错误**。
- `churn / volatile` 出现率：牌面 **0.5% → 55.6%**；引擎 0.4% → **0.0%**。
- 最近一场 09-16：牌面写 **bearish thrust**，同一份 `breadth.json` 里引擎写的是 `{"side":"neutral","line":634.043,"margin":-342.043}`。同一页、同一份 JSON、两个相反的话。
- 修法是一行：payload 里已经带着那条线（`verdict.vote_detail` 的 thrust 条），**后端不用改**。⚠️ 别指望从 `mm` 算——`mm` 块里没有 `universe_size`。

**顺带量到的第二处**：`percentile_context()` 把当日读数排进整个归档的分位，其中四个是原始家数。归档 586 场里只有 4.6% 来自新宇宙，于是新宇宙的家数天然排在历史高位。前端 `MarketStateSummary.jsx:35` 那行 `down-4% {n}th pctile` 印的就是这个数。
⚠️ **我第一版把「换成比率再算差 39 个分位点」当成了误差的大小，复核用一个安慰剂对照拆穿了**：对本来就免疫规模的 `ratio_5d` / `t2108` 做同样的「÷ universe_size」，分位照样掉 32–37 点——除以一个在长大的分母本身就压分位。所以本轮**只能说方向（读数系统性偏高），说不出幅度**，处置建议是不跨断点排分位，不是改成比率就完事。

**往上游追一层（本班最硬的一件）**：`data/output/universe.json` 的历史快照按自身 timestamp 转 ET 排开——

| ET 场次 | 只数 | 市值中位 | <$1B | 字母序最大 |
|---|---|---|---|---|
| 06-25 | 2,588 | **$5.70B** | **0.0%** | ZYME |
| **06-26** | **3,000** | **$0.78B** | **53.4%** | **LOCO** |
| 08-11 | 5,619 | $0.76B | 53.7% | ZYME |

逐票对账：同名票市值相对变动中位 1.8%（**字段没换口径，换的是名单**）· 新进 1,613 只里 **99.4% 在 $1B 以下**、市值中位 1.43 亿 · 掉出的 1,201 只里 1,166 只是字母序 >L（那是翻页截断）· A–L 且 ≥$1B 的 1,422 → 1,397（**老那批人没少，是新来了一批**）。
即 `cap_1.0to`（≥$10 亿）那道条件在 06-26 那晚不再生效，名单当场撑爆 150 页上限——**半字母表事故是它的下游，不是它本身**。筛选串 `f=cap_1.0to,ind_stocksonly` 一字未改，是上游行为变了，我们这边没有任何一次提交记录过它。
这改写了 `coverage_gaps.json` 那条声明的含义：它写「universe size 约 2,590 → 约 5,620」读起来像同一批东西变多了，实际 2,590 是 ≥$1B 的那批、5,620 是全市值的那批——**跨 06-26 比全宇宙家数，比的是两个人口**。
⚠️ 踩坑留痕：这个脚本第一版拿 `git log` 的提交日给快照定日期，JST 的提交日把 06-25 的快照算成 06-26，差点把断点定错一天——`DATA_RELIABILITY.md` §256 记过同一个坑（`backfill_preset_hits`），我又踩了一次。改成读快照自己的 `timestamp` 转 ET。

**复核**：两个只读 agent 独立跑（一个对抗性证伪、一个从头复算），结论与数字一致，另纠了本文两处措辞、加了三处我没查到的东西（后端散文里也留着 300、`0.113` 没进 `METRIC_SOURCES`、前端这半边零测试）——全部写进研究档第七节，我逐条自己核过，没照抄。

## 二、闸：前端不许拿常数判家数类读数

`pipeline/tests/test_frontend_count_thresholds.py`（我的白名单内，已合）。

- 只管**家数类**字段（随 universe_size 缩放的那些）；比率型（`ratio_5d`/`t2108`/`pct_above_*`）不管——它们不受宇宙规模影响，前端拿它们分配色档是显示不是判定。
- 和 **0** 比不算阈值（符号判断，`CourseRead.jsx` 的 `net > 0` 是这一类）——这条是扫描第一版报了它才加的。
- **先证明它报得出阳性再信它的阴性**：两个合成对照 + 真扫描命中前端那三行（`KNOWN_DEFECTS` 能匹配上本身就是真文件上的阳性对照）。
- 已知缺陷单列，**修好之后条目会匹配不上，测试会要求删掉它**——闸不会替一段不存在的代码站岗。

## 二之二、由它引出的第二件：闸 + 事故档

**事故档**（新建，我的地盘）：[`incidents/2026-09-18_the_universe_changed_populations_and_nobody_logged_it.md`](../../reference/incidents/2026-09-18_the_universe_changed_populations_and_nobody_logged_it.md)
——十二周、零记账、被一个更显眼的症状挡住。另在 09-01 半字母表事故档末尾追加一节：**本档只查到症状没查到成因，结论不撤**。

**发布出去的五张筛子单，06-26 那晚一并换了人**（`panels_after_the_break.py`）：

| 发布的单子 | 06-25 | 06-26 | 09-15 |
|---|---|---|---|
| `gainers_4pct` | **<$1B 0.0%** | 64.6% | **69.8%** |
| `vol_up_gainers` | **0.0%** | 64.5% | **70.9%** |
| `momentum_97` | **0.0%** | 60.5% | 38.5% |
| `healthy_charts` | **0.0%** | 31.4% | 37.9% |
| `ema21_watch` | **0.0%** | 37.0% | 40.2% |
| `watchlist`（走 tradeable 闸） | — | — | **0.0%** |

筛子本身从来没有市值条件——条件一直在宇宙那一层，**它们原样照跑，脚下的池子换了人**。
**边界别扩大**：走 tradeable 闸的 `watchlist` / `shortlist` 够不到。⚠️ **更正我自己**：那道闸是**市值 ≥$1B**（`pipeline/themes/__init__.py:31`），不是我先写的 $300M（$300M 是 `2f782b53` 08-09 引入值，次日 `e7ea316d9` 抬到 $1B）——所以「实测 <$1B 占 0.0%」是**按定义如此**，不算独立证据；⚠️ **收工前又自查出一处**：我拿 `run_ledger` 的 `tradeable` 计数当证据也是错的——那份账本**只从 08-19 开始**，没有断点之前。站得住的是直接数 `universe.json` 快照里 ≥$1B 的只数（月中位 03 月 2,481 → 06 月 2,574 → 08 月 2,606 → 09 月 2,592，07 月 1,391 是字母截断）——**老那批人横跨 06-26 稳定**。

**闸**（`pipeline/tools/audit_universe_population.py` + 10 条测试，我的白名单内）：看人口不看行数。
P1＝`<$1B` 占比偏离 20 场滚动中位超 10 个百分点；P2＝市值中位偏离超 1.5 倍。
**真实历史 `--since 2026-03-01` 跑 139 场，首个违规正是 2026-06-26，此前全绿**——先证明它报得出阳性，再信它的绿。
`$1B` 这条线是**自造的**（取自 `cap_1.0to` 实际生效时的效果），docstring 里按宪法 08-31 写明。
它现在**没有任何形式在跑**（测试里只跑合成数据，真实历史那一跑要逐个 `git show` 139 份快照，放 CI 会拖垮每次 push），
已按 `audit_wiring` 的规矩记进 `KNOWN_UNWIRED`，owner 写 DATA ALEX，并写明接线位置与「别每次重走 git 历史」的做法——**这是作者当晚给自己打的第五张欠条，说在明处**。


## 二之三、它咬到哪些研究：**污染的是分母，不是分子**

三组只读 agent 分批重报十三份研究（判定表在[研究档第十一节](../breadth_universe_break_2026-09-18/README.md)），承重的几条我逐条自己核过。

**最重要的一条**：命中样本几乎都是干净的——面板、九个预设、`vcp`、`b4_gates` 的样本里 `<$1B` 占 **0.0%**（它们自带 `marketCapMin = 1.0`）。
**脏的是它们比的那条基线**：`scanner_event_study` 的对照是「同日从全票池随机抽一只」，而那个池子 06-26 之后塞进了三千只小票，且这批小票这段行情里差得多。
我用**另一条数据路径**（`price_panel.pkl` + `universe.json` 快照，不是 agent 用的 `event_bars.pkl`）独立核了全宇宙 20 日前瞻中位：

| 场次 | 核心（≥$1B） | `<$1B` | 差 |
|---|---|---|---|
| 06-12（断点前） | +0.6%（n=2,436） | **池子里一只都没有** | — |
| 06-26 | **+0.63%** | **−4.12%** | **−4.75pp** |
| 07-02 | −0.24% | **−7.53%** | −7.29pp |
| 07-10 | +2.83% | −1.51% | −4.34pp |

**于是断点之后每个「超额 vs 同日随机」都系统性虚高 1.5–2.5pp。** 最干净的例子是 `vcp`：命中里一只小票都没有，超额却从 **+1.60pp 缩到 +0.57pp**——它那个「✅ 有效」建在基线那一侧。

**翻号的结论两条**，都在 Stockbee 那份（唯一用**无市值闸**筛子的研究，**也正是 09-17 那轮半字母表重报漏掉的那一份**）：
「过闸的票中位连零都没到」→ 干净人口上是 **+0.21% / +0.54%**；「4% 突破整体跑输 SPY 0.45%」→ **−0.03% / −0.00%**，**那 0.45pp 基本就是新进来的小票**。

**我自己 09-17 说错的一句**：`vol_up_gainers` 对预设的包含度塌陷，我当时读成「两张单子长度不对称」。
只对了一半——涨的**全是预设按定义收不了的小票**，它的核心部分反而从 42 缩到 27；我独立复算**86.5% 是人口**（agent 算 85.1%，差在窗口口径）。

**另外两件会改行为的**：①十个预设里**只有 `Weekly Momentum 97` 没有市值下限**（我核过 JSON），所以那晚只有它换了人口、并排的九个没换，而页面没有一处说这件事；
②`stockbee/open_questions.md` 的 S1 建议（改 EP 阈值，**会改变 Andy 每天看到的名字**）引的「今天漏 47/55」里，**那 47 只有 45 只 `<$1B`**——在 06-25 那个人口下漏掉的是 **2 只**。宇宙修回之前不该执行。

`amplitude` 的两句副结论也要改（已在它自己的 results.md 追了更正）：「方向 ρ≈0」是两段相反的东西拼出来的零（断点前 +0.057 显著、断点后核心 +0.037 不显著）；「期望值驼峰、两端都负」只在断点之后成立，断点前 Q5 期望是 **+1.16%**、单调上升、没有驼峰。

## 三、safe-merge 已合（全部已在 origin/main）

| commit | 内容 |
|---|---|
| `4df3eafe` | 晨报骨架 |
| `78c463bf` | 前端 300 漂移研究 + `measure.py` + 前端家数阈值闸 |
| `52125cfd` · `0472cb78` | 06-26 人口断点 + `population_break.py` |
| `7d4ff8fe` · `d8a7deec` | §七 两行 + 三条门铃 + 素材一行 |
| `0730c1e0` | `population_reach.py`（`is_core` 分类器） |
| `386873e1` | 五张发布筛子单的人口对照 + `panels_after_the_break.py` |
| `15d9d215` | **事故档**，并在 09-01 半字母表事故档末尾追一节 |
| `02325c2b` · `1d4ba5b6` | `audit_universe_population` + 10 条测试 + `audit_wiring` 欠条 |
| `e2f39051` | **更正我自己**：tradeable 闸是 ≥$1B 不是 $300M |
| `8dc6e059` | 十三份研究的人口重报 + `amplitude/results.md` 两条更正 |

测试：`pipeline/tests` **2223 passed / 7 skipped**（`--deselect test_content_processor`、`--ignore test_discord_to_thread_modes`——本机缺 `dotenv`，CI 有）。
**待合分支**：无。碰过的路径全在 `data/research/**` · `data/reference/incidents/**` · `pipeline/tools/audit_*` · `pipeline/tests/**` 之内；
`frontend/`、`pipeline/screeners/`、`data/output/`、`data/history/` **一个字节没动**。

## 四、门铃待按（只列不按，🔔 行已立）

- **UI Claire** · `MarketStateSummary.jsx:15-17` 三个 `>= 300` 改成读 `verdict.vote_detail` 里 thrust 那条的 `line`
- **DATA ALEX** · `percentile_context` 跨断点排分位 · `breadth_signals.py:405/:420` 两句「300+」散文 · `coverage_gaps.json` 补 06-26 人口断点
- **Writer Mia** · 主树里 Andy 亲笔 X 帖正样本处于未提交删除态（main 上安全）
- **（本班第二批，收工时立）** DATA ALEX · 研究层三件：基线污染、Stockbee 两条结论翻号、S1 建议不该执行

## 五、给 Andy 的（已进 INBOX 📌 节，此处只是副本）

**一条**：他的 Screener 页那几张单子三个月前换了一批股票，不是谁决定的。`gainers_4pct` 里 $10 亿以下的小票 06-25 是 **0.0%**、今天 **69.8%**；Watchlist / Short List 有闸，没变。
**要他回的一句**：这些筛子单**要不要也加一道市值/成交额闸**？两边都可逆、不花钱。

## 六、没动的

- **UI 预览稿**：没做。这次的修法是一行取值逻辑，不是视觉选择题。
- **收藏夹**：无新条目。
- 09-17 记下的第三件（老 7 个筛子研究表里窗口外 M–Z 只占 22.3%）没单独查——但今晚的重报覆盖了那张表所在的研究，判 **C**（基线被污染）。
- 顺手跑了一遍 `audit_universe_shape`（字母那一维）：31 条违规全是历史的（06-26 窗口 + 08-11 恢复），**08-24 之后没有新的**。

## 收工三问

1. **这轮什么做成了、方法值不值得固化**：
   - **做成①**：一条可复用的判据——**「同一个规则在两个地方各写一份」时，先去读写规则那一侧的 commit message**。`16db6a88b` 自己写了「a second copy is how the two would have drifted」，那句话直接指出了另一份拷贝在哪。已写成 memory `method_find_the_drifted_copy_from_the_fix_commit`。
   - **做成②**：**一个被解释清楚的症状会终止对它成因的追问**——09-01 那份半字母表事故档写得越硬，越没人再问「名单为什么会突然超过 3,000」。今晚的做法是**去读那个症状的上游量**（宇宙的成分，而不是它的行数），一步就撞到了。已写成 memory。
   - **做成③**：把它变成闸——`audit_universe_population`（看人口不看行数），**真实历史 139 场里首个违规正是 2026-06-26**，先证明它报得出阳性再信它的绿；已按 `audit_wiring` 的规矩给自己打欠条、写明接线位置。
   - **坑**：①拿 `git log` 的提交日给 `data/output` 快照定日期（`DATA_RELIABILITY §256` 记过，我又踩一次，差点把断点定错一天）；②把「换个口径重算的差值」当成误差幅度，忘了做安慰剂对照；③把 tradeable 闸的门槛写成 $300M（实为 $1B），于是「实测 0.0%」那句本来是**按定义如此**却被我当成独立证据。三条都已写进 memory。
2. **哪条规矩帮了**：「研究结论走 ≥2 个不同视角的 verifier」。今晚**五个 agent 每一个都拆掉我一样东西**：一个纠了「牌面每天在误导」（其实在默认关着的折叠块里），一个用安慰剂拆了 +39 那个数的解读，一个把我 09-17 的「两张单子长度不对称」纠成「86% 是人口」，一个查出污染的是分母不是分子，一个指出 09-17 那轮重报漏掉的正好是受影响最大的那份研究。**没有它们，今晚会发出一份数字对、解读错的报告。**
   **修订建议**：任务书 §2「研究结论默认走 fan-out」建议加半句——**「其中至少一个 verifier 的任务是找安慰剂对照／找能让我的结论成立得太轻松的地方」**。这轮的价值不是复算一致，是那些反例。
3. **下轮第一件事**：取铃，看 UI Claire 与 DATA ALEX 对 §七 [2026-09-18] 两行的 ↳。若 Claire 修了前端，回来把闸里 `KNOWN_DEFECTS` 那三条删掉（测试会提醒）；若 ALEX 动了 `coverage_gaps.json`，核一遍 06-26 那条写的是「人口」不是「规模」。**另外把 `stockbee/gate_results.md` 那两条翻号的结论补记进原文**——今晚只写在研究档和事故档里，原文还没改。
