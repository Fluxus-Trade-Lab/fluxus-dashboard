# 夜间研究班晨报 · 2026-09-23（JST）· linda

ET now 2026-09-22 15:30（周二，盘中）| last completed session **2026-09-21**（周一）| 今天是交易日
任务 `T-0923-10` · 窗口 04:30 JST（在 04:00–10:00 内）· 时间盒 150 分钟

> 连着两晚（`T-0921-12`、`T-0922-09`）都被 worker timeout 掐在半路。
> 那两晚干的是同一件事：**量**——20 道 `audit_*` 闸里有多少道从没被变异测过。
> 昨夜量出五道，最低的一道是 `audit_universe_population`：**67 个变异点，53 个活着**。
> 今晚不再往下量，**改成修**：先把全库最低的那道闸的窟窿堵上，再回头报读数。

## 一、回执

| 来源 | 内容 | 状态 |
|---|---|---|
| 任务板 `list --owner linda` | 名下无新 open 单；`T-0919-23`/`T-0921-12`/`T-0922-09` 三张 blocked（均 worker timeout / 窗口外派发）；`T-0921-42`、`T-0921-100`、`T-0922-67`、`T-0922-76` 已 done | 已核 |
| INBOX 🔔 [09-21] → RND Linda：`feat/linda-regime-caveat-0918` 滞留 >48h | 已由 `T-0921-42` 结清（`3b5ba08a`），本班复核分支是否还在 origin | 见第三节 |
| INBOX 🔗 收藏夹 | 最后一条 09-12 已 ✅，**无新条目** | 不适用，跳过 1.5 节 |
| INBOX 📌 给 Andy 的待办 | 本线名下只有 SPX GEX 那条，09-17 已核销（他选「退役」）；其余非本线 | 已核 |
| 09-22 晨报「三、今晚做了什么」 | 停在「（收尾时补全）」——被 timeout 掐掉，但当晚的读数已进 main（`3b573eba`） | 本班接手并补账 |

**已认领**：无新挂单指名本线。

## 二、进行中

- **今晚的题**：`audit_universe_population` 的 53 个存活变异点。
  这道闸盯的是 06-26 那次「宇宙换了人口」——**它自己有五十三处可以坏掉而没有任何测试会响**。
  按 Growth Gary 08-25 那句总纲（没验证过一个检查能报阳性，就不该信它的阴性），
  这道闸今天的绿，我们没有资格信。
- 做法：先分清「真窟窿」和「等价变异」，只对真窟窿补测试；补完**重跑同一把尺子**，出前后对照。

## 三、今晚做了什么

### ① 全库最低的那道闸，从 21% 补到 82%

`audit_universe_population` 是 19 道 `audit_*` 里变异杀死率最低的一道。
它盯的是 06-26 那次**宇宙换了人口**（`cap_1.0to` 失效，1,613 只中位市值 $143M 的小票一夜进池）——
**它是为了「没人看着人口变化」而建的，而它自己也没人看着**：67 个变异点，53 个活着。

| | killed/总 | 杀死率 | 存活 | unstable |
|---|---|---|---|---|
| 补测试前 | 14/67 | **21%** | 53 | — |
| 补测试后 | **55/67** | **82%** | 12 | **0** |

- 前测 14/67 是**两次独立跑出来的同一个数**（09-22 那班一次、本夜复算一次，逐字相同）。
- 后测用 `--repeat 3`——这把尺子被 Plumber Joe 09-01 抓到过自己跟自己不一致（同 commit 四次 43/47/49/43）。
  本次三试全一致、unstable 0、no_verdict 0，所以这个 82% 不是抽出来的一张签。
- 测试 9 条 → 29 条。补的是**边界、出口、入口**：每条阈值正好卡在线上时的取舍 ·
  `main()` 的三个退出码（**此前一行没测过**，其中 `2` 是「一个快照都没查到」——0 个违规和 0 次检查长得一样）·
  `snapshots()` 的读取契约（**此前完全没测过**：空文件不是空宇宙 · 无时间戳不猜日期 ·
  null 市值记 0 不丢行 · 同场次后写的算数）· 违规消息里人读的那三个数。
- **剩下 12 个存活点一个都不是窟窿，逐条判过**（等价 / 不可达 / 化妆），三条等价判断都是实测验证不是嘴上说：
  `c and c > 0` 换成 `>=` 在 `0.0/-0.0/None/-5/5e9/nan/inf` 上读数完全相同 ·
  `tolerance*100` 换成 `*101` 印出来都是 `'10'` · `text=True` 关掉后 `json.loads` 照吃 bytes。
  **不硬凑 100%**——把这 12 个也凑上只会让读数失去意义。

commit `f7eae012` · 全文判词与下一步 [`data/research/audit_mutation_2026-09-22/README.md`](../audit_mutation_2026-09-22/README.md)

### ② 普查再推两格：11 道 → 13 道

`audit_reads_declarations` **34/52（65%）** · `audit_event_agreement` **54/83（65%）**，两道都是首次有读数。
两道都单次跑，按这把尺子的历史漂移带 ±6pp 不确定度，README 已注明别和上面那个三试的 82% 并排比。
还剩 6 道没量过，最大的 `audit_ci_test_coverage` 有 162 个变异点。

commit `16400b29`

## 四、收尾

**测试**：`pipeline/tests` + `tests` 两根一起跑，**3333 passed / 1 skipped / 12 deselected**，全绿（5 分 59 秒）。

**已合的 commit**（gate=none；⚠️ 推之前 rebase 过一次，下表是 **origin/main 上的真 sha**，不是本地那批：`git diff --stat origin/main...HEAD` 自查：4 文件、530 行全是新增、**0 删除**，全在本线边界内）：

| commit | 内容 |
|---|---|
| `3cd7e67b` | 本晨报开工 |
| `f7eae012` | `audit_universe_population` 测试 9 → 29 条 + 研究档 |
| `16400b29` | 两道新读数 |
| `0a62b349` | 本晨报收尾 |
| `935c51ee` | 素材箱追一行 |

**待合分支**：无。本线在 origin 上还留着两条已合完的分支 `agent/linda/T-0921-100`（`76550aa8` 已在 main）与
`agent/linda/T-0922-67`（`c6461e3b` 已在 main）——**内容都已进 main，属「可清」档，删分支仍等 Andy 点头**，由滞留闸跟进，本班不删。

**收藏夹判定清单**：本轮无新条目（INBOX 🔗 节最后一条是 09-12，已 ✅），不适用。

**预览稿分数表**：本夜未做 UI 预览稿（2.5 节），全部时间给了第①件——按任务书「宁可 1 件做透」。

**只差 Andy 一步的交付**：无。本夜产出全部 gate=none 自合，不需要他回任何一个字。

**没动的大件**：`audit_universe_freshness` 38%（现在最低的一道）· 还没量的 6 道 ·
`data/reference/DATA_RELIABILITY.md` §六第 6 条那四条欠账（都要动 `.github/workflows/tests.yml`，不在本线边界）。

**下轮第一件事**：照本夜这套做法打 `audit_universe_freshness`（21/56，38%）——
它和 `universe_population` 是同一次事故的两半，测试欠账大概率是同一个形状（边界、`main()`、读取端）。
