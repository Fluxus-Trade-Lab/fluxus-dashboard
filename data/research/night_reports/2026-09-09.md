# 夜间组晨报 · 2026-09-09（JST）

> ET now 2026-09-08 15:41（交易日，尚未收盘）| last completed session **2026-09-04** | today is trading day True
> 分支 `auto/night-20260909-7728f0` · 开工 04:41 JST

## 一、回执

| 收到的 | 状态 |
|---|---|
| 我 09-08 收工三问 ③「下轮第一件事＝看 09-02 的 36 行与 08-19 的 12 只有没有被 DATA ALEX 处理」 | ✅ 已查：**一行没动**。`audit_progress` 5 violations 逐条不变、`audit_event_agreement` 三天声明仍在；09-06 以来碰过 `data/history/` 的只有 cron 的两条 `chore: market data`。读数已追进 `DATA_RELIABILITY` §六 |
| Plumber Joe 09-08 更正：`fbclock` 那行 push 命令会回退 main 192 个 commit，正确做法是先 rebase | ✅ 已知悉。**我 09-07/09-08 晨报给的那行命令是错的**，本夜起不再复述；Joe 的四行 rebase recipe 才是投递物 |
| Joe 09-08 ①：`fbclock` 第 5 晚未合，他已验收 2251 passed | 不适用（`pipeline/tools/` 在我白名单外）——仍列门铃 |
| Joe 09-08 ②：09-07 云产线零留痕 → 归 OPS | 不适用（云 routine 归 OPS） |
| Joe 09-08 ④：`sentiment.json` 死文件 / schema 基线欠更新 | 不适用（`data/output` 归 DATA ALEX） |

## 二、本夜做了什么

### 1. 主活：Delayed EP 账本第二次复盘 —— `breaking` 等到了分辨率，然后什么也没说出来

研究档 [`delayed_ep_review_2026-09/results_2026-09-09.md`](../delayed_ep_review_2026-09/results_2026-09-09.md) ·
新脚本 `fetch_bars.py` / `join_by_date.py` / `stage_read.py` / `label_permutation.py` ·
原始输出 `run_2026-09-09_*.txt` · 全套测试 **1710 passed / 6 skipped**

**先读了已有规划**：这个复盘 09-01 已经做过一轮（`results.md`），所以本轮不是重做，是**续跑它当时答不了的那一问**。
那轮的结论是「`breaking` 在 +10 日上 0 个可用观测，它还没被测过」。账本从 648 行 / 12 场长到 **837 行 / 17 场**，
现在 +3/+5 各 **n=29 / dates=8**。

**测了，是空的，而且「空」的两个方向都要写清楚**：

- 预注册方向（Pradeep：第二次突破是**更好**的交易）**三个 horizon 全是反号**，`p_high` 0.81 / 0.94 / 0.91。
- 但「`breaking` 更差」**同样不成立**：场内置换检验 p_low 0.203 / 0.073 / 0.093，×3 校正后没有一个到 0.05。
- **看起来像结论的那个负号，量的是别的东西**——POOLED 自己就是 −0.5% / −0.7% / −1.0%，
  +3d 与 +10d 的 CI 同样排除零。**这 17 场里整个 EP 候选池跑输 SPY**；拿零当参照量到的是这个，不是标签。
  stage 标签的正确参照是「**同一场内随机抽同样多只票**」。

### 2. 顺手抓到的：09-02 那一场的第三个证人，而且是外部的

复盘自带的 join check（账本存的 close vs 今天重拉的同一根 K 线）把偏差**全部**定位在一天：
**全账本 31 行偏差 >0.5%，31 行全在 2026-09-02**，其余 16 场 0 行。
而且是**方向性**的：那 36 行 **0 行**配当日的厂商 K 线、**36 行**配 09-01 的；其余 16 场全是「配当日＝全部行」。
**16 场阴性对照 + 1 场满分阳性。**

前两个证人（`audit_event_agreement` 跨归档、`audit_progress` 对交易日历）都是我们自己的档；
**这一个是拿厂商回看比的**——三种不同的比法指同一天。
⚠️ 边界写在研究档里：我们的扫描器本来就从 yfinance 取数，独立的是**取数的时刻**不是数据源。

## 三、待合分支 / 已合 commit

（进行中）

## 四、门铃待按（只列不按）

（进行中）

## 五、收工三问

（进行中）
