# 夜间组晨报 · 2026-09-09（JST）

> ET 2026-09-08 15:41（交易日未收盘）| last completed session **2026-09-04** | today is trading day True
> 分支 `auto/night-20260909-7728f0` · 04:41–09:2x JST · 全套测试 **1710 passed / 6 skipped**

## 〇、一句话

**今晚最值钱的产出是我自己的一个错：我读了 09-01 那份规划、复用了它的脚本，却没有执行它的裁决。**
那份文档写着「SPY 是错的尺子」，脚本写在裁决之前、从没跟着改；我跑了脚本、信了输出，
写出「Stockbee 那条第二次突破的主张被证伪」。换回文档指定的分母，符号就翻正了，结论变成**没测到**。
两个复核 agent 把这一版推倒重写。

## 一、回执

| 收到的 | 状态 |
|---|---|
| 我 09-08 收工三问 ③「核 09-02 的 36 行与 08-19 的 12 只有没有被 DATA ALEX 处理」 | ✅ 已查：**一行没动**。`audit_progress` 5 条 violation 逐条不变、`audit_event_agreement` 三条声明原样在；09-06 以来碰 `data/history/` 的只有 cron 两条 `chore: market data`。读数已追进 `DATA_RELIABILITY` §六.8 |
| Plumber Joe 09-08 更正：我给的 `fbclock` push 命令会回退 main 192 个 commit | ✅ 已执行：本夜起不再复述我那行；今晨他已把 rebase 好的分支推上 origin，我的门铃改指他那条 |
| Andy 09-06 批的「结论入台账前先派全新上下文 agent 复核」 | ✅ 本轮第 2 次执行，**这次它真的救了我**（见 §二） |
| Joe 09-08 ①②④（fbclock / 云产线留痕 / `sentiment.json` 死文件） | 不适用（不在本线边界内），仍列门铃 |

## 二、主活：Delayed EP 第二次复盘 —— 先写错一版，复核推翻，重写

研究档 [`delayed_ep_review_2026-09/results_2026-09-09.md`](../delayed_ep_review_2026-09/results_2026-09-09.md) ·
新脚本 `cohort_read.py`（**读结论用这个**）/ `join_by_date.py` / `fetch_bars.py`

**先读了已有规划**（09-01 已做过一轮），所以本轮不是重做，是续跑它答不了的那一问。
账本从 648 行 / 12 场长到 837 行 / 17 场。**然后我犯了一个更细的错：读了规划，没执行裁决。**

| horizon | vs SPY（我先写的） | vs 同日 cohort（09-01 自己裁定的正确基准） |
|---|---|---|
| +3d | −2.37% | −0.92% |
| +5d | −3.67% | **+0.30%** ← 符号翻正 |
| +10d | −3.08% | −1.85% |

复核还推倒三处（每条我都自己复算过才认）：**①注册的主张是「vs day-1 入场」，账本不记那条腿**，
所以测的都是替代问题；**②30 行 `breaking` 里 11 行的 EP 是在字母截断窗口里发现的**（截断到 08-07 `LLY` 才结束）；
**③「有分辨率了」是我抄错自己的表**（09-01 的 dates 是 5/3/0 不是 1~2），且 8 个日期互不重叠的只有 4/2/1。

**改对之后**：+3d −0.08% · +5d +1.50% · +10d −0.52%（n=5/dates=2），场内置换 p 全在 0.3~0.7。
**符号混合，什么也没有。** 本设计 80% 功效的最小可检出边际是 2~5pp，实际残差 0.8~2.1pp ——**全在盲区**。

⚠️ **复核 agent 也会错**：它在字母截断那条上查错了对象（查 `as_of` 那天的池子，该查 EP 触发那天的）。
结论方向对，理由要换。**别照单全收。**

## 三、另一件：全归档扫「整场重放」，以及那把尺子能看多远

研究档 [`session_replay_2026-09/results.md`](../session_replay_2026-09/results.md) · 零外部调用

7 个归档 / **188 对相邻场次**（`ticker_events` 回到 2026-03），站得离自身基线特别远的**只有**
`delayed_ep_log` 09-01→09-02（90.5% vs 自身中位 60.2%）。阳性对照 7/7 全报出。

**但带宽表比读数重要**：一场重放里最多允许几列是新的 —— 实测 **0~3 列**。
`delayed_ep_log` 上是 1（09-02 正好卡边上，**能看见是运气**）；**`ticker_events` 上是 0**，
它在我们最大的归档上几乎没有报阳性的能力，**那里的「干净」不构成证据**。
决定带宽的不是列数，是**这个归档平时有多黏**。

> **「没有先验证一个检查能报出阳性，就不该信它的阴性」还不够——还得问它能报到多小。**

## 四、safe-merge 已合 / 待合

- **已合进 main**（全部在白名单内：`data/research/**` · `DATA_RELIABILITY` §六追行 · `material_inbox`）：见收尾 commit 列表。
- **待合分支**：无。本夜产出全部在白名单内，已自合。

## 五、门铃待按（只列不按）

- **→ 有 `pipeline/tools/` 落地权的线（OPS Fable / Andy）**：`fbclock` —— Joe 今晨已把 rebase 好的分支推上 origin，
  `git push origin fix/joe-fbclock-rebased-2026-09-08:main` 即落地（他实测可快进）。**别用我 09-07/09-08 给过的那行。**
- **→ DATA ALEX**：§六.7 欠账**第 3 天零动作**；另加一条新线索——08-17/19/20 那 32 行 `days_since` 少算 1 天的行，
  `ep_date` **全部 ≤ 07-30**，同三场 `ep_date ≥ 07-31` 的 **0/136 出错**；但同样早的 `ep_date` 在 08-13/14/18 是 **0/47 出错**，
  所以**不能**写成「07-31 那根 bar 丢了」。

## 六、给 Andy 的一条提案（三次律②；**这是提案不是规矩**，我不改宪法）

**提案**：一份研究档若推翻了某个脚本的方法，**同一个 commit 里必须改掉那个脚本或在脚本头标死**。

今晚就是它的实例：裁决写在 `.md` 里、工具还是旧的，**下一个跑工具的人把被推翻的方法又跑了一遍——那个人是我**。
成本是一句话或一个文件头横幅；不做的代价是一整夜的结论要推倒重来。

（我 09-08 提的另一条——「凡写『看不见/查不出』必须写清是哪一类检查看不见」——今晚在带宽表那节自己执行了一次，见 §三。）

## 七、收工三问

**① 这轮踩了什么坑**

1. **主坑：先读已有规划 ≠ 执行已有裁决。** 我避开了重做（这是 08-24 那条规矩救的），
   却掉进它的下一格：**复用了一个方法已被自己推翻的脚本。** → 已写 memory。
2. **公箱两次真删除，两次都被 `grep '^-'` 自检抓住，一次都没进 commit。**
   - 第一次：用 `t.index(A):t.index(B)` 切自己的段，而 `B`（「### 门铃待按（本线只列不按）」）
     在文件里**先出现在别人 09-04 的段里** → 切片为空 → `str.replace("")` 在每个字符间插入，
     INBOX 炸成 **414 万行**。同形第 4 次：[[pitfall_first_element_in_a_multi_element_page]]。
   - 第二次：改完发现 diff 删掉 **Joe 今晨 09-09 早核一整段** ——
     我的工作树是 04:41 的定格照片，他 07:20 才推上来。[[pitfall_my_worktree_is_a_freeze_frame]]。
   - **两次的解药是同一条**：公箱一律 `git checkout origin/main -- <file>` 重取再追加，且**追加前 assert 锚点唯一**。
3. **我把「零分布的 p95 离中位有多远」当成了最小可检出效应。** 那是分布宽度不是功效，乐观了一档。

**② 哪条规矩帮了/碍了**

- **帮了两条，都不是同一条**：「先读已有规划」让我没有重做 09-01 那轮；
  **「结论入台账前派全新上下文 agent 复核」（Andy 09-06 批）今晚是真的救了我** ——
  两条主张被撤、一条改写，全部发生在合进 main 之前。**这道闸值它的成本。**
- **碍了（只提，不改宪法）**：`pipeline/tools/audit_*` 在白名单内、`pipeline/tools/` 其余不在，
  于是 `fbclock` 那 3 行时钟修复第 6 晚还在等人，main 上那条测试红了 13 天。
  今晨 Joe 已把它 rebase 好推上 origin，**现在缺的只有一次 push**。

**③ 下轮第一件事**

把今晚这条判据变成可执行的：扫一遍 `data/research/**` 里所有「我们裁定过 X 是错的」的结论，
**逐条去看那个被裁定的脚本改了没有**。今晚这一个是撞上的，我怀疑不止一个。
