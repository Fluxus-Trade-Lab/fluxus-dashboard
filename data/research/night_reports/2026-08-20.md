# 夜间自学 2026-08-20（JST）

**ET now 2026-08-19 15:36（盘中）· last completed session 2026-08-18 · today is trading day True**
分支 `auto/night-20260820-71592d`（工作树开在本会话 scratchpad，共享树没碰）。今晚 21:30 UTC 的 cron 还没跑，所以 `todo_cron_check_2026-08-20` 那三条（429 四轮重试、ma_reclaim 第二晚、fundamentals）**今晚仍验不了**，顺延到 08-20 早的晨核窗口。

---

## 一、做了什么

### 1. I4 的阈值量过了，结论是**拍到了错的单位** ⭐

起因是 `DATA_RELIABILITY.md` 里自己写的「I4 的阈值(0.3×/3×)是拍的」。量下来比"太松"更糟：

- `ticker_events.csv` 96 个可检查 session，每日总行数对前 20 日中位的比值只走过 **[0.63, 2.28]**。`[0.30, 3.0]` 这道闸**一次都没响过**，也响不了。量出来的带是 [0.58, 2.45]。
- **收紧它也没用。** 仓库自己的事故：Finviz 在 08-07 把 `Change` 改名 `Change %`（`e8ac440`，08-15 才修），`gainers_4pct` 与 `vol_up_gainers` 连着四个 session 写 **0 行**：

| session | 总行数 | ×中位 | 旧闸 | breadth 当日涨≥4% | I7 |
|---|---|---|---|---|---|
| 08-07 | 604 | 0.68 | silent | **956** | VIOLATION ×2 |
| 08-11 | 863 | 1.00 | silent | **530** | VIOLATION ×2 |
| 08-12 | 907 | 1.05 | silent | **527** | VIOLATION ×2 |
| 08-13 | 1005 | 1.17 | silent | **617** | VIOLATION ×2 |
| 08-14 / 17 / 18 | — | 1.42 / 2.11 / 1.88 | silent | 511 / 349 / 320 | clean |

  `is_plausible_day` 也放行了 —— 它只要 7 个筛选器里 ≥4 个有行，那天正好 4 个。**旧闸 0/4，I7 4/4，三个健康日零误报。**
  「涨 4% 的是 0 只」可不可能？breadth 归档 **566 个 session，`up_4pct` 最小值 15**，低于 10 的 0 天。所以那是 bug，不是行情。
- 所以检查单位从**文件**换成**系列**：`ticker_events.screener` / `watchlist_hits.panel` / `groups_archive.kind`。**I7**：registry 里定级 `reliable`（出现率 ≥95% 且中位 ≥20 行）的系列当天 0 行 = **违规（拒 commit）**；**I4s**：系列行数越出自己的带 = 警告。
- registry `data/reference/i4_bands.json` **提交进仓库、只在人显式 `--update` 时动**（跟 `schema_snapshot.json` 一个规矩）。理由是实测的：滚动重算只报 08-07 和 08-11，08-12/13 就不报了 —— 窗口装进两个空日后出现率跌破 95%，**故障把基线教成了正常**。冻结的报全四天。

**验证**：`pytest pipeline/tests` 全绿（**722 passed**，按规矩 deselect `test_content_processor`）；新增 7 个测试（I7 违规 / sparse 空了不响 / 系列带警告 / 量出来的带确实被用上 / 定级 / `--until` 冻结 vs 滚动 / 带的护栏）。今晚在真归档上跑 `audit_archives`：**0 violations / 3 warnings**。

文件：[calibrate_i4.py](pipeline/tools/calibrate_i4.py) · [audit_archives.py:130](pipeline/tools/audit_archives.py#L130)（I4/I7）· [i4_bands.json](data/reference/i4_bands.json) · [研究说明](data/research/i4_calibration_2026-08/README.md) · [离线回放](data/research/i4_calibration_2026-08/replay.py) · [DATA_RELIABILITY §六新写](data/reference/DATA_RELIABILITY.md)

**第一晚就捡到两条新警告**（08-18）：`preset:pp_count` 183 行 = 中位的 3.16×；`preset:weekly_20_gainers` **1 行** = 中位的 0.06×。后者值得看一眼。

### 2. 网页优化：watchlist 的 ATR 位（只出预览稿，`frontend/` 一行没改）

先更正一条记忆：`todo_watchlist_ui_fields` 的「ATR 徽章负值会全绿」**已不成立**，`66d4d6f`/`01bf587` 之后负值早就不上色。留下的是另一个洞 —— `atrFill` 对 `v<0`（在 50 日线下方）和 `v==null`（没测出来）都返回 `null`，**一个可交易的读数和一个数据缺口用同一个外观说话**（正是 pitfall`一支 token 两个职责`）。

| | 简洁整齐 | 反多巴胺 | 文案只留交易内容 | 不新增颜色 | 让推理被看懂 | 决策优先 | 合计 |
|---|---|---|---|---|---|---|---|
| [v1 形状通道](data/research/ui_previews/2026-08-20/watchlist_atr_v1.html) | 2 | 2 | 2 | 2 | **1** | **1** | 10 |
| [v2 刻度锚](data/research/ui_previews/2026-08-20/watchlist_atr_v2.html) | **1** | 2 | 2 | 2 | 2 | 2 | 11 |
| [v3 折底降灰](data/research/ui_previews/2026-08-20/watchlist_atr_v3.html) | 2 | 2 | **1** | 2 | **1** | 2 | 10 |
| **[v4 第 2 轮 · 胜出](data/research/ui_previews/2026-08-20/watchlist_atr_v4.html)** | 2 | 2 | 2 | 2 | 2 | 2 | **12** |

扣分位置、每一条的理由和实测对比度表都在 [预览稿 README](data/research/ui_previews/2026-08-20/README.md)。三版都没触判负条件（新增颜色 / 鸡汤 / 动效）。

关键实测：**ramp 相邻档彼此只有 1.06–1.62:1**（以前只量过墨对底，没量过档与档）。颜色能说"哪一档"，说不了"哪个数" —— 所以胜出稿把读数编成**位置**（0→10 的微型轴），颜色退成第二通道。

**改它能让读者多知道什么**：一眼横扫就知道每只票离 50 日线多远，不用逐个读数也不用记颜色；线下方的票自己让出主视线；「没测出来」第一次有了自己的样子，不再被当成一只在均线下方的票。

---

## 二、建议 Andy 决定的（每条一句话 + 依据）

1. **I7 违规要不要拒 commit？** 现在会。依据：冻结回放上 0 误报，breadth 566 天 `up_4pct` 下限 15 撑得住；但这是第一晚，可以先降成警告观察一周。
2. **`vcp` 要不要算 reliable？** 中位 19 行、出现率 100%，差一行没过 20 的门槛；门槛改 15 它就进。
3. **`preset:weekly_20_gainers` 08-18 只有 1 行（中位 16）** —— 是行情还是抓取半截，要不要我查。
4. **v4 预览稿要不要落地到 watchlist？** 落地是前端会话的事，我只交稿。
5. **light 模式 ATR≈9 的 ticker 墨在我这份简化 ramp 上是 3.95:1**（低于 4.5）；线上 `atrFill` 有 L\* 修正、数值会不同 —— 要不要我对着真函数复量一次再说。

## 三、积压里仍没动的大件

- `todo_cron_check_2026-08-20` 三条（429 重试 / ma_reclaim 第二晚 / fundamentals 限速）—— 今晚的 cron 还没跑，**必须等 08-20 早**。
- Delayed EP `--review` —— 目标日 ≥08-21（现在只有 3 个记录 session，不够）。
- GEX 覆盖核对 —— 要盘中跑且 OI 需实时订阅，夜间做不了。
- I8「整段缺失的 session 没人查」：`2026-08-10` 是交易日、`ticker_events` 一行都没有，而 `universe_quality` 有那天的行。I5 只看最新一天，中间的洞看不见。已写进 §七 待办第 1 条。
- universe 行数 vs Finviz 宣称总数（adapter 不存那个数）。
- Library 五页 + `rs-live` 仍是 `Placeholder`，是从零建不是改版，等 Andy 方向。
