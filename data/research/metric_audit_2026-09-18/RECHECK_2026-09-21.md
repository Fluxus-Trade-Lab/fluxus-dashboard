# 自造数字复查 · 09-21 复核（DATA ALEX）

Andy 2026-09-21：「给我自造数字复查9/18的更新」「更新出最新的自造数字复查」。报告页：https://claude.ai/artifact/R3jYpbGbo7AfMfHGVVBq2x （Version 2）。

| 范围 | 09-18 | 09-21 |
|---|---|---|
| 原作者有定义、我们改了（⚡） | 16 | 1（Sugar Babies，待 Andy A/B） |
| 自造且未登记（A 18 · B 27 · C 33） | 78 | 0（改回原文或登记进 METRIC_SOURCES，约 30 条「理由未留档」） |
| 前后端双份规则不一致 | 7 | 0（UI Claire 11 件，`8c52d939` `d0d6a346`） |
| 09-21 复核新挖真偏差 | — | 4，已修：百分位混口径、Conditions 输入与 `thrust` 撞名、Short List EP 旧配方（`d8c52172`）；VIX/VIX3M 丢 1.1（`cefe2255`） |

复核方法：两个只读 agent 分别逐条核 B 表 37 项、C 表 47 项与 A 表 18 字段的现状（读代码与 METRIC_SOURCES，不看 commit message 下结论），主会话现场抽查三条最重的（`breadth_signals.py:836` / `:579`、`run_all.py:1418`）。随后修复与登记：`d8c52172` `0f8a364b` `cefe2255` `45eac8fe` `bb5cf66c` `45cd626b`。

还开着：Sugar Babies 计数单位（T-0921-40 needs_andy）· `gear` 停发等 UI Claire 撤页（T-0921-43）· `ts_state` 含义变化与 regime 文案分支（RND Linda，T-0921-42）· Stockbee 列百分位攒满 60 个交易日（4% 计数约 11 月底、季度约 12 月中）· 小尾巴：TI65/MDT `≥` 应为 `>`、资产层 52 周高用收盘最高、VCP L1 Trend Template 偏离未声明、「RS 21D/63D」标签、4 处过期注释、METRIC_SOURCES 第 58 行 ADR 出处。

## T-0921-40 收尾（09-21 18:xx，DATA ALEX）

上面「还开着」的六项，逐条核实现状全部已合 main，本表不再是最新状态：

1. **Sugar Babies 计数单位**：Andy 裁「B 照建议做」（needs-andy 问的是 A 维持 4% breakout / B 改回 Stockbee 访谈 9M 放量 EP 事件）——已实现，`1a49a5ff`（预设换成 Stockbee top 30 by 9M EP）+ `70c2c725`（并列票按最近一次 9M EP 排先后，不按字母）+ `bf3794bf`（剔除壳公司）；`45eac8fe`/`45cd626b` 同步了 `screener_methods.md` 与 METRIC_SOURCES 登记。原「⚡ 待 Andy A/B」这条至此清零。
2. **`gear` 停发**：UI Claire 先撤页面（T-0921-43 `fa7aee6f`，PASS），DATA ALEX 随后停发 + 同步账本（DATA_CONTRACTS §七 [2026-09-21] ↳ 回，`market_light.instrument_block()` 不再返回 `gear`；真正会拦闸的是 `audit_ledger.EVIDENCE`，已同步摘掉，非 `schema_snapshot`）。
3. **`ts_state` 含义变化**：RND Linda 过目（T-0921-42，`3b5ba08a`，done），regime_ledger 侧无异议。
4. **小尾巴 6 项**：`b11e56c1`（Andy 09-21「小尾巴也清掉。」）一次性修完——TI65/MDT 改严格 `>`；资产层 `high_52w_dist` 改读 52 周最高 High（盘中），与股票层同尺（不是「用收盘」，是与股票层的量口径对齐，两者都读盘中高点）；VCP 第一层补齐 Minervini Trend Template 150 日线三条 + RS≥70，200 日线斜率声明未实现；个股页标签 RS 21D/63D → RS 1M/3M；5 处过期注释清掉；METRIC_SOURCES 第 58 行 ADR 出处改正 + 新增 VCP 第一层登记行。测试 `test_audit_tails_0921.py` 7 red→green + 前端 `TickerStats.test.jsx` red→green。
5. **登记（二）**：METRIC_SOURCES.md 现有 45 行标「2026-09-21 登记」（B 表 23 项 + C 表 26 项，覆盖任务原文「B 22 · C 24」两份报告的全部未登记条目；差额是同一提交里顺带登记的相邻项）；`bb5cf66c`（补 45 行）+ `45cd626b`（补 ema21_watch/turin 四态/Sugar Babies 三行）。
6. **一、真偏差 1–5**：均已在本文件上半部分记录为已修（`d8c52172`/`0f8a364b`/`cefe2255`）。

全仓测试收工前复跑一遍：`python3 -m pytest pipeline/tests tests --deselect pipeline/tests/test_content_processor.py` → 3194 passed, 1 skipped, 12 deselected（无红）。任务 T-0921-40 验收三条（一/二/三 + 复核表追加）至此全部满足，`done`。
