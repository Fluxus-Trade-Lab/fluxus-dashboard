# 宇宙翻倍之后，牌面上那个词没跟着改

**2026-09-18 夜班（Nighty Zac）** · 复现：`python3 data/research/breadth_universe_break_2026-09-18/measure.py`
（在基于 `origin/main` 的树里跑；两张表全部由脚本打印，本文不手抄数字）
**独立复核**：两个只读 agent 各自从头核过一遍（一个对抗性证伪、一个从头复算），结论与数字一致，
并纠了本文两处措辞、拆穿了第五节那个 +39 的解读、指出宇宙的人口也换过——后一条我自己追到了确切的那一晚（第八节）。
**闸**：`pipeline/tests/test_frontend_count_thresholds.py`（本班新增，已合）

## 一句话

2026-08-10 起 Finviz 宇宙从约 2,600 只翻到约 5,620 只，**后端当天就把 thrust 的门槛改成了按宇宙缩放，前端那张牌面上的 300 没人动**——
27 个交易日里 **20 天**牌面上的词和引擎自己的读数对不上，"churn / volatile" 在牌面上出现在 **55.6%** 的日子，
而它在此前 559 场里只出现过 **0.5%**（3 天）。
往上游再追一层：**真正的断点不在 08-10 而在 06-26**——那一晚 ≥$10 亿市值那道筛选条件不再生效，
一千六百只小票进了宇宙，名单撑爆翻页上限，半字母表事故是它的下游（第八节）。
牌面在一个默认关着的折叠块里（`Reference label="Summary tiles"`，关着时根本不 mount），
所以准确说法是**每有人点开一次就误导一次**，不是每天都在误导——也正因为没有日常目击者，它错了 27 场没人报。

## 二、事情的形状

| | 家数门槛 | 出处 |
|---|---|---|
| 后端 | `max(60, 0.113 × universe_size)` = 今天 **634** | `pipeline/screeners/breadth_signals.py:83` `thrust_count()`，`16db6a88b`（2026-08-09）|
| 前端 | 硬编码 **300** | `frontend/src/components/breadth/MarketStateSummary.jsx:15-17` |

`16db6a88b` 的 commit message 自己写了这件事该怎么防：

> state_board imports the same function rather than keeping its own copy of the constant,
> **which is how the two would have drifted**

后端那一侧照做了（`state_board.py` 去 import 那个函数）。**前端这一份拷贝没人知道它存在**，于是它就是被预言的那次漂移。

`0.113` 不是随手取的：它＝ 300 ÷ 老宇宙（2,647）——就是"把历史操作点原样搬过去"。所以**300 和 0.113 是同一条线在两个宇宙下的两个写法**，
问题不是谁对，是宇宙翻倍之后只有一边跟着走。
断点**之前**两边也不是完全一致（22/559 = 3.9% 的日子不同），所以更准的说法是：
**08-10 把一个近似正确的常数变成了系统性错误**，不是"它一直是错的"。

## 三、量出来的

断点后 27 场（`measure.py` 第一节）：

- 牌面 vs 引擎**不一致 20/27 场**；断点前是 22/559 场（3.9%）。
- `churn / volatile` 出现率：牌面 **0.5% → 55.6%**；引擎 0.4% → **0.0%**。
- 最近一场 2026-09-16：up_4pct 292 / down_4pct 580。牌面写 **bearish thrust**（580 ≥ 300）；
  同一份 `breadth.json` 里引擎自己写的是 `{"key":"thrust","side":"neutral","line":634.043,"margin":-342.043}`。
  **同一个页面上，同一份 JSON，两个相反的话。** 那块砖的颜色（`tone`，`MarketStateSummary.jsx:36`）也由这个词决定，所以红绿也跟着错。

## 四、修法（`frontend/` 是 UI Claire 的文件，本线一个字节没改）

payload 里**已经带着正确的线**：`verdict.vote_detail` 里 `key == 'thrust'` 那条的 `line` 字段（今天 634.043）。
所以修法是把 `MarketStateSummary.jsx` 的三个 `>= 300` 换成 `>= line`，取不到时再回落到 300。
⚠️ 别指望从 `mm` 里算：`mm` 块只有十二个计数字段，**没有 `universe_size`**（实测 `mm.universe_size` 是 None）。
线在 `verdict` 那边（`verdict.vote_detail` 的 thrust 条、`verdict.universe_size`），**后端 payload 不用改**。
`VoteGlyphs.jsx:36` 的 `RANGE.thrust = 300` **不是**这个问题——那是画图的量程（半幅），不是判定线。
但它已经失准：今天 margin = −342，`VoteGlyphs.jsx:96` 的 `Math.max(-1, …)` 把它夹死，那根柱子**顶格**，
08-10 之后常态超出 ±300，这枚 glyph 已经没有分辨率了。是美观缺陷不是读数错误，修法同样是读 payload 里那条 `line`。

## 五、顺带量到的第二处：历史分位在跨宇宙比

`breadth_signals.percentile_context()` 把当日读数排进**整个归档**的分位，其中四个是**原始家数**
（`up_4pct` / `down_4pct` / `nh_nl_net` / `qtr_spread`），另三个是比率（`ratio_5d` / `t2108` / `mcclellan_osc`）。
归档 586 场里只有 4.6%（27 场）来自新宇宙，其余全是老宇宙——于是新宇宙的家数天然排在历史高位。
前端 `MarketStateSummary.jsx:35` 那行 `down-4% {n}th pctile` 印的就是这个数。

把同样的算法换成「家数 ÷ 当日 universe_size」再算一遍（`measure.py` 第二节），差出来是：
`up_4pct` 中位 **+39** 分位点、`down_4pct` **+35**、`nh_nl_net` +5、`qtr_spread` −3。

⚠️ **但这个 39 不是误差的大小，复核时被一个安慰剂对照拆穿了**：对本来就免疫规模的三个比率
（`ratio_5d` / `t2108` / `mcclellan_osc`）做同样的「÷ universe_size」，分位照样掉 **32–37** 个点。
除以一个在长大的分母本身就会把分位往下压——所以 **+39 里有一大半是这个机械效应，不是修正量**。
两种口径的差值**不能**当作「比率化修对了多少」的证据。

这一节能说的只有方向，不能说幅度：家数分位跨的是两个规模、两个人口（见下一节），**读数系统性偏高**；
至于高多少，本轮**没有测出来**。老实的处置是**不跨断点排分位**，不是改成比率就完事。

## 六、这轮没有说什么

- **没有说 0.113 是对的门槛。** 它只是把 300 在老宇宙里的操作点搬了过来，本身没有外部口径背书
  （`METRIC_SOURCES.md` 里 `up_4pct` 登记的是"只有涨幅一条件"的自造口径，标准 Stockbee 4% 口径是另外两列 `up_4pct_stockbee` / `down_4pct_stockbee`，2026-09-04 才开始有值，8 行）。
  本文量的是**两边不一致**，不是哪一边更接近真相。
- **没有改任何前端或管线文件。** 两处修法都在别人的边界里，已按契约行 + 门铃路由。

## 七、复核时多查出来的（我逐条自己核过，不是照抄 agent）

1. **后端自己也留了两句 300 的散文，而且是给用户看的。**
   `pipeline/screeners/breadth_signals.py:405`（`_PLAYBOOK['OVERSOLD']`）写「wait for a 300+ up-4% thrust day」，
   `:420`（`_GUIDANCE[('OVERSOLD','Elevated')]`）写「the next 300+ up-4% day is the signal that matters」。
   两句都随 `verdict.playbook` / `verdict.guidance` 上前端。T2108 跌破 20 的那天，页面会叫人等一个 300 的日子，
   而同一份 payload 里 `vote_detail` 写着需要 634。**这两行在管线里，不是前端能修的**——归 DATA ALEX。
   （今天 env=BEARISH，这两句没上台；它们在下一次 OVERSOLD 时才出现。）
2. **`0.113` 没有登记。** `METRIC_SOURCES.md` 里 grep `0.113` 零命中，thrust 门槛一行都没有。
   按宪法「先找口径，别自己造」，自造量要登记、要写明偏离了哪个标准。它现在只活在
   `breadth_signals.py:20-31` 的注释里（那段注释自己用的词是 **tuned**——照 2,650 只的老宇宙反推的工作点）。
   连带一件同样没登记的：thrust 判定用的分子是 `up_4pct`（**只有涨幅一条件**的自造列），
   不是 `METRIC_SOURCES.md:85` 登记的 Stockbee 口径 `up_4pct_stockbee`。2026-09-16 两者差一倍：292/580 vs 159/246。
3. **前端这半边没有测试。** 后端 `pipeline/tests/test_breadth_signals.py` 有四个测试钉住缩放行为
   （`thrust_count({'universe_size': 5615}) == 634`）；`frontend/` 那边 grep `thrust` 零命中。
   闸只装在写规则的一侧，拷贝规则的那一侧裸奔——本班补的 `pipeline/tests/test_frontend_count_thresholds.py` 补的就是这个位置。

## 八、真正的断点不在 08-10，在 06-26——那天换的是人口

复现：`python3 data/research/breadth_universe_break_2026-09-18/population_break.py`
（快照按它自己的 `timestamp` 转 ET 定场次，**不按 git 提交日**——`DATA_RELIABILITY.md` §256 记过那个坑，本脚本第一版也栽在同一处，注释里留着。）

| ET 场次 | 只数 | 市值中位 | <$1B | 字母序最大 |
|---|---|---|---|---|
| 2026-06-25 | 2,588 | **$5.70B** | **0.0%** | ZYME |
| **2026-06-26** | **3,000** | **$0.78B** | **53.4%** | **LOCO** |
| 2026-08-06 | 3,000 | $0.77B | 53.6% | LNTH |
| 2026-08-11 | 5,619 | $0.76B | 53.7% | ZYME |
| 2026-09-16 | 5,611 | $0.72B | 54.3% | ZYME |

**一晚之间，中位市值从 57 亿掉到 7.8 亿，$1B 以下的从 0.0% 变成 53.4%。**

逐票对账（同一个脚本）：

- 同名票的市值相对变动中位 **0.0183** ——**字段没换口径，换的是名单。**
- 新进 **1,613** 只，市值中位 **1.43 亿**，**99.4% 在 $1B 以下**。
- 掉出 1,201 只，其中 **1,166 只字母序 > L** ——那是翻页截断，不是被筛掉的。
- A–L 且 ≥$1B：1,422 → 1,397。**老那批人没少，是新来了一批。**

所以 06-26 那一晚是**两件事**，事故档只记了第二件：

1. **`cap_1.0to`（≥$10 亿市值）不再生效**，约 1,600 只小票一次性进来 —— **这件至今没有回去**；
2. 名单当场超过 3,000，150 页的翻页上限开始截断（A–L）—— 这件 `2f782b53` 在 08-10 修好了。

**①推着②发生**：不是先有截断才有变化，是先多了三千只名字才撑爆了翻页上限。
`pipeline/adapters/finviz_adapter.py` 的筛选串 `f=cap_1.0to,ind_stocksonly` **一字未改**（复核 agent 逐个 commit 查过），
所以这是上游行为变了，我们这边没有任何一次提交记录过它。

**这件事改写了 `coverage_gaps.json` 现在那条声明的含义**：它把 08-10 写成「universe size 约 2,590 → 约 5,620」，
读起来像「同一批东西变多了」。实际上 2,590 是**≥$1B 的那一批**，5,620 是**全市值的那一批**——
**跨 06-26 比任何全宇宙家数，比的是两个人口，不是两个规模。**
而 08-10 只是把 M–Z 还回来，人口在 06-26 就已经换完了。

复核 agent 另外量了这批新人口的行为差（我未独立复算，按线索记，供数据端核）：
同日核心（≥$1B 且日成交额 ≥$2M）与非核心的 4% 日发生率，2026-09-16 是 2.57%/5.85% 对 7.40%/14.08%——
**新来的这批，4% 日的基础频率是老那批的一到三倍**。如果属实，那么连「家数 ÷ universe_size」也仍然不可比：
分母换了人口，新人口的基础发生率更高。**这是第五节不给幅度的第二个理由。**

## 九、给数据端的一句建议（只建议，不越界）

`data/output/run_ledger.jsonl` 里每晚都有 `tradeable`（市值 ≥$1B 且日成交额 ≥$2M，`pipeline/themes/__init__.py:31-32`），
实测 08-10 前后都稳定在 **2,445–2,562**——**它没有跨过任何断点**。
所以「钉在固定质量层上算宽度」这条路，仓库里已经有现成的分母了；
`record_high_pct` / `high_low_index`（08-28 起）也是比值型、对断点免疫的现成口径。
选哪条归 DATA ALEX，本文只把三条路摆在这里。

## 十、06-26 那晚之后，页面上那几张单子换了什么人

复现：`python3 data/research/breadth_universe_break_2026-09-18/panels_after_the_break.py`
（分类器 `population_reach.is_core`：按当日 `universe.json` 快照的 `market_cap`，$10 亿为界——**自造分类**，界取自 06-25 之前 `cap_1.0to` 实际生效时的效果。）

| 发布的单子 | 06-25（断点前一场） | 06-26（当晚） | 09-15 |
|---|---|---|---|
| `gainers_4pct` | n=215 · **<$1B 0.0%** | n=646 · **64.6%** | n=232 · **69.8%** |
| `vol_up_gainers` | n=36 · **0.0%** | n=301 · 64.5% | n=103 · **70.9%** |
| `momentum_97` | n=78 · **0.0%** | n=86 · 60.5% | n=169 · 38.5% |
| `healthy_charts` | n=136 · **0.0%** | n=105 · 31.4% | n=227 · 37.9% |
| `ema21_watch` | n=80 · **0.0%** | n=54 · 37.0% | n=331 · 40.2% |
| `watchlist`（走 tradeable 闸） | — | — | n=219 · **0.0%** |

**一夜之间，五张发布出去的单子从「一只小票都没有」变成「三成到六成半是小票」，至今如此。**
筛子本身**从来没有市值条件**——市值条件一直在宇宙那一层。所以这不是哪个筛子改了，
是**它们脚下那个"全市场"换了人，而它们原样照跑**。**没有任何一次提交决定过这件事。**

**边界要说清楚，别扩大**：`watchlist` / `shortlist` 走 tradeable 闸（市值 ≥$300M 且日成交额 ≥$2M），
**没有跟着变**（09-15 实测 <$1B 占 0.0%，219 只）。`run_ledger` 的 `tradeable` 计数 08-10 前后都稳在 2,445–2,562，
也印证了这条闸横跨断点没动。
所以本文说的是**没有闸的那几张单子**和**数家数的那一层**，不是整个 dashboard。

**这一节不判对错**：小票该不该进筛子单，是产品判断，归 DATA ALEX / Andy。
本文只说一件事——**它是 06-26 那晚被动变成这样的，不是有人决定的**，而变化至今没人记账。

## 十一、这个断点咬到哪些研究——三组只读 agent 分批查，我逐条核了承重的那几个

方法照 `half_alphabet_reach_2026-09-17` 的四步法，只是把「剔窗重算」换成「只留 `is_core == True` 重算」
（`population_reach.is_core`，$10 亿为界，自造分类）。三组分工：面板/Stockbee · amplitude/gate_role/adr_floor/screener_overlap · 延后EP/领涨/回放 + 归档层。

### ⭐ 最重要的一条：被污染的是**分母**，不是分子

十三份研究里，**命中样本几乎都是干净的**——面板、九个预设、`vcp`、`b4_gates` 的样本里 `<$1B` 占 **0.0%**，
因为它们自带 `marketCapMin = 1.0`（`frontend/public/data/screener-presets.json` → `preset_hits.py:103-108` 执行）。
**脏的是它们比的那条基线**：`scanner_event_study` 的对照是「同日从全票池里随机抽一只」，
而那个池子 06-26 之后塞进了三千只小票，**而这批小票在这段行情里差得多**。

我用**另一条数据路径**独立核了这一条（`price_panel.pkl` + `universe.json` 快照，不是 agent 用的 `event_bars.pkl`）——
每个场次的全宇宙、20 个交易日前瞻收益中位：

| 场次 | 核心（≥$1B） | `<$1B` | 差 |
|---|---|---|---|
| 2026-06-12（断点前） | +0.6%（n=2,436） | **池子里一只都没有** | — |
| 2026-06-26 | **+0.63%**（n=1,332） | **−4.12%**（n=1,338） | **−4.75pp** |
| 2026-07-02 | −0.24%（n=1,347） | **−7.53%**（n=1,334） | **−7.29pp** |
| 2026-07-10 | +2.83%（n=1,340） | −1.51%（n=1,345） | −4.34pp |

（agent 用 `event_bars.pkl` 算的是 +0.67/−4.22、−0.16/−8.03、+2.79/−1.94——**两条独立路径同一形状**。）

**后果：断点之后每一个「超额 vs 同日随机」都系统性虚高 1.5–2.5pp。**
最干净的例子是 `vcp`：它的命中里一只小票都没有，但超额从 **+1.60pp 缩到 +0.57pp**——
**它那个「✅ 有效」完全建在基线那一侧。** 这条同时打在 `b4_gates.md` 的「同日随机」列
和 09-17 半字母表重报里**所有**用同一基线的行上。

### 判定表（A 不受影响 · B 剔非核心后方向不变 · C 结论受影响/无法判定）

| 研究 | 跨 06-26 | 断点后 `<$1B` | 头条：原 → 只留核心 | 判 |
|---|---|---|---|---|
| 面板 C 段（`asof_all_panels`） | 是 | 命中 **0.0%** | 头条数一字不变；**基线**变：1st 超额 +2.36→**+0.88pp**、vcs +0.08→**−0.42pp**（由正转负） | **C**（分母） |
| 老 7 个筛子 | 是 | `gainers_4pct` 63.9% · `vol_up_gainers` 71.4% · `vcp` 0.0% | `vol_up_gainers` +5d 由 −0.60% 变 **+0.78%（变号）**；`vcp` 超额 +1.60→+0.57pp | **C** |
| `presets_backfilled` | 是 | 9 个预设 0–2.4%；`weekly_momentum_97` **64.3%** | 9 个逐字不变；`weekly_momentum_97` +0.08%→+0.33%，判词不变 | **B** |
| `b4_gates` | 是 | **0 / 3,866** | 无需重算 | **A**（但「同日随机」那列同病） |
| Stockbee ①「三条闸能分开」 | 是 | 61.1% | holdout +1.25pp/p=0.0019 → **+1.30pp/p=0.0023**；断点前 +1.33pp——三种洗法都过门槛 | **B** |
| Stockbee ②「过闸也跑不赢 SPY」 | 是 | 61.1% | holdout 中位 **−0.06% → +0.21%**（核心）/ **+0.54%**（断点前）——**「连零都没到」不成立** | **C** |
| Stockbee ③「4% 突破跑输 SPY 0.45%」 | 是 | 61.1% | −0.43/−0.47% → −0.12/−0.28% → **−0.03/−0.00%**（断点前）——**跑输的那 0.45pp 基本就是新进来的小票** | **C** |
| `amplitude` | 是 | 61.1% | 头条不变（幅度 ρ +0.296→+0.271）；**两句副结论要改**，见下 | **B**（含两条 C） |
| `gate_role` | 是（切点 06-16，离断点 7 场） | train **0.0%** / holdout 44.3% | 头条（train）0/14 与 6/14 不变；**holdout 列**：`vol_up_gainers` 幅度 +0.100R p=0.024 → p=1 | 头条 **A/B** · **holdout 列 C** |
| `adr_floor` | 是 | 42.3% | H3a −3.52pp（96/96 天）→ **−2.98pp p=1.2e-16（89/91 天）**；H1/H2 仍 NULL | **B** |
| `screener_overlap` | 是 | 两族：Python 筛子 35–75%，预设 0.0% | §1 嵌套恒等式仍 110/110 天；§3§4 受影响，见下 | **A + C** |
| `delayed_ep_review` 08/09 · `delayed_ep_window` · `session_replay` | 否 / 是（≤7.7%） | 7.7%–32% | 六格检验仍全不显著；相同率断点前 66.9% vs 后 68.9% | **B** |
| `leaders_tml` | 否 | **0.0%**（全史最小市值 $1.010B） | 不需重算 | **A** |
| `i4_calibration` | 是（它就在量这个断点） | 约 45% | 「I4 全史零触发」不变且更硬；06-26 ratio 1.51→**0.83** | **B**（含一句 C） |

### 逐条要改的副结论

1. **`amplitude`「方向 ρ≈0」是两段相反的东西拼出来的零**——我用它自己的 `study.py` 重跑：
   全样本 −0.006 (p=0.59) · 只留核心 **+0.050 (p=1.2e-4)** · 断点前 **+0.057 (p=3.8e-5)** · 断点后只留核心 +0.037 (p=0.25)。
   **「期望值驼峰形、两端都负」只在断点之后成立**：断点前 Q1→Q5 是 −0.64% 一路升到 **+1.16%**，没有驼峰。
   已在 [`amplitude_2026-08/results.md`](../amplitude_2026-08/results.md) 的「最大软肋」那条下追更正。
2. **`screener_overlap` 那个「包含度塌陷」86% 是人口，不是筛子坏了。** 预设 `Vol Up Gainers` 带 `marketCapMin=1.0`，
   全窗口里预设命中过的 `<$1B` **只有 1 例**，所以 `C(A→B) = (|A_core|/|A|) × C_core`。我独立复算：
   断点前 C=0.642 / C_core=0.640（70 天）；断点后 C=**0.165** / C_core=**0.576**（43 天）——
   **总塌陷 0.477，剔掉非核心后仍在的只有 0.064 → 人口解释 86.5%**（agent 独立算 85.1%，差在窗口口径）。
   **我 09-17 那句「主要由两张单子长度不对称造成」对了一半**：筛子的单子确实从 42 涨到 97–124，
   但**涨的全是预设按定义收不了的小票，它的核心部分反而从 42 缩到 27**。
3. **十个预设里只有 `Weekly Momentum 97` 没有市值下限**（我自己核过 `screener-presets.json`：另外九个全是 `marketCapMin: 1.0`）。
   所以 06-26 那晚，**Screener 页上这一个预设悄悄换了人口，并排的九个没换**，而页面没有任何地方说这件事。
4. **Stockbee 预注册里那个「票的拼写不该预测收益」的对照组，其实是在指认档案断点**：06-26 之后首字母 N–Z 的事件 **0 条**，
   于是 `ctrl = A–M` 成了「这条事件在不在脏窗口」的完美代理。原文把它归为「p 值乐观 2–3 倍」并加跑了安慰剂——
   **安慰剂本身没问题，诊断错了病因。**
5. **`stockbee/gate_results.md` 从来没被 09-17 那轮半字母表重报覆盖过**（那张 20 行判定表里没有它），
   而它恰好是这批里唯一用**无市值闸**筛子的研究——**漏掉的正好是受影响最大的那一份**。
6. **`open_questions.md` 的 S1 建议（改 EP 阈值，会改变 Andy 每天看到的名字）建在断点人口上**：
   它引的「今天漏 47/55」里，**那 47 只有 45 只 `<$1B`**（44 只 `<$500M`）——
   在 06-25 那个人口下「我们漏掉的」是 **2 只不是 47 只**。宇宙修回之前不该执行。
7. **`i4_calibration` §二「上限 3.00× 只剩 9% 余量」的依据是断点后的最大值**（2.54 落在 08-19），断点前历史最大只有 2.25。
8. **归档层**：`leaders_log` / `momentum97_shadow` / `watchlist_hits` 全史 `<$1B` **0.0%**（`watchlist.py:38` `MIN_CAP = 1e9`）；
   `delayed_ep_log` 是 **31.8%**，因为它的上游 `episodic_pivot.py:33` `_MIN_MARKET_CAP = 500_000_000` —— **低了一倍**。
   同一个断点下两批归档命运完全不同，全部原因就在这两行常数。
   （这四个归档都生于 08-13 之后，所以「数它们的行数跨 06-26」物理上不会发生；真正跨断点的派生归档只有 `ticker_events`。）

### 三处「不能重算，明说」

- 老 7 个筛子的 `measured` 对不上已发表数：`event_bars.pkl`（主树本地、不在 git）在那轮跑完之后于 08-19 扩过票池，所以只能做**同轮对比**。
- `summary.md` §一 的三个组合数（`momentum × ATR≤4` 等）是当轮临时切片，仓库里没有可复现的子集定义——**未量，不估**。
- `summary.md` §二.2「格局压过池子」用 `top_3m` 横截面分位，断点后是在翻倍的宇宙上排名次，要重算得整管线 as-of 重跑——**记为无法判定**。

### 一条有用的稳定口径

**「只留核心」跨两次断点都成立**：`universe.json` 里 ≥$1B 的只数 06-25 是 2,588、08-08 是 2,607、09 月中位 2,592，
中间 07 月掉到 1,391（那是字母截断，不是人口）。所以**以 ≥$1B 为统一重报口径是稳的**——
但⚠️ **在 06-26..08-07 那六周里，只留核心也救不回来**：那段连大票也只剩 A–L 那一半。
在那六周里，「只留核心」是必要条件，不是充分条件。
