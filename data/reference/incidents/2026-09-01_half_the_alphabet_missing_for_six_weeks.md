# 2026-09-01 · 归档里有六周只扫了半个字母表，而所有的闸都是绿的

**发现人**：Nighty Zac（夜间轮 09-01）· **发现路径**：跑 Delayed-EP 首次复盘 → 对抗性验证者报「股票池被按字母截断」→ 我去核，发现范围比它报的大一个数量级。

---

## 一句话

**`data/history/ticker_events.csv` 在 2026-06-26 到 2026-08-07 之间的 21 个 session 里，
没有任何一只票的代码首字母在 `L` 之后。全部 15 个 screener，一天不落。
19,850 行 = 全档的 17.9%，是在半个宇宙缺失的情况下写的。**

## 证据

### 边界是硬的，不是渐变的

| 日期 | 行数 | `>L` 的票 | 占比 |
|---|---|---|---|
| 2026-06-24 | 1,098 | 502 | 45.7% |
| 2026-06-25 | 965 | 425 | **44.0%** |
| **2026-06-26** | **1,613** | **0** | **0.0%** ← 断点 |
| … 中间 21 个 session 全部 0.0% … | | | |
| **2026-08-07** | **604** | **0** | **0.0%** |
| *(08-10 整天缺失 —— 另一个归档洞)* | | | |
| **2026-08-11** | **863** | **438** | **50.8%** ← 恢复 |
| 2026-08-12 | 907 | 357 | 39.4% |

前一天 44.0%，当天 0.0%，六周后回到 50.8%。**没有过渡带。**

### 不是某一个 screener 的毛病，是全部 15 个

窗口内 `>L` 占比 vs 该 screener 窗口外的基线：

| screener | 窗口内 n | 窗口内 >L | 窗口外基线 |
|---|---|---|---|
| `gainers_4pct` | 7,009 | **0.0%** | 45.6% |
| `healthy_charts` | 2,136 | **0.0%** | 45.5% |
| `momentum_97` | 2,091 | **0.0%** | 47.1% |
| `preset:sugar_babies` | 1,815 | **0.0%** | 45.9% |
| `vol_up_gainers` | 1,776 | **0.0%** | 46.1% |
| `ema21_watch` | 1,647 | **0.0%** | 45.7% |
| `preset:pp_count` | 841 | **0.0%** | 47.7% |
| `preset:vol_up_gainers` | 579 | **0.0%** | 45.6% |
| `preset:4_bullish` | 465 | **0.0%** | 47.1% |
| `vcp` | 288 | **0.0%** | 43.3% |
| `preset:weekly_20_gainers` | 256 | **0.0%** | 47.5% |
| `preset:pocket_pivot` | 127 | **0.0%** | 46.0% |
| `episodic_pivot` | 93 | **0.0%** | 51.7% |
| `preset:weekly_momentum_97` | 82 | **0.0%** | 50.0% |
| `preset:stockbee_9m_setup` | 41 | **0.0%** | 46.6% |

**15/15，零例外。** 全部 screener 共用一个上游 → 断在共用的那一层，不在任一个筛子里。

单看 `episodic_pivot`：窗口外基线 51.7%，窗口内 93 行**一个都没有**。
若截断不存在，这个概率是 **4.25e-30**。

### 它污染了什么

- **任何跨 2026-06-26..08-07 的 `ticker_events` 统计**，其宇宙只有约一半，且**不是随机的一半**——
  是按代码字母切的，与行业、市值、主题都相关（同族公司代码相邻、ETF 代码分布不均）。
- 我今晚跑的 **Delayed-EP 复盘**正是被它咬到的：账本前 5 个 session（08-13..08-19）的候选票
  最大代码**恰好都是 `LIFE`**，因为 `load_candidates` 回看 45 天，落在这个窗口里；
  从 08-20 起窗口滑出污染区，最大代码立刻变成 `SWMR`/`WEAV`/`WPM`。
  **同一个账本的前 5 天和后 7 天是两个不同的宇宙**，任何把 12 天混在一起的统计都是拼接出来的。

## 为什么没有闸响

`audit_archives` I1–I7 的每一条在这 21 天里都是对的：
日期是真交易日 ✅ · 无重复主键 ✅ · `spx_close` 不与前日相同 ✅ ·
**I4 逐 session 行数落在滚动 20 日中位数的 [floor, 3x] 区间内 ✅**。

I4 是最接近的那一条，而它看不见——因为**行数没有掉一半**。
2026-06-26 有 1,613 行，比 06-25 的 965 行还**多**。
少掉的不是行，是**宇宙的一半**；剩下那一半在这段时间碰巧更活跃。

> **这就是 `pitfall_row_count_is_not_a_shape_check` 的字母版。**
> 我们量了「有多少行」，从来没量过「这些行来自哪一半的股票池」。
> 一个按**内容维度**切掉一半的源，在所有**计数维度**的检查下都是完美的。

## 我没做的 / 交给谁

⚠️ **根因我没定位。** 断点在 `2026-06-26`，恢复在 `2026-08-07..08-11` 之间。
可能的方向（**都没验证**，别当结论）：上游抓取分页只取了第一页 · 某个排序+`[:N]` ·
Finviz 导出的行数上限 · 某次改动引入又被另一次改动无意修好。
**`pipeline/screeners/` 与 `pipeline/adapters/` 不在我的边界内，我一个字节没改。**

**给 DATA ALEX 的四条**：

1. **先定根因再谈回填**。断点日 `2026-06-26` 与恢复日 `2026-08-11` 前后的 commit 是最短的线索。
2. **回填能不能做**取决于源还在不在（Finviz 历史通常拿不回来）。**拿不回来的话，
   正确动作是给这 21 天打一个「宇宙不完整」的标记**，而不是假装它完整。
3. **所有用过这段区间的研究要复查。** 至少这几个的样本跨过了它：
   `b4_gates` · `tightness_study` · `momentum97_shadow` · `oratnek_diff` · `leaders_log`。
   **不是说它们的结论错了，是说它们的宇宙少了一半，得重新报一次。**
4. **加一道内容维度的闸**（不是行数维度）。最便宜的一个：每晚记录
   `>L 首字母占比`，偏离滚动中位数超过 X 个百分点就报。**任何按内容切一半的截断都会撞上它，
   而所有的行数检查都不会。** 这条我可以写进 `audit_*`（在我白名单里），
   但**判据该定在哪一档由数据端定**，我不替 screener 定口径。

## 可复用的判据

**行数正常不代表宇宙正常。**
每一个「这份数据自己对不对」的检查，都默认了**它覆盖的东西没变**。
要检出覆盖的变化，得量一个和数量无关的**形状**——首字母分布、行业分布、市值分位分布。
挑哪个形状不重要，重要的是**它必须是一个即使行数完全不变也会动的量**。

—— 同族：`pitfall_row_count_is_not_a_shape_check` · `pitfall_the_universe_chose_the_answer`
（这是那条坑的第 4 次，前 3 次是便利样本；**这一次连"我选的样本"都不是我选的**）。
