# Accumulation 因子 —— 先证伪手上的东西

*2026-08-09。样本:本地 `data/output/tickers/*.json` 里能对齐到锚点 2026-08-06 的 111 只(相关矩阵用 91 只,rel_volume 有缺)。*
*脚本:`scratchpad/accum.py`(一次性,结论已抄进本文)。*

竞品对照表第 7 项「Accumulation 因子」判为 ❌。动手前先问:**我们手上的东西是不是已经在测这件事了?**

---

## 一、三个候选,两个直接出局

| 候选 | 它实际测什么 | 判定 |
|---|---|---|
| `abc_rating` | EMA10/EMA20/SMA50 的排列 —— **纯价格结构,不碰成交量** | ❌ 而且**个股宇宙里根本没有这个字段**(0/111,只有 ETF 有) |
| `rel_volume` | 单日量比 —— **不带价格方向** | ❌ 和吸筹三件套全部无关:A/D 量比 −0.155 · OBV 斜率 −0.123 · CMF21 −0.019 |
| `pp_count_30d` | pocket pivot 计数 | ⚠️ **是真的吸筹足迹,但定义写错了** —— 见下 |

吸筹的定义核心是**成交量按价格方向加权、跨数周累计**。
前两个各缺一半:一个没有量,一个没有方向。**它们不是「差一点」,是压根不测这件事。**

---

## 二、pocket pivot 的定义偏差

**Morales / Kacher 的原定义:** 上涨日,且成交量 **> 前 10 天「下跌日」的最大量**。
拿下跌日当基准,是因为要比的是**买盘 vs 卖盘**。

**我们的实现**(`pipeline/adapters/yfinance_adapter.py:398`):

```python
max_prior_vol = max(vols[-11:-1])       # 前 10 根「所有」K 线
pp = bool(vols[-1] > max_prior_vol)
```

不分涨跌。这是更严的条件,但**测的东西变了** —— 变成「11 天内量最大」,一个**量能突增**指标,不是吸筹/派发的对比。

（次要:上涨日判据用的是 `close > open`,不是 `close > 昨收`。）

### 代价可量化

复算保真度先立住:**我复算的当前定义 vs 线上 `pp_count_30d`,Spearman +0.975**(n=91)。

| | vs A/D 量比 | vs OBV 斜率 | vs CMF21 |
|---|---|---|---|
| 当前实现 | +0.522 | +0.500 | +0.512 |
| **Morales 原定义** | **+0.708** | **+0.714** | +0.546 |

**拿全部 K 线当基准,白丢约 0.2 的相关性。**

| | 均值 | 中位 | =0 占比 |
|---|---|---|---|
| 当前实现 | 1.74 | 1 | 20% |
| Morales | 3.47 | 3 | 13% |

两者 Spearman 只有 **+0.702** —— 不是可互换的实现细节。

### 实盘上换不换名字:换

| | Top 10 | Top 20 | Top 30 |
|---|---|---|---|
| 两定义重叠 | **3/10** | 10/20 | 18/30 |

**前十名里七个不一样。**

只在当前实现 Top20:`AEVA ANET CGNX CIFR CNMD CRCT DDD LIFE NAIL ONDS`
只在 Morales Top20:`BLZE BUG CHEF CWEB FBRX IGV KNSA NET PATH SOXS`

### 一个边界情况,查过了

我的 Morales 复算在「前 10 天没有下跌日」时判为真(空集上「大于最大值」空真)。
实测 **0/1643 个上涨日出现过这种情况** —— 影响可忽略。**是查了才敢说,不是假设。**

---

## 三、A/D 三件套内部:其实只有两个量

| | A/D 量比 | OBV 斜率 | CMF21 |
|---|---|---|---|
| A/D 量比 | — | **+0.931** | +0.508 |
| OBV 斜率 | +0.931 | — | +0.510 |

**A/D 量比和 OBV 斜率是近似重复**(+0.931)。CMF 是真的另一个东西。
所以「补一个 Accumulation 因子」不需要三个指标,**最多两个**:一条累计资金流 + 一条 CMF。

而 Morales 版 pocket pivot 对 A/D 量比是 **+0.708** —— 高,但只共享约一半方差。
**pocket pivot 数的是离散事件(爆量那几天),A/D 量比测的是持续流向。两者相关但不重复。**

---

## 四、结论与下一步

**结论:不是「从零建一个因子」,也不是「我们已经有了」。是「有,但定义写错了」。**

**建议顺序:**

1. **先修定义** —— 把基准从「前 10 根所有 K 线」改成「前 10 根下跌日」。这是**正确性修复**:字段叫 `pocket_pivot`,而它算的不是 pocket pivot。
2. **再问值不值得加 A/D** —— 修完之后,`pp_count_30d` 和一条 A/D 量比是否还互补,要在面板上重测。

### 一条必须说清的保留意见

「更接近教科书定义」和「相关性更高」**都不等于「交易结果更好」**。
本文没有任何前瞻收益检验 —— 111 只、单一日期,做不了。

要回答「修完之后它值不值得进评分」,需要一个真正的面板:多只 × 多日 × 前瞻收益。
**本地只有 160 只有 OHLC,其中 46 只停在 5 月。那一步要先补数据。**

所以第 1 步是能立刻做的正确性修复,第 2 步是一个独立的、需要先抓数据的研究。
