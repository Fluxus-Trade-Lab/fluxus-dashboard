# 三张探索稿 — 2026.07.31

- `index.html` — 第一轮三张稿（A 乐谱 / B 工业指引 / C 场）
- **`b2.html` — 第二轮：B 选定后做深**（字体 / 色彩 / 真实仪表盘顶部 / 待定格 / 分享卡）

**2026-08-09 定案**：走 B。C 撤销 —— 那些放射线、同心圆、网格是装饰，
而说明里却写着「没有一笔是装饰」，自相矛盾。A 整体逊色，但两个**想法**移植进 B：
**固定读序**（行动永远在末端）和**待定空槽**（把还没发生的事画成一个可见的位置）。

打开 `index.html` / `b2.html`（双击即可，数据已内联；`fonts/` 里的 IBM Plex 是本地字，离线可用）。

三张稿**用的是同一天的同一份真实数据**，区别只在世界观：

| | 稿 | 它主张什么 |
|---|---|---|
| A | **乐谱** | 推理是一份可读的谱。固定读序，音高即进度，未发生的事画成休止符。 |
| B | **工业指引** | 推理是一套指路系统。硬网格、零装饰、线路图，为陌生人三秒定位而设计。 |
| C | **场** | 推理是一片层积的场。所有笔触都是数据，判读作为硬板压在噪音之上。 |

每张稿都必须过 [`../Fluxus_Operator_Model.md`](../Fluxus_Operator_Model.md) 的六条硬约束：
状态向量 / 四语域层级 / 阶梯 / 传导链 / 轨迹+待定格 / 颜色编码回应质量。

---

## 数据来源（全部可复算）

```bash
python3 prep_0731.py     # 阶梯 + 走势 + 扩散计数（本地 OHLC 优先，缺的走 yfinance）
python3 states_0731.py   # 十维状态板 + 传导链（纯推导，写回 data.json）
```

- **breadth** — `data/history/breadth_archive.csv` 的 2026-07-31 行
- **扩散计数** — `data/history/ticker_events.csv`，最近 10 个归档交易日
- **阶梯与走势** — 每只票的日线，`data/output/tickers/<T>.json` 优先

## 三条诚实标注（图上都画出来了，不是脚注）

1. **利率格留空。** 管线没接债市，所以状态板第八格是空的，不编。
2. **归档缺口 13 个交易日。** 07-13 → 07-27 之间的会话在早前的清洗里被删掉了，
   扩散图上用虚线断口如实画出来，不做插值。
3. **`momentum_97` 不计入扩散。** 它是固定 top-N 截断（每天恒定 90），
   当计数用会画成一条假的平线。

## 一处与复盘对不上的地方

7/31 复盘写 `AXTI +28% | Monster earnings | Still below 200-day`。
盘面：收 60.43，**200 日线 46.75（在其上）**，压制它的是 **50 日线 77.52**
—— 这只票 50 日线高于 200 日线（暴涨后暴跌的形态）。
判断是把压在头上的均线记成了 200。图上按盘面画。

这条本身就是改版要解决的问题的例子：数字摆出来，口误就自己现形了。

## 已知的粗糙处（探索稿，不是成品）

- 阶梯的梯级判定是为这三张稿写的近似规则，不是引擎里的 12 条阈值 —— 落地时要对齐，不是另起
- 传导链的亮/半亮/不亮阈值同上
- 状态板的五档 `LEVELS` 是为看形状而设，档位边界没有回测支撑
