"""Mirror the user's X/Twitter Bookmarks folder into local markdown files.

Grep-able archive under `_source_material/tweets/`. Each tweet becomes one
.md file matching the JeffSun_Wiki/sources/tweets/ schema plus bookmark-specific
fields (`bookmarked_at`, `media`).
"""
