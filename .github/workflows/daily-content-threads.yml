# Daily Content Thread Fetch — Fluxus Trading Community
# Fetches Andy's Discord messages and commits them as
# data/output/threads/{folder}/messages.json. Generation happens in the
# cloud routine "Discord→X 草稿（云）" (Max subscription, no API credits) —
# 2026-09-10 redesign: the old in-CI Claude API call died on credit balance
# and is gone; this workflow no longer needs ANTHROPIC_API_KEY.
name: Daily Content Threads

on:
  schedule:
    # 22:00 UTC = 17:00 EST Mon-Fri (30 min after data pipeline)
    - cron: '0 22 * * 1-5'
  workflow_dispatch:
    inputs:
      date:
        description: 'Date to generate threads for (YYYY-MM-DD). Default: today.'
        required: false

permissions:
  contents: write

jobs:
  generate-threads:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: pipeline/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r pipeline/requirements.txt

      - name: Generate thread drafts
        run: |
          if [ -n "$INPUT_DATE" ]; then
            python -m pipeline.content.discord_to_thread --fetch-only --date "$INPUT_DATE"
          else
            python -m pipeline.content.discord_to_thread --fetch-only
          fi
        env:
          INPUT_DATE: ${{ github.event.inputs.date }}
          PYTHONPATH: ${{ github.workspace }}
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
          DISCORD_CHANNEL_ID: ${{ secrets.DISCORD_CHANNEL_ID }}
          DISCORD_USER_ID: ${{ secrets.DISCORD_USER_ID }}

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/output/threads/
          git diff --staged --quiet || (git commit -m "content: discord messages fetched $(date +%Y-%m-%d)" && git push)

      - name: Discord notification (success)
        if: success() && env.DISCORD_WEBHOOK_URL != ''
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          curl -s -H "Content-Type: application/json" \
            -d "{\"content\":\"Discord messages fetched — draft generation queued (cloud)\"}" \
            "$DISCORD_WEBHOOK_URL"

      - name: Discord notification (failure)
        if: failure() && env.DISCORD_WEBHOOK_URL != ''
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          curl -s -H "Content-Type: application/json" \
            -d "{\"content\":\"Content pipeline FAILED — check Actions logs\"}" \
            "$DISCORD_WEBHOOK_URL"
