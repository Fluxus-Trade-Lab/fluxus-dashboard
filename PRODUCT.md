# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**主要用户:Andy(操作者本人)。** $1M 自营账户,两腿系统(战术 + 核心),日本时区,
盘前 06:30 JST 读盘。他是唯一每天使用全部界面的人。

**第二读者:付费订阅者。**(2026-08-18 确认)官网定价页把 "Full daily brief + watchlist"
列为付费内容,所以**选股器和今日名单不是私人仪器**——每一格都必须能被一个陌生人读懂。
这条直接决定:配方 / 定义 / 口径从「可选的附加说明」升为**必备**。

## Product Purpose

一台把市场数据变成**可复核判断**的仪器。它不预测价格,只报「给了机会 / 接住没接住」。

成功的定义:**一个陌生人能从界面自己得出结论**,而不是被递到手上一个结论。
(DESIGN.md 〇:「让推理被看懂,不是让数字被看见。」)

选股器与今日名单这两页的共同任务是 **发现潜在标的**(2026-08-18 确认)。

## Positioning

三件邻居产品无法照抄的:

1. **每一个数字都能追到它的来源和口径。** 阈值、股票池、截断、未测量状态全部在界面上写明。
2. **零结果照发。** 序列挖掘 0/42、52 周高点筛选负阿尔法、Liquid Leader 20 日预测 NULL ——
   都留在产品里,不删。
3. **自建全链路**:自己的管线、自己的股票池口径、自己的移植(VCS / LL-HL / Pocket Pivot
   逐位对过黄金样本),不是第三方扫描器的皮。

## Operating Context

**盘前 06:30 JST**(美股前一日收盘后)。数据每晚 cron 生成静态 JSON,前端只渲染,不现算。

**这两页上的实际动作**(2026-08-18 确认,按顺序):

1. **先看一眼市场对不对** —— 今天该不该动手。环境读数必须在名单**之前**。
2. **就地做今天的短名单** —— 在页面上挑出要盯的名字,形成一份当日名单。
   这意味着页面需要**选中 / 标记 / 带走**的机制,并且要记住选了谁。

**晨读是一条三段的路,不是四个独立页面**(2026-08-18 确认):

| 段 | 页 | 它回答 |
|---|---|---|
| 1 | **Dashboard** | 今天该不该动手 —— 市场状态 |
| 2 | **Themes** | 钱在往哪儿走 —— 主题的趋势变化 |
| 3 | **Screener / Watchlist** | 买哪几只 —— 个股选择 |

**三个合起来才是一个包。** 单独设计其中任何一个,都会漏掉它上游给的条件和下游要的产出。
Dashboard 与 Themes 目前已大致定型,但**仍有更改空间**。

第 3 段内部两个入口的分工:**同一件事的两条路,不是主次关系。**

| | 走哪条路发现标的 |
|---|---|
| 选股器 | **主题与个股的变化和趋势** —— 四态、RS、加速度、行业/主题归属 |
| 今日名单 | **当日事件扫描** —— 结构转折 / 口袋支点 / 压缩 / 动量,**不走主题维** |

## Capabilities and Constraints

**数据**:`data/output/*.json`,每晚 cron。股票池闸 = 市值 ≥$1B 且日成交额 ≥$2M(2,084 只)。
`tradeable` 布尔列是管线的定义,**前端不得自行重算**。

**今日名单**读 `watchlist.json`:zones → panels → tickers。每格自带 `recipe`(筛选规则原文)、
`measured` 布尔、`count` 与 `truncated`。票项带 `rs_1m`、`rs_line_pctl_21`、`rs_high`、`top_3m`。

**选股器**读 `universe.json` + `groups.json`:八个扫描集、四态、主题/行业归属。

**必须守住的三条显示纪律**(已在产品里,不可退化):

- `measured: false` 显示「未测量」,**永远不是 0**。
- 截断必须报数(`25 / 共 372`),静默截断等于宣称「就这些」。
- 任何按结果排序的列表,必须自曝选择机制。

**术语**:四态 Leading / Weakening / Improving / Lagging;took(抓住了机会)/ refused(给了机会没抓住);
未测量(never 无 / 0)。中文永不进等宽字。

**未定**:短名单的存储位置(localStorage / Google Sheet)未定;三段之间是否传递状态
(在 Themes 选中的主题要不要收窄第 3 段的名单)未定。

## Brand Commitments

- **反多巴胺**:不用霓虹绿、不用火箭、不用「十倍」。图书馆,不是赌场。
- **字体**:IBM Plex(Sans / Condensed / Mono),中文 PingFang SC。
- **暗色地**:`#12110F`(品牌墨色,海报系统同源)。
- **编码对**:海报蓝 `#3b82c4` / 海报红 `#d94032`(landing page 上那两个球)——
  填充用原色,文字用同色相同饱和、按对比度抬亮的 `#77b4fb` / `#ef5442`。
- **表情组永不上仪表盘。**
- 完整视觉规格见 `DESIGN.md`,证据在 `Fluxus_Brand/visual/`。

## Evidence on Hand

- 真实管线数据:`data/output/`(universe / watchlist / groups / breadth,每晚更新)。
- **与 oratnek 扫描的逐格对照**(2026-08-17):他的 RS 1M 破译为 `rs_line_pctl_21`,29/29 复现;
  逐格差异与四个已证伪假设记录在案。
- 零结果三则(序列挖掘 0/42、52 周高点筛选、Liquid Leader NULL)——**产品里保留,不许删**。
- **没有的东西,不许编造**:没有用户见证、没有订阅数、没有第三方背书、没有业绩承诺。

## Product Principles

1. **可追溯优先于简洁。** 一个数字如果说不清口径,宁可不显示。
2. **未测量、找到零个、被门槛挡住是三种状态**,长得必须不一样。
3. **陌生人测试。** 每一格都要能被一个不在这个房间里的人读懂(付费读者)。
4. **环境在名单之前。** 三段的顺序就是判断的顺序:市场 → 主题 → 个股。
   任何一段都不许在它的上游条件还没给出时就要求你做决定。
5. **发现,不是宣判。** 这两页产出候选与依据,不产出买卖指令。

## Accessibility & Inclusion

- 对比度 ≥4.5(小字)/ ≥3(图形),全站探针可复跑。
- **灰度是承重列**:任何编码在灰度下必须仍可区分(截图与打印是真实使用条件)。
- 三个以上身份不得只靠一个色彩通道承担,必须加第二通道(字重 / 位置 / 纹理)。
