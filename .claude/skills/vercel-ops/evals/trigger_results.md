# 触发率实测（skill-creator run_loop，2026-09-13）

20 条查询（10 该触发 / 10 近邻不该触发）× 每条 3 次，60/40 训练/留出，最多 5 轮改写。模型 claude-opus-5，在空白临时项目里跑 `claude -p`（没有本仓库的 CLAUDE.md）。测的是 description+when_to_use 拼成的一段。

| 轮 | 训练 正确 | 训练 precision | 训练 recall | 留出 正确 | 留出 precision | 留出 recall |
|---|---|---|---|---|---|---|
| 1 | 22/36 | 100% | 22% | 14/24 | 100% | 17% |
| 2 | 20/36 | 100% | 11% | 15/24 | 100% | 25% |
| 3 | 21/36 | 100% | 17% | 14/24 | 100% | 17% |
| 4 | 20/36 | 100% | 11% | 15/24 | 100% | 25% |
| 5 | 21/36 | 100% | 17% | 14/24 | 100% | 17% |

最佳留出分 `4/8`；最佳描述就是原稿，5 轮改写没有一轮胜过它。

**读法**：precision 一直 100%（近邻题一次没误触发），recall 很低（该触发的多数没触发）。离 skill OS v2 的 ≥80% 目标很远，**未达标**。
已知的量具局限（没有量化过，不当结论用）：①这套 harness 把 skill 注册成临时 command，并在空白目录里跑，模型对「看一下」「怎么处理」这类短问句常直接作答不调工具；②真实会话里还有 `paths:` 触发（改 vercel.json 等三处文件时自动带上），harness 不测它。
兜底：KNOWLEDGE 方法层与 SOP 登记处已登记；memory `pitfall_no_one_owns_the_product` 与 MEMORY.md 索引指向本 skill。下一步若要提 recall，先换量具（在本仓库目录里跑、用真实会话 transcript 回放）再改描述。
